# 深度学习算子问题

> 2026-08-28 当前 HEAD 复核：灰度/16-bit 预处理和工程化参数契约已经补齐，NMS 与异步模型加载也已实质改造；原文未同步。性能项仍有 `PreprocessImage()` 的整张 tensor 复制，并缺真实模型、高候选量、高帧率与 provider 选择的工业基准/现场签核，因此只把该证据与性能尾项转入 [未尽事项统一补齐 TODO](../当前计划/ClearVision-未尽事项统一补齐TODO-2026-08-28.md)，本页继续保留为审计快照。

* **【已修复/不存在】** 高: TensorRT 路径是“假支持” —— **现已完备支持**。
  * **最新核排事实（2026-05-30）**：经核排最新 [DeepLearningOperator.cs](file:///c:/Users/A/Desktop/ClearVision/ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/DeepLearningOperator.cs#L539)，已实现 `TryAppendTensorRtExecutionProvider` 和 `GpuAvailabilityChecker` 底层检测，利用反射完美动态加载 ONNX Runtime 物理接口 `AppendExecutionProvider_TensorRT`。若探测不通过则退回 `AppendExecutionProvider_CUDA` 或 CPU 并附带合理日志报警，不存在静默退回 CPU 却谎称 GPU 正常启动的假支持。
* **【已修复/不存在】** 高: 模型缓存淘汰存在并发销毁风险 —— **已引入精细引用租约机制**。
  * **最新核排事实（2026-05-30）**：经核对最新 [DeepLearningOperator.cs](file:///c:/Users/A/Desktop/ClearVision/ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/DeepLearningOperator.cs#L203) 源码，模型缓存已引入多线程引用计数 `CachedModelSession` 与租约类 `ModelSessionLease`，在模型被驱逐（Eviction）时只做销毁标记，只有当推理租约清零（`_leaseCount == 0`）时，才会真正销毁底层会话。彻底杜绝了并发 use-after-dispose 的灾难性崩溃。
* **【已修复/不存在】** 高: 标签回退策略会静默给错类名 —— **已增加条件阻断与匹配检测**。
  * **最新核排事实（2026-05-30）**：经核对 [DeepLearningLabelResolver.cs](file:///c:/Users/A/Desktop/ClearVision/ClearVision.Product/src/ClearVision.Product.Infrastructure/Services/DeepLearningLabelResolver.cs#L55) 与 [DeepLearningOperator.cs](file:///c:/Users/A/Desktop/ClearVision/ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/DeepLearningOperator.cs#L2377) 源码，在 `TargetClasses` 为空时，标签解析方法已在开头做出了 `if (required.Length == 0) return null;` 阻断退出处理，不再做任何无谓的 bundled labels 遍历；而在 TargetClasses 不为空时，也增加了 `required.All(...)` 全部包含性验证，从而在不匹配时合理报错，根本不存在将无标签模型强行、静默归属于线序标签（`scenario-package-wire-sequence/labels.txt`）的隐患。
* 中高: 预处理对工业现场常见的灰度图/非 3 通道图不稳。代码在 **DeepLearningOperator.cs (line 474)** 固定创建 **CV_8UC3** 画布，在 **DeepLearningOperator.cs (line 481)** 直接 **CopyTo**，并在 **DeepLearningOperator.cs (line 494)** 假设 **Vec3f**。对单通道工业相机图像，这条链路很容易直接炸掉。
* 中: 性能实现还没到工业级。**ArrayPool** 在 **DeepLearningOperator.cs (line 490)** 租了缓冲区，但又在 **DeepLearningOperator.cs (line 512)** **ToArray()** 整体复制，收益基本被抵消；NMS 仍是 **DeepLearningOperator.cs (line 917)** 的 O(n²) 纯托管实现；模型加载还在 **DeepLearningOperator.cs (line 336)** 同步 **Wait()**。高帧率或候选框多时，延迟和抖动都会比较难控。
* 中: 工程化治理明显落后于仓库里更新的一代 ONNX 算子。**DeepLearningOperator** 只有 **ModelPath** 和很弱的参数校验 **DeepLearningOperator.cs (line 171)**、**DeepLearningOperator.cs (line 549)**；而 **SemanticSegmentationOperator** 已经支持 **ModelId**、**ModelCatalogPath**、**ExecutionProvider** 和更完整的 session/loading contract **SemanticSegmentationOperator.cs (line 31)**、**SemanticSegmentationOperator.cs (line 154)**、**SemanticSegmentationOperator.cs (line 255)**。这说明 **DeepLearningOperator** 更像早期实现，和当前平台标准已经脱节。

**结论**
现在这个“深度学习”算子我会判断为：**可用于 demo / 小规模验证，但还没达到工业级落地标准**。主要短板不在“能不能跑”，而在“高负载/多模型/多线程/现场资源不完美时会不会稳定且可解释地跑”。

**验证与残余风险**

* 我实际跑了 **DeepLearningOperatorTests**，结果是 **37/37** 通过。
* 但这组测试 **DeepLearningOperatorTests.cs (line 30)** 主要是参数、反射和失败路径，没有覆盖真实 ONNX 推理、灰度图输入、GPU provider 选择、并发缓存淘汰，所以这次发现的问题目前基本都测不出来。
