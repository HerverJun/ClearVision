# 深度学习算子问题

> 2026-08-28 专业当前性复核：灰度/16-bit 预处理、NMS、异步加载和工程化参数契约均已实质改造。正式推理在 `DeepLearningOperator.cs:559` 使用 `PreprocessImageLease()`，带 `ToArray()` 的 `PreprocessImage()` 只是无生产调用的私有兼容 wrapper，因此“整张 tensor 复制是生产性能阻断”属于误判。真正残项已转入 [未尽事项统一补齐 TODO](../当前计划/ClearVision-未尽事项统一补齐TODO-2026-08-28.md) U01/U07：smoke/precision 声明隔离、交付模型身份、目标硬件 provider/性能证据和现场签核。本页继续保留为历史审计快照，以下旧行不得脱离顶部纠偏单独执行。

* **【实现存在/交付证据待补】** 高: TensorRT 路径不是“假支持”。代码已实现 TensorRT/CUDA 探测和 fallback；仍须让实际 provider/fallback 原因进入稳定报告，并只对目标发布 SKU 做硬件实测，不能用代码路径存在替代现场证据。
* **【已修复/不存在】** 高: 模型缓存淘汰存在并发销毁风险 —— **已引入精细引用租约机制**。
  * **最新核排事实（2026-05-30）**：经核对最新 [DeepLearningOperator.cs](file:///c:/Users/A/Desktop/ClearVision/ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/DeepLearningOperator.cs#L203) 源码，模型缓存已引入多线程引用计数 `CachedModelSession` 与租约类 `ModelSessionLease`，在模型被驱逐（Eviction）时只做销毁标记，只有当推理租约清零（`_leaseCount == 0`）时，才会真正销毁底层会话。彻底杜绝了并发 use-after-dispose 的灾难性崩溃。
* **【已修复/不存在】** 高: 标签回退策略会静默给错类名 —— **已增加条件阻断与匹配检测**。
  * **最新核排事实（2026-05-30）**：经核对 [DeepLearningLabelResolver.cs](file:///c:/Users/A/Desktop/ClearVision/ClearVision.Product/src/ClearVision.Product.Infrastructure/Services/DeepLearningLabelResolver.cs#L55) 与 [DeepLearningOperator.cs](file:///c:/Users/A/Desktop/ClearVision/ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/DeepLearningOperator.cs#L2377) 源码，在 `TargetClasses` 为空时，标签解析方法已在开头做出了 `if (required.Length == 0) return null;` 阻断退出处理，不再做任何无谓的 bundled labels 遍历；而在 TargetClasses 不为空时，也增加了 `required.All(...)` 全部包含性验证，从而在不匹配时合理报错，根本不存在将无标签模型强行、静默归属于线序标签（`scenario-package-wire-sequence/labels.txt`）的隐患。
* **【已修复/待同步】** 中高: 灰度/16-bit 和非 3 通道预处理已有明确转换与契约测试，旧行号和旧实现不再代表当前代码。
* **【旧阻断删除】** 中: 正式路径使用池化租约，不执行旧 wrapper 的 `ToArray()`；NMS 与异步模型加载也已改造。死 wrapper 可清理，但不是工业验收条件。剩余性能工作是按目标 SKU 对真实交付模型测高候选量、高帧率、多线程、p50/p95 和峰值内存。
* **【已修复/待同步】** 中: DeepLearning 已具备 ModelId/ModelCatalog/ExecutionProvider 等工程化契约。仍需补的是 session cache 的模型内容 hash，而不是重做整套参数体系。
* **【真实开放】** 高: `DeepLearning_coco_real_model_baseline` 使用 generated constant smoke model，AP50/Precision/Recall 为 0，却仍可 `Accepted=true` 并被聚合为质量 Pass。该报告必须降为机器可判的 `InferenceSmokeOnly`，实际精度按交付模型 manifest、hash、数据集和非零门槛验收。

**结论**
当前实现已经不能再按旧文笼统判为“仅能 demo”。代码层的预处理、租约、NMS、加载和参数契约已明显推进；尚不能闭环的是**质量声明真实性、模型内容身份、交付硬件性能与现场证据**。因此结论是“实现主体可进入交付 profile 验证，工业级声明仍待证据”，而不是重新实施旧代码改造清单。

**验证与残余风险**

* 原审计曾记录 **DeepLearningOperatorTests 37/37**；这是历史结果，不是本次文档复核的新鲜测试证据。
* 单元测试不能替代真实交付模型、目标 provider、高负载和现场签核；这些证据按 U01/U07 的 release SKU/profile 分层取得。
