# F07 G1-R：权限合同修补报告

## 状态

```text
REPORT_STATE=DONE
INITIAL_SHA=084165300d87b2abf09d5a879da53920531672a5
G1_R_SHA=084165300d87b2abf09d5a879da53920531672a5
F07_G1_PERMISSION_BLOCKER=CLOSED
F07_SETTINGS_ROUTE=IMPLEMENTED_IN_G2
F07_SECTION_SAVES=NOT_IMPLEMENTED
G3_IMPLEMENTATION=FORBIDDEN
```

G1-R 在本轮 G2 开始前已经完成并推送。本报告补齐用户要求的下划线命名入口；历史的详细修补报告仍保留在 `F07_G1-R_权限合同与操作门禁修补报告.md`。

## B01 关闭

以下 endpoint 使用既有 `RequireAuthenticated` permission contract：

| Endpoint | 未认证 | Admin | Engineer | Operator |
| --- | --- | --- | --- | --- |
| `GET /api/settings` | 403 | full projection | safe subset | safe subset |
| `GET /api/ai/models` | 403 | full redacted DTO | safe DTO | safe DTO |
| `POST /api/ai/reasoning-support` | 403 | catalog | catalog | catalog |

未修改 safe/full projection、AI redaction、角色语义、后端 authority 或配置默认值。

## Owner 门禁

`SettingsOwner.enqueueEndpointOperation(section, endpointId, task)` 必须解析 `SETTINGS_ENDPOINT_MATRIX` 的具体 contract 后才进入既有 section 串行 coordinator：

- Engineer 允许 `plc.test-connection`、`tcp.runtime`、`camera.trigger-and-preview`。
- Engineer 禁止 Admin-only generic/configuration mutation。
- Operator 全部禁止。
- unknown、excluded、section mismatch、route-only endpoint fail closed。
- task context 携带已解析 endpoint contract；不再仅凭 Settings route 角色执行操作。

## 验证

- Settings unit/typecheck/lint：PASS。
- Desktop endpoint 定向测试：`SettingsResetEndpointTests` + `AiModelEndpointsTests`，串行 58/58 PASS。
- TRX：`.tmp/test_results/f07-g1r-20260801-final/f07-g1r-settings-ai.trx`。

G1-R 没有新增 HTTP、EventBus、HostBridge、持久化或后端权限；G2 只复用本合同和 `ProductRuntime.api`。
