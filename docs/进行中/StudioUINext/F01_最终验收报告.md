# Studio UI Next F01 最终验收报告

> 本文记录最终提交前的可提交技术事实。Git push 与最终 SHA GitHub Actions 属于提交后外部证据，仓库内状态保持 `AWAITING_CI`；最终 run URL、run ID、SHA 与 conclusion 由不再改动该 SHA 的交付回报记录，避免“写回 CI 证据又产生新 SHA”的循环。

## 1. Git 基线与状态

```text
Branch=studio-ui-next
Initial SHA=b288b77ff2902b0fdf22059d356ed3c1a8d11cec
Initial origin/studio-ui-next=b288b77ff2902b0fdf22059d356ed3c1a8d11cec
Final audited origin/codex初稿=01e88c5e82ddc7c8ddbf3138383c225272f7331a
Candidate Final SHA=AWAITING_COMMIT
Remote SHA=AWAITING_PUSH
FINAL_SHA_CI=AWAITING
VISUAL_CONFIRMATION=AWAITING_USER
F01_STATE=AWAITING_CI
```

最终稳定线新增 3 个提交只涉及 operator naming tests 与生成/手册文档，与 F01 authority/security/contract 无交集，因此按单向同步规则审计后不合入。

提交后若最终 SHA CI PASS，且用户尚未视觉确认，唯一合法状态转为 `F01_STATE=AWAITING_VISUAL_CONFIRMATION`。不得标记 DONE，不得启动 F02。

## 2. 并行组织

本轮按长 Goal 使用 1 个主协调 + 3 个子代理角色：

| 角色 | 实际工作 | 边界 |
| --- | --- | --- |
| A Design Foundation | tokens、primitives、Design Lab、unit、Design Playwright 与截图 | 未修改 Host、Canvas canonical、CI；无 commit/push |
| B FlowCanvas/browser | canonical Canvas 宿主、fixture、interaction/identity/lifecycle、中央 Playwright | canonical 共享修改交主协调集成；无 commit/push |
| C WebView2/DPI/performance/no-Node | 泛化既有 runner、真实矩阵、DPI probe、A/B 与 no-Node evidence | 未创建第三 runner；无 commit/push |
| 主协调 | 共享文件、authority 审计、集成、完整回归、文档、Git 与 CI | 唯一提交/推送 owner |

并行只用于无重叠工作包；Router/App composition、Vite、Host、`.csproj`、canonical 共享边界、测试集成和全部文档由主协调串行收口。

## 3. 原文矩阵

逐条账本：`F01_原文对照验收矩阵.md`。

提交候选统计：

```text
TOTAL=191
PASS=183
WARNING=1
NOT_PERFORMED=3
AWAITING_USER=1
AWAITING_CI=2
AWAITING_PUSH=1
BLOCKED=0
UNEXPLAINED_PENDING=0
```

最终 push/CI PASS 后，运行时统计应为：PASS 186、WARNING 1、NOT_PERFORMED 3、AWAITING_USER 1、BLOCKED 0。

## 4. Design Foundation

```text
DESIGN_FOUNDATION=PASS
```

- 完整 token 类别：color、typography、spacing、radius、elevation、motion、z-index、status、canvas、density；
- light/dark、compact/comfortable、system/explicit reduced motion；
- Typography、Surface、Button、IconButton、Field、Select、Panel、StatusBadge、Modal、Toast、Splitter 等代表 primitives；
- Modal focus trap/Escape/焦点恢复、Toast timer 清理、Splitter keyboard/pointer listener 生命周期；
- brand 与 OK/NG/Warning/Info/Idle 独立；
- 不加载 legacy CSS，不复制 FrontendV2；
- Central Playwright Design 5 场景 PASS，12 张 viewport/theme/density 截图。

截图索引：`F01_DesignLab截图索引.md`。

## 5. Canvas

```text
CANVAS_DECISION=REUSE_EXISTING_FLOW_CANVAS
CANVAS_CORRECTNESS=PASS
CANVAS_IDENTITY=PASS
CANVAS_LIFECYCLE=PASS
CANVAS_PERFORMANCE=WARNING
```

唯一 canonical 为现有 `flowCanvas.js` / `flowCanvasAdapter.js` / `FlowEditorInteraction`。没有第二内核、Pointer Kernel、前端私有 flow authority 或 Pinia Canvas instance。

关键结果：

- rejection matrix 全部命中预期错误码；
- drag、pan、zoom、resize、selection、connection pointer gestures PASS；
- 20 次 mount/unmount，ownerCount 1→0，listener/RAF/ResizeObserver/theme observer 释放；
- canonical、100/150、300/450 fixtures identity fingerprint 保持；
- 3 个完整性能组，2 warmups + 5 formal samples，hard failure 0；
- 仅 g01 drag 单场景超过 20%，g02/g03 未复现，故 WARNING 而非 BLOCKED。

详见 `F01_Canvas验证报告.md`。

## 6. 真实 WebView2

```text
REAL_WEBVIEW2=PASS
```

`f01m3` 10/10：Debug legacy、diagnostics、Design、Canvas × DPR 1/1.25/1.5/2；Release publish diagnostics、publish Canvas、missing-assets。public health 与 authenticated read-only API 全部成功；`mountCount=1`、`activeRoot=studio-ui`、console/page errors 为空；进程、user-data、DB 与 runtime 可清理。

详见 `F01_WebView2验收报告.md`。

## 7. DPI

```text
DPI_AUTHORITY=PASS
EFFECTIVE_RUNTIME_DPI_MODE=PerMonitorV2
```

唯一 authority 为 Desktop `.csproj` 的 `ApplicationHighDpiMode=PerMonitorV2`；Program/manifest 无第二声明。Win32 runtime probe 证实真实进程为 PerMonitorV2。WebView2 force scale、JS DPR、CDP layout、native size、Canvas backing/hit testing 分层记录。

详见 `F01_DPI证据报告.md`。

## 8. Publish 与 no-Node

```text
LOCAL_NO_NODE_EVIDENCE=PASS
CLEAN_MACHINE_WITHOUT_NODE=NOT_PERFORMED
```

- Release win-x64 self-contained publish PASS；
- forbidden publish artifacts 0；
- 15 份 Desktop process-tree evidence 的 Node descendants 均为 0；
- 16 份 sanitized PATH/cleanup evidence PASS；
- 外部 Node CDP driver 明确位于 Desktop process tree 外；
- 干净未安装 Node 的独立 Windows 目标机没有执行。

详见 `F01_publish与no-Node报告.md`。

## 9. 本地强制门禁

```text
StudioUI npm ci = PASS
StudioUI lint = PASS
StudioUI strict typecheck = PASS
StudioUI unit = 17 files / 107 tests PASS
StudioUI build = PASS
Central Playwright studio-ui-next = 10/10 PASS
Legacy Canvas core = 32/32 PASS
Legacy template/interaction = 14/14 PASS
Legacy Chromium = 3/3 PASS
Desktop focused = 72/72 PASS
Desktop full = 612/612 PASS
Release self-contained publish = PASS
Publish audit = PASS
真实 WebView2 matrix = 10/10 PASS
DPI evidence = PASS
Local no-Node evidence = PASS
7 PowerShell scripts AST parser = PASS
6 related CJS node --check = PASS
```

Desktop full 最初诊断暴露 9 个既有 fixture/contract 失配；主协调仅修正 test package 的正式 final-decision binding、测试 fixture/hash 与 wire-sequence 断言，没有放宽 Runtime authority。最终完整复跑 612/612。

## 10. 原始 evidence

- WebView2/DPI/no-Node：`.tmp/studio-ui-next/f01/matrix/f01m3/`
- 性能原始样本：`.tmp/studio-ui-next/f01/matrix/f01m3/performance/`
- Central Playwright：`.tmp/studio-ui-next/f01/playwright/final-central/`
- Legacy Playwright：`.tmp/studio-ui-next/f01/playwright/final-legacy/`
- Focused TRX：`.tmp/studio-ui-next/f01/dotnet/desktop-focused-final-2/desktop-focused-final-2-focused.trx`
- Full Desktop TRX：`.tmp/studio-ui-next/f01/dotnet/desktop-full-final/desktop-full-final.trx`

## 11. NOT_PERFORMED 与外部等待

```text
Real Windows 100/125/150/200 system-scale matrix=NOT_PERFORMED
PerMonitorV2 cross-monitor move=NOT_PERFORMED
Real 200% industrial display=NOT_PERFORMED
Clean Windows target with Node never installed=NOT_PERFORMED
Real camera/PLC/Station/site acceptance=OUT_OF_SCOPE_NOT_PERFORMED
User visual confirmation=AWAITING_USER
Final SHA GitHub Actions=AWAITING_CI
```

## 12. 受保护文件

`CLAUDE.md` 是用户修改，文件 SHA256 保持：

```text
B51CA5520D5D359B5EC3C10A3164F01C0061D8F4C582531184B92DE5F364D837
```

提交前必须再次确认 `git diff --cached -- CLAUDE.md` 为空，提交后确认 commit file list 不含该文件。

## 13. 最终状态块（提交候选）

```text
ORIGINAL_PLAN_REQUIREMENTS_ACCOUNTED_FOR=YES
DESIGN_FOUNDATION=PASS
CANVAS_DECISION=REUSE_EXISTING_FLOW_CANVAS
REAL_WEBVIEW2=PASS
DPI_AUTHORITY=PASS
CANVAS_PERFORMANCE=WARNING
LOCAL_NO_NODE_EVIDENCE=PASS
FINAL_SHA_CI=AWAITING
VISUAL_CONFIRMATION=AWAITING_USER
F01_STATE=AWAITING_CI
```
