# F09 完成报告

```text
F09_STATE=PARTIAL
EXECUTION_MODE=OVERNIGHT_CONTINUOUS
INITIAL_SHA=246dbe581ea27c9568b9a85e5006d41247599609
CODE_CANDIDATE_SHA=5513f36c87508f6ae343ad6d77727ea3b4f056ee
FINAL_SOURCE_SHA=NOT_PRODUCED
FINAL_EVIDENCE=NOT_RUN
BRANCH=studio-ui-next
DEFAULT_ENTRY=NEXT_DEFAULT_CANDIDATE
NEXT_UI_DEFAULT_ENTRY=NOT_ENABLED
LEGACY_ROLE=CONFIGURABLE_FALLBACK
FOLLOW_UP=F09_R_SMALL_REPAIR
F10_LONG_PLAN=FORBIDDEN
P0_OPEN=0
P1_OPEN=2
P2_OPEN=5
P3_OPEN=0
```

本报告记录的是应用代码候选 `5513f36c8750` 的静态/浏览器级状态；本报告及其交接文档不改变应用代码候选。最终候选的干净提交证据尚未产生，因而不声明 `FINAL_SOURCE_SHA`。`NEXT_DEFAULT_CANDIDATE` 不是 `NEXT_DEFAULT`，本轮没有修改 `appsettings.json` 的启动 Profile。

## 结论

F09 已完成 G0/G1 的审计与能力矩阵、G2/G3 的候选代码和保护门禁、以及 G7 的状态收口。G4 至 G6 没有满足正式默认入口所需的全部证据，因此 F 系列不能关闭，M 系列也不能作为完成交接开始。

阻断默认切换的事实是：

- `F09-I001`：Operator 的后端权限是有意的只读投影，正式 admission、execute、PLC、项目写入与 `/inspection` 均为 `403`。不得用前端或测试夹具放宽该边界。
- `F09-I002`：历史 rollback manifest 在 Desktop Host close/flush 超过 15 秒时失败，尚无最终候选源码的成功 rollback 演练。

## Goal 状态

| Goal | 状态 | 说明 |
| --- | --- | --- |
| G0 基线与台账 | DONE_WITH_OPEN_ISSUES | 见 [G0 审计](./F09_G0_进入审计与问题台账.md)。 |
| G1 能力矩阵 | DONE_WITH_DEFERRED_LEGACY_FALLBACKS | 见 [G1 矩阵](./F09_G1_LegacyNext终局能力矩阵.md)。 |
| G2 入口、权限、生命周期 | DONE_WITH_P1 | Workspace flag-off 形成零 owner 投影；P1 仍阻断默认切换。 |
| G3 Profile 与回退机制 | DONE_WITH_P1 | 配置级 Profile 已实现；最终候选的 rollback PASS 尚未获得。 |
| G4 数据兼容与异常恢复 | PARTIAL | 不把历史或代码级证据写成最终用户旅程 PASS。 |
| G5 Host、发布、no-Node、CI | PARTIAL | 前端门禁通过；Desktop/发布/现场/CI 证据尚未完成。 |
| G6 Pilot 与默认切换 | PARTIAL | 既有/代码级只读 Operator pilot 通过；最终候选的完整 G6 证据未产生，`NEXT_DEFAULT` 切换被 P1 正确阻断。 |
| G7 最终证据与交接 | DONE_WITH_OPEN_ISSUES | 本报告、证据清单、手册与 Legacy/M00 边界已归档。 |

## 已验证的代码候选会话证据

下列命令在本次任务中、工作树尚干净且 `HEAD=5513f36c8750` 时执行。其控制台结果可证明该代码候选的前端门禁，但不替代最终候选的 WebView2/Profile/Rollback/Final manifest。

| 范围 | 结果 | 证据 |
| --- | --- | --- |
| StudioUI lint | CODE_CANDIDATE_SESSION_PASS | `npm run lint`，代码候选 `5513f36c8750`。 |
| StudioUI typecheck | CODE_CANDIDATE_SESSION_PASS | `npm run typecheck`。 |
| StudioUI unit | CODE_CANDIDATE_SESSION_PASS | `npm run test:unit`，128 files / 790 tests。 |
| StudioUI production build | CODE_CANDIDATE_SESSION_PASS | `npm run build`。 |
| Bundle gate / reproducibility | CODE_CANDIDATE_SESSION_PASS | `npm run bundle:ci` 与 `npm run bundle:verify`。 |
| G15X capability guard | CODE_CANDIDATE_SESSION_PASS | `node --test tests/unit/g15x-capability-owners.test.mjs`，12/12。 |
| F03 Workspace | PASS | 外部管理静态服务下 54/54；受管 Playwright teardown 的 access-denied 另列 `F09-I004`。 |

## 未作为 PASS 声明的范围

| 范围 | 状态 | 原因 |
| --- | --- | --- |
| Product full | NOT RUN | 本次代码候选未运行完整 Product 回归。 |
| Desktop full / 最终候选 WebView2 | NOT RUN | 作者收口时同一 Desktop 项目已有外部 `dotnet` 进程占用输出；不得并发运行同一 `.csproj`。 |
| Browser full（最终候选） | NOT RUN | F03 54/54 不是完整 Browser suite。 |
| Profile / rollback / final evidence | NOT RUN（最终候选） | runner 已加 provenance 门禁；尚未在干净已提交的最终候选上完成。历史 rollback 是 FAIL，不是 NOT RUN。 |
| Release publish / 本机 no-Node / 独立 no-Node | NOT RUN | 未在本次候选执行。 |
| Remote CI / 真实 WebView2 DPI / Station、Camera、PLC、TCP、硬件与生产 soak | NOT RUN | 不以静态构建或浏览器测试替代。 |

## 主要变更

- Workspace capability flag 关闭时卸载为 `WorkspacePage` 的零 owner 禁用投影，而非路由 `403`。
- F03 lifecycle assertions、Operator 只读 pilot、静态 server 生命周期与 G15X owner guard 已补强。
- Profile、Rollback 与 Final evidence runner 现在拒绝脏工作树、顶层 `-NoBuild` 和 caller-supplied Desktop EXE，并使用 canonical fresh build。
- G0、G1、问题台账与本轮 G7 文档明确区分代码候选、历史证据、最终证据和未运行范围。

## F09-R 小修范围

1. 由产品和安全责任方决定 Operator 正式运行/连续检测的授权合同，或将该角色从 G6 接收条件中正式排除；不得由前端绕过现有授权。
2. 修复或取得最终候选的 Host close/flush rollback PASS。
3. 在干净提交上补跑串行 Desktop、Profile、Rollback、Final、完整 Browser、publish/no-Node/CI 与真实宿主证据；`F09-I004` 和 `F09-I010` 均需复核。

详细问题见 [F09_OPEN_ISSUES.md](./F09_OPEN_ISSUES.md)，默认切换与回退步骤见 [F09_Cutover与Rollback操作手册.md](./F09_Cutover与Rollback操作手册.md)。
