# F06 G3-R 中断恢复检查点

## 恢复基线

- Initial HEAD：`6e0e0cf7c32bda2a18a54e21c5d1d7b13c64df7c`。
- 已有备份 patch：`C:\Users\HerverJun\Desktop\ClearVision-G3-R-Recovery\20260730-101306\tracked-increment.binary.patch`，SHA-256 `39673b7a4dcf4bfdee2deea481680a19a8ac5f7176fe21a1bcbd8388a28c7f1d`。
- 孤立 `.git/index.lock` 已在恢复前安全删除；本轮开始和提交前均确认不存在。
- 正式配置裁决：`FORMAL_STUDIO_UI_DEFAULT=false`、`LOCAL_STUDIO_UI_OVERRIDE=true`、`LOCAL_OVERRIDE_IS_PROTECTED=YES`、`DEFAULT_ENTRY_CHANGE=BLOCKED`。

## 已修复合同

1. All/Any 参数分组继续保留 canonical 条件语义，只统计 active 且未禁用的参数。
2. Camera 资源继续由后端 canonical resource authority 判定，前端不自行宣布资源已满足。
3. `resourceRevision` 可从公开 Session Snapshot 恢复；资源 mutation 使用 canonical revision，stale revision 返回最新投影并阻止旧写入。
4. AgentRun replay 恢复公开 Build snapshot 与 diagnostics，保持 redaction、sequence 和 terminal projection 语义。

上述修复没有增加 Handoff、Workspace consume、Canvas 写入或 Project Save；`FlowCanvas`、`ImageCanvas`、`ProjectSaveCoordinator` 和既有保存链未进入 AI capability。

## 隔离同步

- detached worktree：`C:\Users\HerverJun\Desktop\ClearVision-G3-Recovery-Verify`，基于 Initial HEAD。
- 正式 `appsettings.json` 来自 HEAD，`Studio:StudioUiEnabled=false`，且隔离工作树对该文件无 diff。
- 同步范围：39 个 tracked G3 产品源码、后端合同/实现、测试与 E2E fixture/support 文件；9 个 untracked G3 源码或测试文件；合计 48 个文件，逐文件内容与原工作树一致。
- 10:13 的既有备份包含 8 个 untracked 文件；之后新增的第 9 个是 `AiResourceDecisionPanel.spec.ts`，属于 G3 资源决策组件测试，已纳入隔离验证与 checkpoint。
- 明确排除：原工作树 `appsettings.json`、8 个 `packages.lock.json`、`playwright-report/`、`.tmp/`、测试结果、截图、数据库和其他本地产物。
- 同步后及门禁结束后 `git diff --check` 均通过；隔离工作树 lockfile diff 为 0。

## 验证结果

原工作树既有最终定向证据：`AgentRunEndpointsTests` 63/63 PASS，后端修复定向 2/2 PASS。

| 门禁 | 隔离结果 |
|---|---|
| StudioUI lint | PASS，0 warning |
| StudioUI typecheck | PASS |
| G3 定向 Vitest + 4 个 formal-default guard 文件 | PASS，11 files / 59 tests |
| StudioUI full unit | PASS，96 files / 577 tests |
| StudioUI production build | PASS，377 modules |
| bundle verify | PASS，reproducibility PASS |
| bundle gate | PASS |
| Infrastructure build | PASS，0 warning / 0 error |
| Desktop build | PASS，0 warning / 0 error |
| AI / StudioUI architecture guards | PASS，16/16 |
| AgentRun endpoint suite | PASS，63/63 |
| Product AI directed suite | PASS，258/258 |
| Desktop endpoint regression | PASS，352/352 |
| Desktop full regression | PASS，682/682 |

首次 Desktop test project 构建因 worktree 长路径下的隔离 ONNX NuGet 缓存缺少两个 header 而在测试执行前停止。改用本轮专用短路径 `DOTNET_CLI_HOME` 后，同一代码和 guard 未作修改，architecture、AgentRun、endpoint 与 Desktop full 均一次通过；这不是 G3 产品失败。

最终 TRX 已确认 All/Any、Camera resource authority、`resourceRevision` stale 防护、replay snapshot/diagnostics 均通过。没有新的真实 G3 产品失败。

## Checkpoint

- Recovery checkpoint SHA：`526f10fa62f9c838552c8fda55fde6b3b3ab9532`。
- checkpoint 使用显式文件白名单，包含全部 7 个新增生产文件、2 个新增测试文件及全部 tracked G3 增量。
- 受保护的本地 `StudioUiEnabled=true` override、8 个 lockfile、Playwright report 与临时产物不进入 checkpoint。
- 11 个受保护对象在提交前按既有 SHA-256 清单复核为 11/11 不变。

## 仍未完成

- 本轮未执行 G3 Playwright、真实 Desktop endpoint/WebView2、真实模型质量或现场设备验证。
- 本轮不生成 G3 正式完成报告，不声明 G3 完成；checkpoint 仍需评审后才可继续 G3 主体开发。
- G4 Handoff artifact、Workspace consume、Canvas 写入、Project Save 接入、默认入口切换与 Legacy AI 退役均未获授权。

```text
F06_G2_STATE=DONE
F06_G3_STATE=RECOVERED_CHECKPOINT
F06_G3_COMPLETION=NOT_CLAIMED
F06_G3_CONTINUATION=AWAITING_REVIEW
F06_G4_IMPLEMENTATION=FORBIDDEN
F06_HANDOFF_IMPLEMENTATION=FORBIDDEN
DEFAULT_ENTRY_CHANGE=BLOCKED
LEGACY_AI_RETIREMENT=NOT_APPROVED
```
