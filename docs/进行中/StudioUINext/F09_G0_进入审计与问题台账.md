# F09 G0 进入审计与问题台账

```text
F09_G0_STATE=DONE_WITH_OPEN_ISSUES
EXECUTION_MODE=OVERNIGHT_CONTINUOUS
BASELINE_SHA=246dbe581
BRANCH=studio-ui-next
TRACKING_REF=origin/studio-ui-next
DEFAULT_ENTRY=NEXT_DEFAULT_CANDIDATE
LEGACY_ROLE=CONFIGURABLE_FALLBACK
NEXT_DEFAULT_SWITCH=NOT_ALLOWED
```

审计时间：2026-08-04。本文以当前代码和配置为准；F08 的报告与历史测试结果仅作为线索，不作为 F09 完成证据。

## 基线结论

- `appsettings.json` 当前将 `Studio:StartupProfile` 设为 `NEXT_DEFAULT_CANDIDATE`，同时保留 `LEGACY_FALLBACK`。这不是 `NEXT_DEFAULT` 的正式切换；本轮不改变该配置。
- 启动 Profile 由 `StudioStartupProfileCatalog` 投影到入口、功能开关和允许角色；`WebView2Host` 将已解析的 Profile 与只读角色数组注入 Next。后端端点权限仍由 `ClearVisionPermissionPolicies` 决定。
- 当前 Workspace 生命周期基线是 54 个 F03 Browser 用例；源代码中存在 20 次 mount/unmount 的 owner/resource ledger 断言。历史计划中的“53/54”不是当前用例计数。外部管理静态服务已得到 54/54 行为 PASS；受管 Playwright `webServer` 的退出/端口释放仍需针对最终提交复证。
- 工作树包含 F09 的 Profile、导航、证据脚本和测试改动。审计期间没有重置、清理或覆盖这些改动。
- Legacy 示例工程创建接口已被补上 `CanEditProject` 后端权限；示例工程自身仍是 Legacy fallback 能力，不把它误报为 Next 生命周期迁移完成。

## 当前证据

| 范围 | 当前证据 | 结论 |
| --- | --- | --- |
| Profile/入口/角色 | `WebView2HostTests`，2026-08-04 定向运行 60/60 PASS | 代码级 PASS |
| 示例工程写权限 | `DemoEndpointsTests`，2026-08-04 与 Profile 测试合并运行，总计 64/64 PASS | Operator 被拒绝；Admin/Engineer 可写 |
| StudioUI 启动协议、路由、导航 | 10 个定向 Vitest 文件，2026-08-04 运行 66/66 PASS | 代码级 PASS |
| F03 生命周期 | 外部管理静态服务的当前代码已得 54/54 PASS；受管 launcher 的 clean exit 尚未在最终提交复证 | 行为 PASS；G5 清理证据待补 |
| Operator Profile | 真实 WebView2 只读 authority 场景已得 PASS：受认证读取与 403 拒绝均由服务端验证 | 仅 G3 只读证据；不等同于 G6 运行/连续检测 Pilot |
| 回滚 | 历史 `ba2389d` manifest 为 FAIL：Desktop close/flush 超过 15 秒 | 必须在最终候选提交复跑，不可写为未运行 |
| WebView2、Release、no-Node、现场设备、Remote CI | 无最终 F09 候选 SHA 的完整完成证据 | 不能写 PASS |

## 约束复核

- Project、Flow、GlobalVariables、正式 assets、Results、Runtime Package 和 Station 仍以既有后端 authority 为准。
- 不新增 Project save endpoint、HTTP client、EventBus、ServiceRegistry、Canvas 内核或 HostBridge。
- `NEXT_DEFAULT_CANDIDATE` 与 `LEGACY_FALLBACK` 是进程启动时的配置选择；切换不能依赖 CSS 隐藏。证据脚本必须在提交后以新鲜 build 运行，不能仅用 `git rev-parse HEAD` 标记脏工作区或陈旧 EXE。
- F09 不删除 Legacy，也不将真实 WebView2/DPI、独立 no-Node、现场硬件或 Remote CI 伪装为已完成。

问题明细见 [F09_OPEN_ISSUES.md](./F09_OPEN_ISSUES.md)。Legacy/Next 能力结论见 [F09_G1_LegacyNext终局能力矩阵.md](./F09_G1_LegacyNext终局能力矩阵.md)。
