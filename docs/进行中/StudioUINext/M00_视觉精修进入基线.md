# M00 视觉精修进入基线

## M 系列接管校正（2026-08-06）

`M00` 已完成 dirty worktree 接管、分类提交和产品代码范围冻结。最终计划记录的远端 `31026167704=PASS`
仍只作为计划来源事实；当前基线的实现、本地门禁和 Browser 验证已通过，但真实 WebView2 100% 证据采集自
先前 dirty candidate，且 Windows 125% 和产品签收仍缺失，因此不能宣称 M00 完成。

```text
M00_STATE=PARTIAL_ACCEPTANCE_OPEN
M00_AUDIT_SOURCE_SHA=9800d6045a9f5fdfc62a166242e83529b833dc7d
M00_BASELINE_SHA=f8f581932469f7c52fe547b7bcabe8ad45d89532
M00_WORKTREE_STATE=CLEAN_AFTER_DOCUMENTATION_COMMIT
M00_SCOPED_PRODUCT_COMMITS=6
M00_BROWSER_BASELINE=PASS_CURRENT_BASELINE
M00_REAL_WEBVIEW2_100=PASS_DIRTY_CANDIDATE_NOT_FINAL_SHA
M00_REAL_WEBVIEW2_125=BLOCKED
CURRENT_BASELINE_SOURCE_GATES=PASS
M00_VISUAL_MATRIX_APPROVED=NOT_YET
PRODUCTION_ACCEPTANCE=NOT_GRANTED
LEGACY_RETIREMENT=NOT_APPROVED
```

## 2026-08-07 当前接管结果

- 初始 `65 tracked + 54 untracked = 119` 个 dirty 条目已完整分类；39 个 handoff/HTML 生成条目无损归档到
  ignored `.tmp`，8 个性能报告、2 个 lockfile 顺序噪声和 1 个无内容差异状态已恢复为 HEAD。
- 69 个有意源码、测试和文档改动按 owner/验证层收敛；产品实现与证据工具形成 6 个 scoped commits，
  `M00_BASELINE_SHA=f8f581932469f7c52fe547b7bcabe8ad45d89532`。逐组归类见
  [M00_文件Owner与验证矩阵.md](./M00_文件Owner与验证矩阵.md)。
- 提交前、与产品基线内容等价的候选 Chromium 全量回归为
  `146 passed / 26 explicit skipped / 0 failed`；StudioUI unit 为 `129/129 files`、`800/800 tests`，
  基础设施 Node tests 为 `3/3`。这些命令未在 commit SHA 上重跑。
- 已取得真实 Debug WebView2、Windows 原生 100%（96 DPI）证据和 Release publish runtime 证据，均来自 dirty
  candidate，不能作为 scope-clean final SHA。Windows 125% 仍 `BLOCKED`。
- 当前基线 Remote CI、产品负责人视觉签收、独立无 Node 目标机、现场设备和生产验收均未取得。

历史 F09 的 `30966530885=FAIL` 仍然保留，并明确属于历史审计事实；后续 `31026167704=PASS` 只表示计划来源中的 Remote CI 事实，不代表当前 `M00_BASELINE_SHA` 已重新执行。未重新执行的 Remote CI、WebView2、DPI、no-Node、现场硬件和用户签收均按 `NOT RUN`/`BLOCKED` 记录。

```text
HISTORICAL_M00_STATE=BLOCKED_BY_REMOTE_CI
HISTORICAL_F09_R_STATE=PARTIAL
HISTORICAL_F09_STATE=PARTIAL
FRONTEND_MIGRATION_MAINLINE=COMPLETE
HISTORICAL_F_PLAN_SERIES=CLOSURE_PENDING_REMOTE_CI
HISTORICAL_CONFIGURED_PROFILE=NEXT_DEFAULT
HISTORICAL_EFFECTIVE_DEFAULT_UI_ROOT=STUDIO_UI_NEXT
HISTORICAL_F09_R2_PRODUCT_SOURCE_SHA=d1c82ba88e351a2d48bcfae7f97e047483dbba98
HISTORICAL_PRODUCT_SOURCE_FOLLOW_UP_SHAS=c83dcc114290cf73e5e8d9b91e7b49732db8ec68,1545bca25
HISTORICAL_EVIDENCE_RUN_SHA=9dd69bd2bde44e8ea5b7285bfd18f47e02f95007
HISTORICAL_AUDIT_BRANCH=audit/f09-r2-d1c82ba88
HISTORICAL_OFFICIAL_REMOTE_SHA=7d43af9e19ad5a98240651fd5519a8e0f5a1e9f5
HISTORICAL_ROLLBACK_DRILL=PASS
HISTORICAL_REMOTE_CI_RUN=30966530885
HISTORICAL_REMOTE_CI=FAIL
HISTORICAL_FINAL_GATE=FAIL
HISTORICAL_M_SERIES_ENTRY=BLOCKED_BY_REMOTE_CI
HISTORICAL_PRODUCTION_ACCEPTANCE=NOT_GRANTED
```

F09-R2 已完成 Operator 只读产品决策、Next 默认入口配置和 Desktop shutdown diagnostics 工程修复；当前 SHA 的本地 Profile、Rollback、Final、Browser、Product、Desktop 和 Release 125% 证据均已通过。但 Remote CI run `30966530885` 在已提交 measurement report 的 `ColorMeasurement=FAIL` 处失败，Final Gate 也失败。M 系列入口保持阻塞，后续视觉工作不能把远端失败、独立 no-Node、完整 DPI、现场硬件或生产 soak 等 acceptance debt 改写为 PASS。

M 系列必须继续遵守：

- 现有 Studio 后端 authority、唯一 owner、ProjectSaveCoordinator、HTTP/SSE 和 Canvas/HostBridge 边界；不创建第二套基础设施。
- Operator 只读产品投影，以及 `CanEditProject`、`CanOperateHardware` 和后端 `403` 边界。
- `NEXT_DEFAULT` 为当前配置 Profile，`LEGACY_FALLBACK` 为可用的配置级回退入口；不删除 Legacy，不引入双写 owner。
- 1920x1080、Windows 125%、简体中文高信息密度和 Quiet Precision 作为视觉验收基线。
- 每项视觉调整都在真实浏览器或 WebView2 中复核，但不能用视觉证据替代功能、权限、保存、回退或发布证据。

剩余 acceptance debt 由 [F09_FinalEvidenceManifest.md](./F09_FinalEvidenceManifest.md) 和 [F09_OPEN_ISSUES.md](./F09_OPEN_ISSUES.md) 维护。M00 可继续进行范围内的前端实现、证据整理和本地审计；M01 及后续阶段仍须满足各自入口门禁，不能把历史 Remote CI 事实、Browser 证据、DPI 或现场验收互相替代，也不能扩展新的业务能力。
