# M00 视觉精修进入基线

```text
M00_STATE=DEFERRED
F09_STATE=PARTIAL
DEFAULT_ENTRY=NEXT_DEFAULT_CANDIDATE
```

M 系列只能承接视觉层级、布局、字体、间距、图标、动效、质感和视觉验收。它不能吸收或掩盖 F09 的权限、保存、运行、身份、生命周期、Profile、默认入口、Legacy fallback、数据库、发布或 CI 问题。

当前不启动 M00 实施，原因是 `F09-I001` 与 `F09-I002` 仍是 P1，正式默认入口未启用。`F09-I004`、`F09-I005`、`F09-I006`、`F09-I010` 等 P2 也必须继续作为独立证据或 Legacy 事项维护。

当 F09-R 关闭 P1 并重新完成默认入口门禁后，M00 的视觉工作仍需遵守：

- 以现有 Studio 后端 authority、唯一 owner、ProjectSaveCoordinator、HTTP/SSE 和 Canvas/HostBridge 边界为前提；不创建第二套基础设施。
- 以 1920x1080、Windows 125%、中文高信息密度和 Quiet Precision 为验收基线。
- 每项视觉调整必须在真实浏览器或 WebView2 中复核，不能替代功能、权限或发布证据。

因此 M00 不是 F09 完成的替代结论，也不创建 F10 长期迁移计划。
