# M00 视觉精修进入基线

```text
M00_STATE=BLOCKED_BY_ROLLBACK
F09_R_STATE=IMPLEMENTED_AWAITING_ROLLBACK
F09_STATE=PARTIAL
FRONTEND_MIGRATION_MAINLINE=NEAR_COMPLETE
F_PLAN_SERIES=CLOSURE_PENDING_ROLLBACK
CONFIGURED_PROFILE=NEXT_DEFAULT
EFFECTIVE_DEFAULT_UI_ROOT=STUDIO_UI_NEXT
F09_R2_SOURCE_SHA=d1c82ba88e351a2d48bcfae7f97e047483dbba98
AUDIT_BRANCH=audit/f09-r2-d1c82ba88
OFFICIAL_REMOTE_SHA=7d43af9e19ad5a98240651fd5519a8e0f5a1e9f5
ROLLBACK_DRILL=NOT_RUN
M_SERIES_ENTRY=BLOCKED_BY_ROLLBACK
PRODUCTION_ACCEPTANCE=NOT_GRANTED
```

F09-R2 已完成 Operator 只读产品决策、Next 默认入口配置和 Desktop shutdown diagnostics 工程修复的代码实现。由于真实 rollback 尚未执行，M 系列入口保持阻塞；后续视觉工作不能把未运行的 rollback、真实 WebView2、DPI、publish、Remote CI 或现场证据改写为 PASS。

M 系列必须继续遵守：

- 现有 Studio 后端 authority、唯一 owner、ProjectSaveCoordinator、HTTP/SSE 和 Canvas/HostBridge 边界；不创建第二套基础设施。
- Operator 只读产品投影，以及 `CanEditProject`、`CanOperateHardware` 和后端 `403` 边界。
- `NEXT_DEFAULT` 为当前配置 Profile，`LEGACY_FALLBACK` 为可用的配置级回退入口；不删除 Legacy，不引入双写 owner。
- 1920x1080、Windows 125%、简体中文高信息密度和 Quiet Precision 作为视觉验收基线。
- 每项视觉调整都在真实浏览器或 WebView2 中复核，但不能用视觉证据替代功能、权限、保存、回退或发布证据。

剩余 acceptance debt 由 [F09_FinalEvidenceManifest.md](./F09_FinalEvidenceManifest.md) 和 [F09_OPEN_ISSUES.md](./F09_OPEN_ISSUES.md) 维护。M00 不创建 F10，也不扩展新的业务能力。
