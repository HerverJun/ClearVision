# Studio UI Next F04-R 完成报告

> 报告日期：2026-07-25
> Initial SHA：`f0ef6a06cdf50f915f9959793b34e9ceae5140cc`
> 修复 SHA：`25f811c3cce697d4542fd59d4667804901f0e356`
> CI hygiene SHA：`5f3cf2ae2d21022c3feb2142bd7bd6cbcf58ab5c`
> Source secret scan 修复 SHA / Remote code SHA：`70a0ab7018270d0c467d556f263dff6b812cf48f`
> Final Evidence SHA：本报告、证据索引、README 与主计划状态的独立提交；提交 SHA 与后置 Remote CI 由最终交付回报记录
> Remote CI：workflow_dispatch [`30143297252`](https://github.com/HerverJun/ClearVision/actions/runs/30143297252)，`PASS`

## 1. 结论

G4B 已完成真实 WebView2 Debug、真实 WebView2 Release、真实 Windows 125% 系统缩放、Release publish 与发布产物启动、本地回归、Remote CI、Final Gate 和证据收口。产品负责人已在 G4A.2 后确认整体方向并批准进入 G4B；本轮没有扩功能、没有修改正式默认 flags、没有退役 Legacy、没有启动 F05。

```text
G4B_STATUS=DONE
PRODUCT_VISUAL_CONFIRMATION=PASS
REAL_WEBVIEW2_DEBUG=PASS
REAL_WEBVIEW2_RELEASE=PASS
WINDOWS_125_DPI=PASS
RELEASE_PUBLISH=PASS
REMOTE_CI=PASS
FINAL_GATE=PASS

F04R_STATUS=COMPLETE
DEFAULT_ENTRY_CHANGE=NOT_AUTOMATIC
LEGACY_RETIREMENT=NOT_APPROVED
F05_ENTRY=READY_FOR_PLANNING
F05_STARTED=NO
```

## 2. G4B 暴露并修复的真实阻断

1. WebView2 生命周期离开目标仍使用已移除的 `/about`；已改为当前 Pilot 的 `/projects`。
2. Formal Run 成功后，runner 未按当前产品语义显式点击“查看本次结果”；已补齐。
3. F04 导航合同仍要求旧的 Overview / Operators / Diagnostics / About；已更新为 Pilot 的“工程 / 检测结果”，Stations 即使 flag 开启也不在本轮产品导航暴露。
4. runner 请求白名单缺少 FinalDecision、Camera、Recent Projects、Evidence 与 Runtime Package；已补齐。
5. 真实 ImageAcquisition 使用 `CameraId`，Camera Binding owner 只读取 `CameraBindingId`；现由同一 owner 兼容两个参数名，未新增第二 binding owner 或保存链。
6. 新增 G4B 黄金旅程宿主证据场景与截图，覆盖 Camera Binding、GlobalVariables、FinalDecision、Preview/ROI、Save、Run/Stop/Reconcile、Result/Evidence、Runtime Package。

## 3. 架构与产品边界

- Project、Flow、GlobalVariables 与正式 assets 仍进入现有 Application Service / `ProjectSaveCoordinator`。
- Formal Run、Stop、Reconcile、Results、Evidence 与 Runtime Package 仍使用既有 HTTP/后端权威。
- Camera 参数兼容发生在现有 Camera Binding editor owner 内，没有新增第二 owner、API 或写入口。
- HostBridge 只用于宿主能力投影，没有新增执行通道。
- `Studio:StudioUiEnabled=false` 的正式默认值未改变；用户本地 `appsettings.json` 保留且未提交。

## 4. 验证摘要

- Browser / Playwright：`89 passed / 21 skipped / 0 failed`。
- WebView2 Debug：1920×1080、1366×768，100% 与真实 125% 均 PASS。
- WebView2 Release：1920×1080、1366×768，100% 与真实 125% 均 PASS。
- Windows 125%：系统档位真实切换，native DPI `120`、scale `1.25`、JS DPR `1.25`；结束后恢复 100%。
- Release publish：self-contained `win-x64` PASS，发布目录黄金旅程双分辨率 PASS，静态资源/no-Node 本机审计 PASS。
- typecheck、lint、build：PASS。
- Vitest：全量 `495/499`；4 个已知失败只来自用户本地默认值。排除该配置断言后 `495/495 PASS`。
- Desktop endpoints：`335/335 PASS`。
- Desktop architecture：`8/9`；排除同一用户配置断言后 `8/8 PASS`。
- single-owner / import / lifecycle：PASS，含 20-cycle 与最终零资源 ledger。
- Remote CI：Run `30143297252` / SHA `70a0ab7018270d0c467d556f263dff6b812cf48f` 全部实际门禁 job、Product Tests、Coverage Summary 与 Final Gate 均 PASS。

完整证据见 [G4B WebView2 / DPI / Release 证据索引](./F04R_G4B_WebView2_DPI_Release证据索引.md)。

## 5. 已知非阻断项

- 主 JS chunk `963.63 kB` 警告继续记录；未造成 Release 或宿主性能阻断，本轮不做大规模拆包。
- 真实相机、PLC、Station 与独立无 Node 目标机均未执行，不改写为 PASS。
- Browser fixture、Playwright、WebView2、DPI 和 publish 证据已分开记录。

```text
REAL_CAMERA=NOT_PERFORMED
REAL_PLC=NOT_PERFORMED
REAL_STATION=NOT_PERFORMED
INDEPENDENT_NO_NODE_TARGET=NOT_PERFORMED
```

## 6. 最终状态

```text
G4B_STATUS=DONE
PRODUCT_VISUAL_CONFIRMATION=PASS
REAL_WEBVIEW2_DEBUG=PASS
REAL_WEBVIEW2_RELEASE=PASS
WINDOWS_125_DPI=PASS
RELEASE_PUBLISH=PASS
REMOTE_CI=PASS
FINAL_GATE=PASS

F04R_STATUS=COMPLETE
DEFAULT_ENTRY_CHANGE=NOT_AUTOMATIC
LEGACY_RETIREMENT=NOT_APPROVED
F05_ENTRY=READY_FOR_PLANNING
F05_STARTED=NO
```
