# G10C：PixelToWorld 与 Scene 空间投影

> 阶段：Geometry/Spatial
> 状态：`READY`（G08-G10B-FOLLOWUP-3 已完成；本卡尚未执行）
> 目标：把 ROI/Full 坐标链与现有 CalibrationBundleV2、PixelToWorld 实现接通。

> 2026-07-03 备注：`G08-G10B-FOLLOWUP-3` 未执行 G10C 范围；Follow-up-3 完成后本卡恢复为下一项 READY。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/PixelToWorldTransformOperator.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Calibration/CalibrationBundleV2.cs`
- `ClearVision.Product/tests/ClearVision.Product.Tests/Operators/PixelToWorldTransformOperatorTests.cs`

## 本轮范围

[ ] ImageFull/Undistorted/World2D transform chain。
[ ] Scene frame-aware projection。
[ ] 诊断显示 source frame、target frame、unit、bundle ID。

## 明确不做

- 新增平行 CalibrationArtifact
- 在前端重复计算权威世界坐标
- Station/package 改造

## 执行清单

[ ] 复用 CalibrationBundleV2 runtime。
[ ] frame 不匹配 fail closed。
[ ] 结果中保留 traceability。
[ ] 验证 ROI local 点经 Full Image 到 World2D。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] 运行现有 calibration/phase42 regression 和新增链路测试。

## 完成条件

[ ] 坐标链可解释、可复现。
[ ] 旧 PixelToWorld 输入继续工作。

## 回填区

- 状态：`READY`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G11A`
