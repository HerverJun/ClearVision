# G13X：VM 式算子模块结果面板 MVP

> 阶段：Vertical Product
> 状态：`DONE`（受控插入 Goal，位于 G13B 与 G13C 之间）
> 目标：在保留 ComfyUI 式 FlowCanvas 编排体验的前提下，基于现有 preview observation / artifact / scene / resultPath 能力，提供类似 VM 的当前节点模块结果查看面板。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. G05A/G05B/G06/G07A/G08/G11B-FOLLOWUP/G12B 相关执行卡；
5. 现有 Preview Coordinator、Node Preview Overlay、Property Panel、Property Sidebar Controller；
6. 现有 Preview Artifact / Preview Node endpoint、Scene / ImageCanvas 投影代码、相关 UI tests / architecture guard tests。

## 本轮范围

[x] 当前选中节点的模块结果面板。
[x] 未选择、未运行、running、失败、stale、disabled 等稳定状态。
[x] observation/result 输出通用查看：scalar/table/list/geometry/image-artifact/unknown JSON tree。
[x] 复用现有 ImageCanvas / Scene projector，不新增第二图像渲染内核。
[x] 基于现有 Preview Artifact 安全读取能力展示 artifact 摘要/安全预览。
[x] 诊断、warnings/errors、安全截断 raw JSON。
[x] 面板内轻量节点结果列表，点击同步当前节点。

## 明确不做

- 不重写 FlowCanvas。
- 不改变 ComfyUI 式拖拽、连线、节点编排体验。
- 不把复杂结果表格/中间图/历史回放塞进节点画布本体。
- 不新增第二 ImageCanvas 或第二 Scene projector。
- 不持久化 preview history。
- 不做正式 Inspection history / 历史对比 / Evidence ZIP。
- 不做 Runtime Package / Station / PixelToWorld E2E。
- 不改 AgentRun / EventStore / terminal recovery。
- 不改 Circle Search 内核或 G12A solver 数学。

## 执行清单

[x] 复用现有 Preview/Inspector/Property 右侧区域挂载模块结果面板。
[x] 定义仅供 UI 使用的 OperatorResultViewModel，不成为持久化权威。
[x] 未选择节点显示“请选择一个算子节点查看模块结果”。
[x] 选中节点但无 preview 显示“该算子暂无预览结果”。
[x] disabled 节点显示禁用说明。
[x] preview running 显示 loading，不闪烁旧结果。
[x] preview failed 显示稳定错误。
[x] 参数/flowRevision stale 显示“结果已过期，请重新预览”。
[x] 概览区展示 node/operator/status/timing/identity/diagnostics。
[x] 输出区安全展示 key-value/list/table/geometry/artifact/JSON tree。
[x] 图像叠加区只同步或说明现有 ImageCanvas/Scene 状态。
[x] artifact 缺失/过期 fail-soft。
[x] raw JSON 与 artifact preview 截断并脱敏。
[x] 节点结果列表只显示轻量状态并同步选择。

## 验证清单

[x] 执行 `git diff --check`。
[x] focused UI/unit tests 覆盖空状态、未运行、成功、失败、stale、disabled、artifact、scene、raw JSON、安全脱敏、切换/销毁 lifecycle。
[x] architecture guard 覆盖无第二 ImageCanvas、无 Runtime/Station/AgentRun 改动。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/`MSBuild`/Playwright Chromium。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[x] 所有节点至少有通用结果查看入口。
[x] FlowCanvas 仍是流程编排/选择/连线/轻量徽标 owner。
[x] Property Panel 仍是参数编辑 owner。
[x] ImageCanvas/Scene 投影未复制。
[x] Preview data 不持久化为 Inspection history。

## 回填区

- 状态：`DONE`
- 开始时间：`2026-07-04 13:40:08 +08:00`
- 完成时间：`2026-07-04 14:25:02 +08:00`
- Initial SHA：`fe0c23bf4d6bb8ba1dd88ba1b9b843321dba27cb`
- Final SHA：提交自身 SHA 不写入 tracked 文件；以 push 后核对值为准
- 远端 SHA：提交后以最终报告核对值为准
- 修改文件：
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/app.js`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/operatorResultViewModel.mjs`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/previewCoordinator.js`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/previewPanel.js`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/propertyPanel.js`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/shared/styles/sprint-c-enhancements.css`
  - `ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/Architecture/Studio2ArchitectureGuardTests.cs`
  - `ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/unit/preview-panel-memory.test.mjs`
  - `TODO.md`
  - `docs/进行中/Studio2/goals/G13X.md`
- 新增/变更契约：新增前端-only `OperatorResultViewModel` adapter；`previewCoordinator.js` additive 导出 `buildPreviewParameterSnapshot()`，复用既有 parameter snapshot/stale 判定逻辑；未新增 API、Project format、Runtime contract。
- active owner / 唯一写入口：`PreviewPanel` 继续挂载在 `PropertyPanel` 的 `#operator-preview-container`；结果面板只读消费 `previewCoordinator` state；Artifact 读取唯一入口仍为 `previewCoordinator.readArtifactForCurrentState()`；节点点击通过既有 `FlowCanvasAdapter.selectNode()` 同步选择。
- Legacy mounted / subscription / timer 状态：未改 FlowCanvas/NodePreviewOverlay/NodePreviewInspector owner；`PreviewPanel` 新增 preview subscription 与 flow structure subscription，`destroy()` 统一 unsubscribe/abort/clear；无新增持久化 timer，保留原 auto-preview debounce 行为。
- 测试命令与结果：
  - `node --test tests/unit/preview-panel-memory.test.mjs`：PASS（10/10）
  - `node --test tests/unit/property-panel-memory.test.mjs`：PASS（12/12）
  - `& "./scripts/run-dotnet-test-serial.ps1" -Project "ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/ClearVision.Product.Desktop.Tests.csproj" -FullyQualifiedName Studio2ArchitectureGuardTests`：PASS（17/17）
  - `npm run test:unit`：PASS（636/636）
  - `git diff --check`：PASS
- 截图/Benchmark/Artifact：截图 NOT RUN；Benchmark NOT RUN；新增正式 Artifact NONE。
- API / Project format / Runtime / Station / AgentRun 影响：API NONE；Project format NONE；Runtime NONE；Station NONE；AgentRun NONE。
- 技术债与非阻断事项：真实 WebView2 NOT PERFORMED；完整 GitHub CI NOT RUN。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G13C`
