# G09C：Polygon 与 PointSequence 编辑

> 阶段：Geometry/Spatial
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：完成 ROI 多边形与标定采点所需的通用交互。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/roiEditorPanel.js`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/RoiManagerOperator.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/NPointCalibrationOperator.cs`

## 本轮范围

[ ] Polygon 顶点增删拖动。
[ ] PointSequence 顺序、选择、启用状态的 UI draft。
[ ] 与现有 PolygonPoints/PointPairs adapter。

## 明确不做

- 把 CoordinateAxis 做成可编辑 geometry
- 标定求解
- 正式资产保存

## 执行清单

[ ] 非法自交/越界策略明确。
[ ] 旧字符串参数可解析并 round-trip。
[ ] Scene 中 CoordinateAxis 保持只读 primitive。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[ ] Polygon ROI 可编辑。
[ ] PointSequence 可供 G12 使用。
[ ] 旧参数可读写。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G10A`
