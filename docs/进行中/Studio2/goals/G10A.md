# G10A：Spatial Context 数学与 sidecar 契约

> 阶段：Geometry/Spatial
> 状态：`DONE`
> 目标：定义 frame/transform/unit 的可组合语义，先不修改 ImageWrapper 生命周期对象。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/ImageWrapper.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Calibration/CalibrationBundleV2.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Calibration/CalibrationPlanarTransformRuntime.cs`

## 本轮范围

[x] `SpatialContextV1`、FrameRef、2D transform composition/inverse。
[x] 旧无元数据图像默认 ImageFull。
[x] sidecar 绑定 output/artifact identity。

## 明确不做

- 直接给 ImageWrapper 加可变业务元数据
- 持久化 Project asset
- 修改 ROI 算法

## 执行清单

[x] 先写 ADR 比较 sidecar 与 ImageWrapper 扩展；默认采用 sidecar。
[x] 支持 ImageFull、RoiLocal、Undistorted、World2D。
[x] 明确 px/mm 和矩阵方向。
[x] 多级 transform compose。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[x] 纯数学 round-trip、inverse、单位和非法矩阵测试。

## 完成条件

[x] 空间数学与图像内存生命周期解耦。
[x] 旧流程默认语义明确。

## 回填区

- 状态：`DONE`
- 开始时间：2026-07-03 01:59:47 +08:00
- 完成时间：2026-07-03 02:05:04 +08:00
- Initial SHA：`55b14652121d4e7b11fd80686b1f8ca996d2b01d`
- Final SHA：verify after commit
- 远端 SHA：verify after push
- 修改文件：
  - `ClearVision.Product/src/ClearVision.Product.Infrastructure/Calibration/SpatialContextV1.cs`
  - `ClearVision.Product/tests/ClearVision.Product.Tests/Calibration/SpatialContextV1Tests.cs`
  - `docs/进行中/Studio2/architecture/G10A-SpatialContext-sidecar-ADR.md`
  - `docs/进行中/Studio2/goals/G10A.md`
  - `TODO.md`
- 新增/变更契约：新增 `SpatialContextV1` sidecar、`FrameRefV1`、`SpatialTransform2DV1` 和 `SpatialContextBindingV1`；方向为 `targetPoint = T(sourcePoint)`；legacy image 默认 `ImageFull / px`；sidecar 可绑定 flow output 与 preview artifact identity。
- active owner / 唯一写入口：无新增执行 owner；sidecar 只描述空间上下文，不拥有图像 bytes/Mat、Project 保存或 Artifact 存储。
- Legacy mounted / subscription / timer 状态：无前端挂载、subscription、timer 或 WebView2 生命周期变更。
- 测试命令与结果：
  - `& "./scripts/run-dotnet-test-serial.ps1" -Project "ClearVision.Product/tests/ClearVision.Product.Tests/ClearVision.Product.Tests.csproj" -FullyQualifiedName SpatialContextV1Tests -Verbosity minimal` - PASS, 10 tests.
  - `& "./scripts/run-dotnet-test-serial.ps1" -Project "ClearVision.Product/tests/ClearVision.Product.Tests/ClearVision.Product.Tests.csproj" -FullyQualifiedName SpatialContextV1Tests,PixelToWorldTransformOperatorTests,CalibrationV2IntegrationTests,LegacyCalibrationContractAuditTests -NoBuild -NoRestore -Verbosity minimal` - PASS, 30 tests.
  - `git diff --check` - PASS.
  - Process audit - PASS after build-server shutdown and compiler-server cleanup; no residual `dotnet`, `testhost`, or `node` process.
- 截图/Benchmark/Artifact：NOT RUN（本轮为 Product-side contract/math；无 UI）。
- API / Project format / Runtime / Station / AgentRun 影响：无 API、Project format、Runtime、Station 或 AgentRun 变更；未给 `ImageWrapper` 添加可变空间元数据。
- 技术债与非阻断事项：G10B 将把 sidecar 传播接入 RoiManager crop；G10C remains not executed.
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G10B`
