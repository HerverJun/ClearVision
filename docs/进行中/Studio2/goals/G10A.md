# G10A：Spatial Context 数学与 sidecar 契约

> 阶段：Geometry/Spatial
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：定义 frame/transform/unit 的可组合语义，先不修改 ImageWrapper 生命周期对象。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/ImageWrapper.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Calibration/CalibrationBundleV2.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Calibration/CalibrationPlanarTransformRuntime.cs`

## 本轮范围

[ ] `SpatialContextV1`、FrameRef、2D transform composition/inverse。
[ ] 旧无元数据图像默认 ImageFull。
[ ] sidecar 绑定 output/artifact identity。

## 明确不做

- 直接给 ImageWrapper 加可变业务元数据
- 持久化 Project asset
- 修改 ROI 算法

## 执行清单

[ ] 先写 ADR 比较 sidecar 与 ImageWrapper 扩展；默认采用 sidecar。
[ ] 支持 ImageFull、RoiLocal、Undistorted、World2D。
[ ] 明确 px/mm 和矩阵方向。
[ ] 多级 transform compose。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] 纯数学 round-trip、inverse、单位和非法矩阵测试。

## 完成条件

[ ] 空间数学与图像内存生命周期解耦。
[ ] 旧流程默认语义明确。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G10B`
