# G07B：字段级全局变量绑定 V1

> 阶段：Observation
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：在现有 SourceBinding 上 additive 增加 resultPath，保持旧端口根值语义。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Core/ProjectVariables/ProjectGlobalVariableContracts.cs`
- `ClearVision.Product/tests/ClearVision.Product.Tests/ProjectVariables/FlowExecutionProjectVariableBindingTests.cs`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/global-variables/globalVariableStore.js`

## 本轮范围

[ ] optional `ResultPath` + `ResultPathVersion`。
[ ] schema 兼容升级。
[ ] Inspector 仅对允许的稳定 scalar 叶子提供绑定入口。

## 明确不做

- 改变旧 binding 语义
- 支持图像/矩阵/动态索引
- 绕过变量工作副本事务

## 执行清单

[ ] `resultPath` 为空时读取 output port 根值。
[ ] 保留 `Expression` 的既有转换语义。
[ ] 失败/取消/Preview/DryRun 不污染正式 Session。
[ ] 旧 schema 无迁移即可读取。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] 扩展现有同轮写入、preview clone、失败回滚、重启持久化测试。

## 完成条件

[ ] 旧绑定不回退。
[ ] 新路径 preview/正式一致。
[ ] 无效路径不静默使用默认值。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G08`
