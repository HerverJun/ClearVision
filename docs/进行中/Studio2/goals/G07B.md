# G07B：字段级全局变量绑定 V1

> 阶段：Observation
> 状态：`DONE`
> 目标：在现有 SourceBinding 上 additive 增加 resultPath，保持旧端口根值语义。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Core/ProjectVariables/ProjectGlobalVariableContracts.cs`
- `ClearVision.Product/tests/ClearVision.Product.Tests/ProjectVariables/FlowExecutionProjectVariableBindingTests.cs`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/global-variables/globalVariableStore.js`

## 本轮范围

[x] optional `ResultPath` + `ResultPathVersion`。
[x] schema 兼容升级。
[x] Inspector 仅对允许的稳定 scalar 叶子提供绑定入口。

## 明确不做

- 改变旧 binding 语义
- 支持图像/矩阵/动态索引
- 绕过变量工作副本事务

## 执行清单

[x] `resultPath` 为空时读取 output port 根值。
[x] 保留 `Expression` 的既有转换语义。
[x] 失败/取消/Preview/DryRun 不污染正式 Session。
[x] 旧 schema 无迁移即可读取。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[x] 扩展现有同轮写入、preview clone、失败回滚、重启持久化测试。

## 完成条件

[x] 旧绑定不回退。
[x] 新路径 preview/正式一致。
[x] 无效路径不静默使用默认值。

## 回填区

- 状态：`DONE`
- 开始时间：2026-07-02 21:35:51 +08:00
- 完成时间：2026-07-02 22:24:00 +08:00
- Initial SHA：`295363ecfad05711ecb7d214513a6aa090fd64ed`
- Final SHA：见 G07B 完成提交与最终报告
- 远端 SHA：见 G07B 完成提交与最终报告
- 修改文件：`ExecutionObservationEnvelopeV1.cs`、`ExecutionObservationProjector.cs`、`wwwroot/src/app.js`、`nodePreviewInspector.js`、`nodePreviewSelectionStore.js`、`globalVariablePanel.js`、`globalVariableStore.js`、Desktop architecture/observation tests、UI unit tests、Playwright `node-preview.spec.ts`、`TODO.md` 与本卡。
- 新增/变更契约：Observation Summary/Detail additive `BindableVariableTypes`；front-end SourceBinding round-trip preserves optional `resultPathVersion`/`resultPath` without migrating legacy bindings；Inspector emits canonical field-binding descriptors only from backend metadata；`GlobalVariablePanel.bindPreviewField(descriptor)` is the only UI mutation entry for field binding.
- active owner / 唯一写入口：Core ResultPath parser/formatter/resolver remains the only semantic authority；Inspector only receives DTO metadata and calls the injected callback；GlobalVariablePanel clones baseline schema and saves only through `ProjectManager.saveGlobalVariables` and the existing global-variables endpoint.
- Legacy mounted / subscription / timer 状态：不新增 mounted owner、subscription 或 timer；NodePreviewInspector 继续复用唯一 `NodePreviewCoordinator` 和 capability-local selectionStore；legacy root source picker continues creating root SourceBinding without ResultPath fields and clears ResultPath when switching back to root.
- 测试命令与结果：UI focused `node --test tests/unit/global-variable-panel.test.mjs tests/unit/node-preview-inspector.test.mjs` PASS（51 tests）；Desktop targeted `ExecutionObservationProjectorTests,Studio2ArchitectureGuardTests` PASS（32 tests）；Product targeted `FlowExecutionProjectVariableBindingTests,ProjectGlobalVariableSchemaTests,ResultPathV1Tests` PASS（95 tests）；Playwright field-binding focused PASS（1 test）；`npm run test:unit` PASS（580 tests）；`npm run test:preview-smoke` PASS；`npx playwright test tests/e2e/node-preview.spec.ts --reporter=list` PASS（10 tests）；`ClearVision.Product.Tests` full serial PASS（3128 passed, 4 skipped）；Desktop full serial PASS（453 tests）；Desktop Debug build PASS；Station Debug/Release build PASS；Desktop Release publish to `.tmp/publish-check/g07b` PASS；build/publish V2 asset audit PASS；publish no Node/source/dev artifacts PASS；`git diff --check` PASS；changed-file secret/large-file scans PASS；publish scratch cleaned.
- 截图/Benchmark/Artifact：未生成截图；未做 benchmark；publish scratch verified and cleaned.
- API / Project format / Runtime / Station / AgentRun 影响：Project SourceBinding schema only preserves and writes additive nullable ResultPath fields；no new endpoint, parser, Runtime Package, Station-specific path, AgentRun, EventStore, Workspace Snapshot, terminal/recovery change；Station/formal execution continues to reuse shared `FlowExecutionService`.
- 技术债与非阻断事项：完整 GitHub CI：NOT RUN；真实 WebView2：NOT PERFORMED；V1 只绑定已有变量，不自动创建变量，不提供 Expression editor；未执行 G08。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G08`
