# G13A：Project 正式资产权威与保存恢复

> 阶段：Vertical Product
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：扩展现有硬编码 ProjectSaveCoordinator，使 Calibration/Spatial 正式资产参与同一 revision/恢复事务。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Application/Services/ProjectSaveCoordinator.cs`
- `ClearVision.Product/src/ClearVision.Product.Application/Services/ProjectService.cs`
- `ClearVision.Product/src/ClearVision.Product.Application/DTOs/ProjectDto.cs`

## 本轮范围

[ ] 设计可扩展 participant，或显式增加 Calibration/Spatial participant。
[ ] staging、hash、commit intent、apply、recovery、rollback。
[ ] Application DTO/endpoint 保存和加载。

## 明确不做

- 单独先写资产文件再保存 Project
- 独立 asset revision/recovery
- Runtime package/Station

## 执行清单

[ ] 承认当前 coordinator 仅硬编码 Project/Flow/VariableState。
[ ] 为新 participant 定义 candidate、metadata、hash 和 failure points。
[ ] 候选 CalibrationBundleV2 经过统一保存后才成为 authority。
[ ] 保存失败/进程中断/重启恢复测试。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] 覆盖每个 failure injection point、stale revision、checksum mismatch、旧工程无资产。

## 完成条件

[ ] 正式资产与 Project revision 原子一致。
[ ] 旧工程可读。
[ ] 无第二保存框架。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G13B`
