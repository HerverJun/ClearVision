# G13A：Project 正式资产权威与保存恢复

> 阶段：Vertical Product
> 状态：`DONE`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：扩展现有硬编码 ProjectSaveCoordinator，使 Calibration/Spatial 正式资产参与同一 revision/恢复事务。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Application/Services/ProjectSaveCoordinator.cs`
- `ClearVision.Product/src/ClearVision.Product.Application/Services/ProjectService.cs`
- `ClearVision.Product/src/ClearVision.Product.Application/DTOs/ProjectDto.cs`

## 本轮范围

[x] 设计可扩展 participant，或显式增加 Calibration/Spatial participant。
[x] staging、hash、commit intent、apply、recovery、rollback。
[x] Application DTO/endpoint 保存和加载。

## 明确不做

- 单独先写资产文件再保存 Project
- 独立 asset revision/recovery
- Runtime package/Station

## 执行清单

[x] 承认当前 coordinator 仅硬编码 Project/Flow/VariableState。
[x] 为新 participant 定义 candidate、metadata、hash 和 failure points。
[x] 候选 CalibrationBundleV2 经过统一保存后才成为 authority。
[x] 保存失败/进程中断/重启恢复测试。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[x] 覆盖每个 failure injection point、stale revision、checksum mismatch、旧工程无资产。

## 完成条件

[x] 正式资产与 Project revision 原子一致。
[x] 旧工程可读。
[x] 无第二保存框架。

## 回填区

- 状态：`DONE`
- 开始时间：`2026-07-04 12:20:40 +08:00`
- 完成时间：`2026-07-04 12:25:57 +08:00`
- Initial SHA：`00797812407f949bb87fe1612272d40794c1634c`
- Final SHA：提交自身 SHA 不写入 tracked 文件；以 push 后核对值为准
- 远端 SHA：提交后以最终报告核对值为准
- 修改文件：Application `ProjectAssetsDto`/`IProjectAssetStorage`/`ProjectSaveCoordinator`/`ProjectService`/`ProjectDto`；Infrastructure `JsonFileProjectAssetStorage`/DI；Desktop `CalibrationDraftEndpoints`；legacy workbench Formal Save；Product/Desktop/UI tests；`TODO.md`/G12B/G13A/G13B docs。
- 新增/变更契约：`ProjectDto.assets`；`ProjectAssetsDto` with `calibrationAssets`/`spatialAssets`；`ProjectCalibrationAssetSaveRequest/Response`；`IProjectAssetStorage` metadata `{persistenceRevision, assetsHash, saveId}`；`POST /api/projects/{projectId}/calibration-assets/from-draft`。
- active owner / 唯一写入口：Formal asset authority only enters through `ProjectService.SaveCalibrationAssetAsync` -> `ProjectSaveCoordinator` `ProjectAssets` participant; frontend posts draft candidate to Application endpoint only and never writes asset authority directly.
- Legacy mounted / subscription / timer 状态：G12B workbench remains the same owner; Formal Save button uses existing workbench lifecycle, no new subscription/timer, and destroy behavior remains unchanged.
- 测试命令与结果：Product focused `ProjectSaveCoordinatorTests,ProjectServiceTests` PASS（39/39）；Desktop focused `CalibrationDraftEndpointsTests,Studio2ArchitectureGuardTests` PASS（19/19）；UI focused `property-panel-memory.test.mjs` PASS（12/12）；Product full serial PASS（3252 passed, 4 skipped）；Desktop full serial PASS（479/479）；UI unit full PASS（627/627）；Playwright `npoint-calibration-workbench.spec.ts` PASS（2/2）；`git diff --check` PASS。
- 截图/Benchmark/Artifact：Playwright reused existing focused workbench spec screenshots; no new benchmark artifact.
- API / Project format / Runtime / Station / AgentRun 影响：Project DTO adds optional `assets`; old missing assets deserialize/load as empty; Runtime Package unchanged; Station unchanged; PLC/机器人 unchanged; AgentRun unchanged.
- 技术债与非阻断事项：完整 GitHub CI NOT RUN；真实 WebView2 NOT PERFORMED；Runtime/Station consumption deferred to G13B/G13C。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G13B`
