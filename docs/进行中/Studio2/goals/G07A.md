# G07A：Canonical ResultPath 解析器

> 阶段：Observation
> 状态：`DONE`（G07A follow-up：ResultPath Preview/正式执行一致性与 fail-closed 边界）
> 目标：建立前后端共用的稳定字段路径语义，为变量绑定和 Scene 双向定位提供唯一规则。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Core/ProjectVariables/ProjectGlobalVariableContracts.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Services/FlowExecutionService.cs`
- `ClearVision.Product/src/ClearVision.Product.Desktop/Endpoints/PreviewNodeEndpoints.cs`

## 本轮范围

[x] 路径相对于已选择的 output port 根值。
[x] 定义转义、对象键、稳定 ID 集合和 scalar 叶子规则。
[x] 同一解析器用于 preview、正式运行和 Station/shared execution。

## 明确不做

- V1 支持动态数字数组索引
- 把 `Expression` 当 resultPath
- 只在 UI 解析路径

## 执行清单

[x] 设计 `ResultPathVersion=1`。
[x] 对象键采用明确转义；集合仅支持稳定 ID selector 或明确禁止。
[x] Matrix/Image/Mask/任意脚本路径 fail closed。
[x] 增加路径解析器的独立 Core/Application 测试。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[x] 相同数据在 preview/正式运行得到相同值。
[x] 无效路径返回明确诊断。
[x] 路径规则可被 Inspector 与 Scene 复用。

## 回填区

- 状态：`DONE`
- 开始时间：2026-07-02 19:32:05 +08:00
- 完成时间：2026-07-02 20:54:23 +08:00
- Initial SHA：`746bbe52c4405b63d474508efd49813f895fad41`
- Final SHA：见 G07A follow-up 完成提交与最终报告
- 远端 SHA：见 G07A follow-up 完成提交与最终报告
- 修改文件：`Core/ResultPaths/ResultPathV1.cs`、`Core/ProjectVariables/ProjectVariableValidation.cs`、`Infrastructure/Services/FlowExecutionService.cs`、`Desktop/Observation/ExecutionObservationProjector.cs`、ResultPath/ProjectVariables/FlowExecution/Observation tests、`InspectionEventEndpointsTests.cs`、`TODO.md` 与本卡。
- 新增/变更契约：新增 Core-owned `ResultPathVersion=1` parser/formatter/resolver contract；SourceBinding additive nullable `ResultPathVersion`/`ResultPath`；Observation Summary/Detail additive nullable `OutputPortId`、`OutputPortName`、`ResultPathVersion`、`ResultPath` metadata。
- active owner / 唯一写入口：Core 是唯一 grammar/canonical/diagnostic/resolver authority；Project schema 保存仍走 `ProjectService -> ProjectSaveCoordinator`；execution writes still flow through `FlowExecutionService` working session then existing commit handler。
- Legacy mounted / subscription / timer 状态：不涉及 UI mount、subscription 或 timer；未执行 G07B 字段绑定 UI。
- 测试命令与结果：Product targeted ResultPath/ProjectVariables/FlowExecution PASS（95 tests）；Desktop targeted Observation/Preview/Architecture PASS（60 tests）；Desktop endpoint isolation PASS（37 tests）；`ClearVision.Product.Tests` full PASS（3128 passed, 4 skipped）；Desktop full serial with `xUnit.ParallelizeTestCollections=false` PASS（453 tests）；Desktop Debug build PASS；Station Debug/Release build PASS；Desktop Release publish to `.tmp/publish-check/g07a-followup` PASS；build/publish V2 asset audit PASS；publish no Node/source/dev artifacts PASS；`git diff --check` PASS；changed-file secret/large-file scans PASS；publish scratch/process checks PASS。
- 截图/Benchmark/Artifact：未生成截图；未做 benchmark；publish scratch verified and cleaned.
- API / Project format / Runtime / Station / AgentRun 影响：Project SourceBinding schema only receives additive nullable fields; TargetBinding unchanged；Runtime/Station package receives additive schema naturally through existing serialization and reuses shared `FlowExecutionService`; no Runtime Package authority, AgentRun, EventStore, Workspace Snapshot, terminal/recovery change。
- 技术债与非阻断事项：完整 GitHub CI NOT RUN；真实 WebView2：NOT PERFORMED；stable-ID selector syntax and adapter contract exist, but no generic reflection-based collection adapter is registered；未执行 G07B。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G07B`
