# G07A：Canonical ResultPath 解析器

> 阶段：Observation
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：建立前后端共用的稳定字段路径语义，为变量绑定和 Scene 双向定位提供唯一规则。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Core/ProjectVariables/ProjectGlobalVariableContracts.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Services/FlowExecutionService.cs`
- `ClearVision.Product/src/ClearVision.Product.Desktop/Endpoints/PreviewNodeEndpoints.cs`

## 本轮范围

[ ] 路径相对于已选择的 output port 根值。
[ ] 定义转义、对象键、稳定 ID 集合和 scalar 叶子规则。
[ ] 同一解析器用于 preview、正式运行和 Station/shared execution。

## 明确不做

- V1 支持动态数字数组索引
- 把 `Expression` 当 resultPath
- 只在 UI 解析路径

## 执行清单

[ ] 设计 `ResultPathVersion=1`。
[ ] 对象键采用明确转义；集合仅支持稳定 ID selector 或明确禁止。
[ ] Matrix/Image/Mask/任意脚本路径 fail closed。
[ ] 增加路径解析器的独立 Core/Application 测试。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[ ] 相同数据在 preview/正式运行得到相同值。
[ ] 无效路径返回明确诊断。
[ ] 路径规则可被 Inspector 与 Scene 复用。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G07B`
