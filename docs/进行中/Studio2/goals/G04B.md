# G04B：V2 单请求工程保存与持久化身份

> 阶段：Foundation
> 状态：`DONE`（仅当本 Goal 被当前执行项明确选中时执行）
> 目标：收敛 V2 的保存方式，复用现有 `/api/projects/{id}` 与 `ProjectSaveCoordinator`；保留旧 `/flow` 兼容入口。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/project/projectManager.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/Endpoints/ApiEndpoints.cs`
- `ClearVision.Product/src/ClearVision.Product.Application/DTOs/ProjectDto.cs`
- `ClearVision.Product/src/ClearVision.Product.Application/Services/ProjectService.cs`
- `ClearVision.Product/src/ClearVision.Product.Application/Services/ProjectSaveCoordinator.cs`

## 本轮范围

[x] 为 V2 定义一次提交 metadata + Flow + variables 的 client facade。
[x] 保存响应刷新 canonical ProjectDto 和 `PersistenceRevision`。
[x] 用契约测试证明后端仍进入 ProjectSaveCoordinator。

## 明确不做

- 删除旧 `/flow` endpoint
- 把 UI revision 当持久化 revision
- 无证据增加 clientMutationId 或新保存 endpoint

## 执行清单

[x] 先写 ADR：旧 `projectManager.saveProject()` 当前两次 HTTP 提交属于兼容行为。
[x] V2 优先使用现有 `PUT /api/projects/{id}` 一次 payload。
[x] 如需要并发保护，优先基于现有 `PersistenceRevision` 设计；新增请求字段前必须有竞争测试。
[x] 旧 ProjectManager 暂不迁移，留到 G15.8。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[x] 新增保存、stale revision、运行中 mutation lease、恢复回归测试。

## 完成条件

[x] V2 不进行两次独立业务保存。
[x] 没有第二保存 endpoint。
[x] 旧入口兼容且 ProjectSaveCoordinator 测试通过。

## 回填区

- 状态：`DONE`
- 开始时间：2026-07-02 10:06:29 +08:00
- 完成时间：2026-07-02 10:32:13 +08:00
- Initial SHA：`b4936ce22f380b00b5ff9e211c95219b10863b46`
- Final SHA：见完成提交与最终报告
- 远端 SHA：见完成提交与最终报告
- 修改文件：`FrontendV2/src/project/**`、`FrontendV2/src/components/**`、`FrontendV2/src/workspace/workspaceShellRuntime.ts`、`FrontendV2/src/adapters/legacyModules.ts`、`FrontendV2/src/flowEditor/studioFlowEditorPort.ts`、`FrontendV2/src/tests/**`、`wwwroot/src/core/messaging/httpClient.js`、`Application/DTOs/ProjectDto.cs`、`Application/Services/ProjectService.cs`、`Desktop/Endpoints/ApiEndpoints.cs`、相关 ProjectService/ProjectSaveCoordinator/Desktop endpoint/UI/Playwright/Architecture Guard 测试、G01 ADR、迁移台账、根 `TODO.md` 与本卡。
- 新增/变更契约：新增 V2 `StudioProjectPersistencePort`；新增 `UpdateProjectRequest.ExpectedPersistenceRevision`；`PUT /api/projects/{id}` 在 `PSV011` 时返回 409；legacy `httpClient.put` 支持 `AbortSignal` 并保留 HTTP error payload。
- active owner / 唯一写入口：V2 只注册 `studio2.projectPersistencePort`；保存只走既有 `PUT /api/projects/{id}` -> `ProjectService` -> `ProjectSaveCoordinator`；`flowRevision` 仅为前端本地草稿 revision，后端并发身份为 `PersistenceRevision`。
- Legacy mounted / subscription / timer 状态：未迁移旧 Project 页面；未修改 `app.js`；未订阅旧 Project list；未新增 timer；旧 `projectManager.saveProject()` 与 `/flow` 兼容入口保留到 G15.8/G16 收口。
- 测试命令与结果：`npm run lint` PASS；`npm run typecheck` PASS；`npm run test:unit` PASS，8 files/40 tests；`npm run build` PASS；`ProjectServiceTests,ProjectSaveCoordinatorTests` PASS，29 tests；`ProjectGlobalVariableEndpointsTests,Studio2ArchitectureGuardTests` PASS，36 tests；`node --test tests/unit/app-infrastructure.test.mjs` PASS，20 tests；`npx playwright test tests/e2e/studio2-flow-editor-port.spec.ts --reporter=list` PASS，3 tests；Desktop `dotnet build` PASS；Desktop `dotnet publish` 到 `.tmp/publish-check/g04b-desktop` PASS；build/publish `wwwroot/v2` asset audit PASS；`git diff --check` PASS。
- 截图/Benchmark/Artifact：未生成截图；未运行 benchmark；临时 publish artifact 已按 AGENTS 清理。
- API / Project format / Runtime / Station / AgentRun 影响：未新增保存 endpoint；Project DTO 增加请求侧 `ExpectedPersistenceRevision`，响应侧继续使用既有 `PersistenceRevision`；Runtime Package、Station、AgentRun 未修改。
- 技术债与非阻断事项：旧 `projectManager.saveProject()` 仍保持两步兼容保存，Project 页面正式迁移仍等待 G15.8；完整 GitHub CI 未运行；真实 WebView2 人工启动未执行。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G05A`
