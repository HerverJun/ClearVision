# G04B：V2 单请求工程保存与持久化身份

> 阶段：Foundation
> 状态：`READY`（仅当本 Goal 被当前执行项明确选中时执行）
> 目标：收敛 V2 的保存方式，复用现有 `/api/projects/{id}` 与 `ProjectSaveCoordinator`；保留旧 `/flow` 兼容入口。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/project/projectManager.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/Endpoints/ApiEndpoints.cs`
- `ClearVision.Product/src/ClearVision.Product.Application/DTOs/ProjectDto.cs`
- `ClearVision.Product/src/ClearVision.Product.Application/Services/ProjectService.cs`
- `ClearVision.Product/src/ClearVision.Product.Application/Services/ProjectSaveCoordinator.cs`

## 本轮范围

[ ] 为 V2 定义一次提交 metadata + Flow + variables 的 client facade。
[ ] 保存响应刷新 canonical ProjectDto 和 `PersistenceRevision`。
[ ] 用契约测试证明后端仍进入 ProjectSaveCoordinator。

## 明确不做

- 删除旧 `/flow` endpoint
- 把 UI revision 当持久化 revision
- 无证据增加 clientMutationId 或新保存 endpoint

## 执行清单

[ ] 先写 ADR：旧 `projectManager.saveProject()` 当前两次 HTTP 提交属于兼容行为。
[ ] V2 优先使用现有 `PUT /api/projects/{id}` 一次 payload。
[ ] 如需要并发保护，优先基于现有 `PersistenceRevision` 设计；新增请求字段前必须有竞争测试。
[ ] 旧 ProjectManager 暂不迁移，留到 G15.8。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] 新增保存、stale revision、运行中 mutation lease、恢复回归测试。

## 完成条件

[ ] V2 不进行两次独立业务保存。
[ ] 没有第二保存 endpoint。
[ ] 旧入口兼容且 ProjectSaveCoordinator 测试通过。

## 回填区

- 状态：`READY`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G05A`
