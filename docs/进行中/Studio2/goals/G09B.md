# G09B：Circle、Annulus 与 Arc 编辑

> 阶段：Geometry/Spatial
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：在已验证内核上加入圆类几何，为 Circle Search 工具服务。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/imageCanvas.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/roiEditorSupport.mjs`

## 本轮范围

[ ] Circle、Annulus、Arc 模型和 handles。
[ ] GeometryParameterAdapter。
[ ] 高 DPI、边界和最小尺寸规则。

## 明确不做

- Polygon/PointSequence
- Circle Search 算法
- 新增不对应现有参数的 RotatedRectangle

## 执行清单

[ ] 定义角度方向、单位和归一化规则。
[ ] Annulus 内外半径约束 fail closed。
[ ] 参数↔几何 round-trip。
[ ] commit 仍走 V2 port。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[ ] 圆类编辑稳定。
[ ] 参数兼容。
[ ] 不修改算法结果。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G09C`
