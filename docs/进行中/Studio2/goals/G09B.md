# G09B：Circle、Annulus 与 Arc 编辑

> 阶段：Geometry/Spatial
> 状态：`DONE`
> 目标：在已验证内核上加入圆类几何，为 Circle Search 工具服务。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/imageCanvas.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/roiEditorSupport.mjs`

## 本轮范围

[x] Circle、Annulus、Arc 模型和 handles。
[x] GeometryParameterAdapter。
[x] 高 DPI、边界和最小尺寸规则。

## 明确不做

- Polygon/PointSequence
- Circle Search 算法
- 新增不对应现有参数的 RotatedRectangle

## 执行清单

[x] 定义角度方向、单位和归一化规则。
[x] Annulus 内外半径约束 fail closed。
[x] 参数↔几何 round-trip。
[x] commit 仍走 V2 port。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[x] 圆类编辑稳定。
[x] 参数兼容。
[x] 不修改算法结果。

## 回填区

- 状态：`DONE`
- 开始时间：2026-07-03 01:10:00 +08:00
- 完成时间：2026-07-03 01:25:18 +08:00
- Initial SHA：`28f3330e542bff60fbe0e293f85455ac2a161feb`
- Final SHA：提交后核对
- 远端 SHA：提交并 push 后核对
- 修改文件：
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/roiGeometry.mjs`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/roiEditorSupport.mjs`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/roiEditorPanel.js`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/imageCanvas.js`
  - `ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/unit/roi-geometry.test.mjs`
  - `ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/e2e/roi-editor.spec.ts`
  - `TODO.md`
  - `docs/进行中/Studio2/goals/G09B.md`
- 新增/变更契约：角度单位为 degrees，0° 指向 +X，图像坐标系正向为 clockwise；角度归一化到 `[0, 360)`，arc span 按 clockwise positive span 计算，full annulus 使用 360° span。新增 Circle 和 PolarUnwrap Annulus/Arc 参数适配，旧参数名保持不变。
- active owner / 唯一写入口：`ImageCanvas` 仍是唯一 canvas 交互 owner；`RoiEditorPanel` 经 `GeometryParameterAdapter` 做参数↔geometry round-trip；`PropertyPanel` 仍只在 commit phase 进入既有 V2 flow/preview 链。
- Legacy mounted / subscription / timer 状态：未新增第二 canvas、未新增全局 undo bus、未修改算法；RoiManager Circle、PolarUnwrap Annulus/Arc 复用现有 ROI editor mount。
- 测试命令与结果：
  - `node --test tests/unit/roi-geometry.test.mjs`：PASS，22 tests。
  - `node --test tests/unit/canvas-core.test.mjs`：PASS，25 tests。
  - `npx playwright test tests/e2e/roi-editor.spec.ts --reporter=list`：PASS，8 tests。
  - `npm run test:unit`：PASS，601 tests。
  - `npm run test:preview-smoke`：PASS。
  - `git diff --check`：PASS。
  - process audit：PASS，无残留 `dotnet`/`testhost`/`node`。
- 截图/Benchmark/Artifact：未新增截图或 benchmark；本阶段验证为 unit + Playwright geometry editing parity。
- API / Project format / Runtime / Station / AgentRun 影响：无 API、Project format、Runtime、Station 或 AgentRun 影响；未修改 Circle Search / PolarUnwrap / ArcCaliper 算法结果。
- 技术债与非阻断事项：G09C 继续在同一 kernel 上扩展 Polygon/PointSequence；RoiManager Polygon 保持只读。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G09C`

## Follow-up Reference

- 2026-07-03 `G08-G10B-FOLLOWUP` closed additional Visual Geometry / SpatialContext consistency gaps that touched this goal area. Follow-up Initial SHA: `6a0a18bdd398dda71849bdc7715e01737f8ea4a0`; final SHA is verified in the follow-up completion commit/final report. Historical Initial/Final SHA fields above are unchanged.
