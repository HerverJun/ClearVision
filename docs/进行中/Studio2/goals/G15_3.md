# G15.3：Global Variables capability 迁移

> 阶段：Productization
> 状态：`DONE`
> 目标：迁移 Global Variables 的外壳和交互，同时保证 flag on 时旧实现完全不挂载；删除动作需独立证据门禁。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/global-variables`
- `ClearVision.Product/src/ClearVision.Product.Core/ProjectVariables/ProjectGlobalVariableContracts.cs`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/app.js`

## 本轮范围

[ ] 先盘点旧 owner、订阅、timer、写入口和合同。
[ ] V2 capability flag 实现与 parity。
[ ] 显式切换 active owner。

## 明确不做

- 同轮迁移其他 capability
- 仅用 CSS 隐藏旧实现
- 未经人工批准立即删除复杂旧实现

## 执行清单

[ ] 写行为清单和切换矩阵。
[ ] flag on：旧模块不 mounted、不订阅、不运行 timer、不持有资源。
[ ] 完成合同测试、Playwright、WebView2 人工验收。
[ ] 达到证据门禁后再执行删除；不能等待“时间窗口”，改用明确的 N 次 clean CI/人工批准。
[ ] 删除旧 CSS、临时 adapter 和 flag 后再次回归。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[ ] 该 capability 只有一个生产 owner。
[ ] 旧实现删除或明确保留底层库及理由。
[ ] 其他 capability 无回退。

## 回填区

- 状态：`DONE`
- 开始时间：`2026-07-04 22:53:57 +08:00`
- 完成时间：`2026-07-04 23:22:07 +08:00`
- Initial SHA：`2b8043c2b7ccfcb63f9d67a6261fdc33fa57a73e`
- Final SHA：提交自身 SHA 不写入 tracked 文件；最终报告写
- 远端 SHA：提交后最终报告写
- 修改文件：
  - `ClearVision.Product/src/ClearVision.Product.Desktop/Configuration/StudioOptions.cs`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/WebView2Host.cs`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/appsettings.json`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/index.html`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/app.js`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/global-variables/globalVariablesCapabilityOwner.mjs`
  - `ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/unit/g15x-capability-owners.test.mjs`
  - `ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/Architecture/Studio2ArchitectureGuardTests.cs`
  - `ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/WebView2HostTests.cs`
- 新增/变更契约：
  - 新增默认关闭 flag：`Studio2.GlobalVariables` / `Studio:GlobalVariablesCapabilityEnabled=false`。
  - 新增 `GlobalVariablesCapabilityOwner` 与 `GlobalVariablesCapabilityAdapter`；Project global variables schema、SourceBinding、TargetBinding、VariableWrite/Increment 后端契约未改。
  - `index.html` 不再静态加载 G15X legacy panel 模块；GlobalVariables legacy 模块只在 flag off 分支动态 import。
- 行为清单和切换矩阵：
  - all flags off：legacy `GlobalVariablePanel` 唯一 owner。
  - `Studio2.GlobalVariables=true`：只挂载 `GlobalVariablesCapabilityOwner`，保留 `globalVariablePanel` service key 供 Inspector/Property Panel 兼容。
  - `GlobalVariables on + PropertyPanel on`：变量 schema 更新仍经 `projectManager.updateGlobalVariables/saveGlobalVariables`，无第二 authority。
  - all G15 flags on：GlobalVariables 只有 V2 owner、V2 adapter、一个 runtime state subscription。
- active owner / 唯一写入口：`GlobalVariablesCapabilityOwner -> GlobalVariablesCapabilityAdapter -> projectManager.updateGlobalVariables()/saveGlobalVariables()`。
- Legacy mounted / subscription / timer 状态：flag on 时 legacy `GlobalVariablePanel` 不构造、不 mounted、不订阅 Inspection runtime state、不运行 runtime polling timer、不写入；flag off 继续原行为。
- flag 登记：owner `GlobalVariablesCapabilityOwner`；default `false`；off behavior legacy panel；on behavior V2 owner；cutover G15.3；deletion goal `G16`。
- 测试命令与结果：
  - `node --check` touched JS/MJS：PASS。
  - `node --test tests/unit/g15x-capability-owners.test.mjs ...` focused UI：PASS（492/492）。
  - `& "./scripts/run-dotnet-test-serial.ps1" -Project "ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/ClearVision.Product.Desktop.Tests.csproj" -FullyQualifiedName WebView2HostTests,Studio2ArchitectureGuardTests`：PASS（33/33）。
  - `git diff --check`：PASS（仅 LF→CRLF normalization warnings）。
- 截图/Benchmark/Artifact：Playwright NOT RUN；真实 WebView2 NOT PERFORMED；screenshot NOT PERFORMED；benchmark NOT RUN；artifact NOT GENERATED。
- API / Project format / Runtime / Station / AgentRun 影响：Project format 未改；Project/Application Service/ProjectSaveCoordinator authority 未改；Runtime/Station/AgentRun 未改。
- 技术债与非阻断事项：V2 owner 是默认关闭的收口壳；复杂 legacy 实现保留为 flag-off library，删除与完整回归门禁在 G16。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G15.5`
