# G04A：V2 Flow 编辑端口与本地 stale 防护

> 阶段：Foundation
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：为新 V2 组件提供单一编辑 facade；不强迫旧 DOM 和 Agent 在本轮全部迁入。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/flowCanvasAdapter.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/flowCanvas.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/project/projectManager.js`

## 本轮范围

[ ] 定义 TS `StudioFlowEditorPort`。
[ ] 运行时实现直接包裹现有 `FlowCanvasAdapter`。
[ ] 对 V2 命令使用 projectId、selected node、UI flow revision/request sequence 防止迟到覆盖。

## 明确不做

- 让 Agent apply 经过前端 TS port
- 建立第二 command bus
- 声称旧模块已拥有唯一写入口

## 执行清单

[ ] 补齐 snapshot/select/patchParameters/replaceFlow/structure & selection subscription/dispose。
[ ] V2 组件禁止直接访问 `window.flowCanvas` 或 raw 内部字段。
[ ] 完成一次 V2 节点选择和参数 draft/commit。
[ ] 明确 `flowRevision` 仅是 UI 本地版本，不等同后端 `PersistenceRevision`。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[ ] V2 Flow 写操作只能经该 facade。
[ ] 旧模块仍可运行但不由 V2 重复订阅。
[ ] Agent 现有链路无变化。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G04B`
