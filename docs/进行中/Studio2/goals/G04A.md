# G04A：V2 Flow 编辑端口与本地 stale 防护

> 阶段：Foundation
> 状态：`DONE`
> 目标：为 V2 组件提供单一 Flow 编辑 facade；不强迫旧 DOM、Agent 或 Project 保存链路在本轮迁入。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. G01 ADR、capability 迁移台账；
5. G03 Workspace runtime/lifecycle；
6. 下列真实代码锚点。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/flowCanvasAdapter.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/flowCanvas.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/project/projectManager.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/propertyPanel.js`
- `ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/Architecture/Studio2ArchitectureGuardTests.cs`
- `ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/unit/canvas-core.test.mjs`

## 本轮范围

[x] 定义 TS `StudioFlowEditorPort`。
[x] Runtime 直接包装现有 `FlowCanvasAdapter`，不建立第二 command bus、第二状态树或第二 canvas facade。
[x] 对 V2 写命令使用 `projectId`、`requestSequence`、`flowRevision`、`selectionRevision` 和 `nodeId` 防止迟到覆盖。
[x] `flowRevision` 明确为前端本地编辑版本，不等同后端 `PersistenceRevision`。
[x] Workspace runtime 私有持有 adapter，`ServiceRegistry` 只公开 `studio2.flowEditorPort`。
[x] Tool Dock 增加最小 Flow 编辑端口验证面板，支持真实节点标量参数 draft/apply/discard。
[x] 修复 hosted adapter 旧实例二次 `dispose()` 不得驱逐当前 adapter 的生命周期缺陷。

## 明确不做

- 不实现 G04B 的 Project 保存、后端持久化身份或新 API。
- 不修改 `ProjectSaveCoordinator`、`projectManager.saveProject`、Project/Flow/Agent/Variables 后端。
- 不让 Agent apply 经过前端 TS port。
- 不建立第二 command bus。
- 不声明旧模块已经拥有唯一写入口。
- 不迁移完整 PropertyPanel、Preview、ROI、AI、Results 或 Project 页面。

## 执行清单

[x] 补齐 `getSnapshot`、`replaceFlow`、`selectNode`、`patchParameters`、structure/selection subscription 和 `dispose`。
[x] V2 禁止直接访问 `window.flowCanvas`、`adapter.raw` 或 raw `FlowCanvas` 内部对象。
[x] 完成一次 V2 真实节点选择和参数 draft/commit 浏览器验证。
[x] stale disposition 覆盖 `project_mismatch`、`stale_request`、`stale_flow_revision`、`stale_selection`、`node_not_found`、`parameter_not_found`、`disposed`。
[x] Adapter 参数 patch 只修改已存在参数；拒绝 missing parameter 时不产生部分写入。
[x] Snapshot 对 flow 和 selected node 深拷贝，外部修改不污染真实 FlowCanvas。
[x] structure/selection subscription 每个 adapter event 只触发一次，dispose 后不再触发。
[x] 20 次 Port + Workspace mount/dispose 覆盖 registry、listener、adapter 释放。

## 验证清单

[x] `npm run lint`。
[x] `npm run typecheck`。
[x] `npm run test:unit`。
[x] `npm run build`。
[x] `node --test tests/unit/canvas-core.test.mjs`。
[x] `node --test tests/unit/ai-agent-ui-contract.test.mjs`。
[x] `npx playwright test tests/e2e/studio2-flow-editor-port.spec.ts --reporter=list`。
[x] `& "./scripts/run-dotnet-test-serial.ps1" -Project "ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/ClearVision.Product.Desktop.Tests.csproj" -FullyQualifiedName Studio2ArchitectureGuardTests,BuildFromPlanArchitectureGuardTests`。
[x] `dotnet build "ClearVision.Product/src/ClearVision.Product.Desktop/ClearVision.Product.Desktop.csproj" --nologo --verbosity minimal`。
[x] `dotnet publish "ClearVision.Product/src/ClearVision.Product.Desktop/ClearVision.Product.Desktop.csproj" --nologo --verbosity minimal -o ".tmp/publish-check/g04a"`。
[x] build/publish 输出审计：`wwwroot/v2` 存在，包含 `index.html`、`.vite/manifest.json` 和 2 个 assets；未包含 `.ts/.tsx/.vue`、`package.json`、`package-lock.json` 或 `node_modules`。
[x] `git diff --check`。
[x] 测试结束确认无本轮遗留 `dotnet`、`testhost`、node 或浏览器测试进程。
[x] 回填本卡和根目录 `TODO.md` 后提交并推送。

## 完成条件

[x] V2 Flow 写操作只能经 `StudioFlowEditorPort` facade。
[x] 旧模块仍可运行，旧页 flag off 未挂载 V2 root。
[x] Agent 现有 apply 链路无变更，并通过 `ai-agent-ui-contract.test.mjs` 回归。
[x] G04B 标记为下一项 `READY`，但本轮未执行。

## 回填区

- 状态：`DONE`
- 开始时间：2026-07-01 22:46:25 +08:00
- 完成时间：2026-07-01 23:17:44 +08:00
- Initial SHA：`33c8276332fe6543036d260bfb39f50a815c1c17`
- Final SHA：见完成提交与最终报告
- 远端 SHA：见完成提交与最终报告
- 修改文件：`ClearVision.Product/src/ClearVision.Product.Desktop/FrontendV2/**`；`ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/flowCanvasAdapter.js`；`ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/flowCanvas.js` 的最小 selection subscription 支持；`ClearVision.Product/tests/ClearVision.Product.UI.Tests/**`；`ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/Architecture/Studio2ArchitectureGuardTests.cs`；`docs/进行中/Studio2/**`；`TODO.md`。
- 新增/变更契约：新增 `StudioFlowEditorPort`，由 `createStudioFlowEditorPort()` 包装 legacy `FlowCanvasAdapter`；`ServiceRegistry` 只注册 `studio2.flowEditorPort`；snapshot 为深拷贝；所有写命令返回 typed disposition；adapter 参数 patch 保持 metadata 和大小写格式。
- active owner / 唯一写入口：V2 组件只取得 Port；Workspace runtime 私有持有 hosted adapter；正式 Project 保存仍留给 G04B 及后续 Project authority。
- Legacy mounted / subscription / timer 状态：旧页面 flag off 仍使用原 `#flow-canvas`；本轮未改 `app.js`、旧 PropertyPanel、Agent apply 或旧业务 timer；V2 Port dispose 后释放 subscriptions 与 adapter。
- 测试命令与结果：见“验证清单”，均 PASS；完整 GitHub CI NOT RUN；真实 WebView2 人工启动 NOT PERFORMED。
- 截图/Benchmark/Artifact：未生成需归档截图；Playwright 浏览器验证逻辑已提交为 `tests/e2e/studio2-flow-editor-port.spec.ts`。
- API / Project format / Runtime / Station / AgentRun 影响：未新增 API；未修改 Project DTO、Runtime Package、Station 或 AgentRun 权威；`flowRevision` 仅为前端本地编辑版本。
- 技术债与非阻断事项：G04B 仍需实现单请求 Project 保存与后端持久化身份；完整 PropertyPanel/Preview/ROI/AI/Results/Project capability 迁移继续保持后续 Goal；完整 GitHub CI 未运行。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G04B`

## 2026-07-02 follow-up：Port stale 与 revision 收口

- 状态：`DONE`
- Initial SHA：`59544fa7d6384d06828c8eaa3b3d183de9c96dcd`
- Final SHA：见本轮完成提交与最终报告
- 修改白名单：`FrontendV2/src/flowEditor/**`、`FrontendV2/src/components/FlowEditorPortPanel.vue`、`FrontendV2/src/tests/**`、`wwwroot/src/core/canvas/flowCanvas.js`、`ClearVision.Product.UI.Tests` 中 G04A canvas/Playwright 测试、`Studio2ArchitectureGuardTests.cs`、本卡、根 `TODO.md` 和 Studio2 README。
- 修复收口：
  - `StudioFlowEditorPort` 改为按 `projectId` 维护 `maxObservedRequestSequenceByProject`，在 project identity 检查后先校验并记录合法 request sequence；高序号命令即使随后返回 `stale_flow_revision`、`stale_selection`、`node_not_found` 或 `parameter_not_found`，也会封住同 project 的后续低序号命令。
  - `project_mismatch` 不推进当前 project sequence；`disposed` 命令优先返回 `disposed` 且不更新 sequence；`0`、负数、非整数、非 safe integer、`NaN`、`Infinity` 均返回 `stale_request`。
  - `StudioFlowEditorPort.nextRequestSequence(projectId)` 成为 V2 命令的唯一 allocator；`FlowEditorPortPanel` 不再维护组件本地 `Date.now()`/自增 request sequence。
  - `FlowCanvas` 节点拖拽在 mouse down 记录起点，mouse move 只做视觉坐标更新，mouse up 发现坐标变化时仅调用一次 `markFlowStructureChanged('moveNode')`；点击不移动不增加 `flowRevision`。
  - `FlowEditorPortPanel` 在编辑开始记录 draft baseline（`projectId`、`nodeId`、`flowRevision`、`selectionRevision`），脏草稿遇到 flow/selection 变化时保留输入并提示“流程或选择已变化，请放弃草稿或重新加载”；提交使用 baseline revision，让 Port 返回真实 stale disposition；放弃草稿恢复最新 snapshot。
  - 布尔参数改用 checkbox；非法数字和非法布尔值阻止提交，不再把无效数字写成字符串。
- 验证记录：
  - `npm run lint`：PASS。
  - `npm run typecheck`：PASS。
  - `npm run test:unit`：PASS，7 files / 31 tests。
  - `npm run build`：PASS，V2 asset validation PASS。
  - `node --test tests/unit/canvas-core.test.mjs`：PASS，25/25。
  - `node --test tests/unit/ai-agent-ui-contract.test.mjs`：PASS，351/351。
  - `npx playwright test tests/e2e/studio2-flow-editor-port.spec.ts --reporter=list`：PASS，2/2；其中 flag off 旧页未挂载 V2 root。
  - `& "./scripts/run-dotnet-test-serial.ps1" -Project "ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/ClearVision.Product.Desktop.Tests.csproj" -FullyQualifiedName Studio2ArchitectureGuardTests,BuildFromPlanArchitectureGuardTests`：PASS，17/17。
  - `dotnet build "ClearVision.Product/src/ClearVision.Product.Desktop/ClearVision.Product.Desktop.csproj" --nologo --verbosity minimal`：PASS。
  - `dotnet publish "ClearVision.Product/src/ClearVision.Product.Desktop/ClearVision.Product.Desktop.csproj" --nologo --verbosity minimal -o ".tmp/publish-check/g04a-followup"`：PASS。
  - build/publish 输出审计：PASS；Debug build 与 Release publish 的 `wwwroot/v2` 均包含 `index.html`、`.vite/manifest.json` 和 2 个 assets，未包含 `.ts/.tsx/.vue`、`package.json`、`package-lock.json` 或 `node_modules`。
  - `git diff --check`：PASS。
  - 完整 GitHub CI：NOT RUN。
  - 真实 WebView2 人工启动：NOT PERFORMED。
- 边界确认：未执行 G04B；未新增 Project 保存、`PersistenceRevision`、后端 API 或 Agent apply 迁移；未修改 `ProjectSaveCoordinator`、Project/Flow/Agent/Variables 后端、Runtime Package、Station 或 `main`。
- 下一 Goal：`G04B`
