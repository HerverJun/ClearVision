# G02A：FrontendV2 构建与发布底座

> 阶段：Foundation
> 状态：`DONE`
> 目标：引入 Vue 3/TypeScript/Vite 的构建产物，但不挂载业务、不建立第二通信栈。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/ClearVision.Product.Desktop.csproj`
- `ClearVision.Product/src/ClearVision.Product.Desktop/WebView2Host.cs`
- `ClearVision.Product/tests/ClearVision.Product.UI.Tests/package.json`
- `.github/workflows/ci.yml`
- `.gitignore`

## 本轮范围

[x] 建立独立 `FrontendV2` 源目录、package/lock、strict TS、Vue、Pinia、Vite、lint/typecheck/unit/build。
[x] 设计确定性的 Vite 输出与 MSBuild copy/publish 链。
[x] CI cache 同时覆盖现有 UI 测试 lockfile 和 FrontendV2 lockfile。

## 明确不做

- 把 Vue 依赖塞进现有 CommonJS Playwright 测试包
- 把生成 dist 作为无清理策略的手工复制目录
- 修改业务页面或 Flow 语义

## 执行清单

[x] 优先输出到 `obj/FrontendV2/dist` 或等价生成目录；由 MSBuild 明确复制到发布 `wwwroot/v2`。
[x] Node 只在构建机存在，目标机不要求 Node。
[x] clean clone 下执行 npm build → dotnet build → publish。
[x] 为生成目录补充 ignore/clean 规则，禁止陈旧资产混入发布。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[x] 在无 Node 环境模拟检查发布目录，仅运行已构建静态资产。

## 完成条件

[x] Release/Publish 包包含 V2 静态资产。
[x] 旧入口行为完全不变。
[x] 没有 tracked 的陈旧 dist。

## 回填区

- 状态：`DONE`
- 开始时间：2026-07-01 17:32:48 +08:00
- 完成时间：2026-07-01 17:46:32 +08:00
- Initial SHA：`ff9a9430c0eb84e9b9f8e88beac9140c987bd4e8`
- Final SHA：见 G02A 完成提交与最终报告
- 远端 SHA：见 G02A 完成提交与最终报告
- 修改文件：`ClearVision.Product/src/ClearVision.Product.Desktop/FrontendV2/**`；`ClearVision.Product/src/ClearVision.Product.Desktop/ClearVision.Product.Desktop.csproj`；`.github/workflows/ci.yml`；`.gitignore`；`ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/Architecture/Studio2ArchitectureGuardTests.cs`；`docs/进行中/Studio2/architecture/Studio2-架构边界-ADR.md`；`docs/进行中/Studio2/architecture/Studio2-capability-迁移台账.md`；`docs/进行中/Studio2/README.md`；`docs/进行中/Studio2/goals/G02A.md`；`TODO.md`
- 新增/变更契约：新增 `ClearVision.Product/src/ClearVision.Product.Desktop/FrontendV2/` 独立源目录，包含 Vue 3、TypeScript strict、Vite、Pinia、ESLint、Vitest、`package-lock.json`；Desktop `.csproj` 默认执行 `npm ci` + Vite build，并复制 `obj/<Configuration>/<TargetFramework>/FrontendV2/dist` 到 build/publish 的 `wwwroot/v2/`；CI 在 .NET build/publish 前执行 FrontendV2 `npm ci`、lint、typecheck、unit test、build。
- active owner / 唯一写入口：本轮未挂载 Vue root；`app.js` 仍是旧前端唯一 composition root；`EventBus`、`ServiceRegistry`、`ProjectService + ProjectSaveCoordinator`、AgentRun/EventStore 权威不变。
- Legacy mounted / subscription / timer 状态：旧入口仍 mounted；本轮未新增订阅、timer、HostBridge、API client 或业务 store；V2 只产出静态占位资产。
- 测试命令与结果：`npm ci` PASS；`npm run lint` PASS；`npm run typecheck` PASS；`npm run test:unit` PASS，1 file/3 tests；`npm run build` PASS；删除 Debug V2 dist/output 后 `dotnet build ClearVision.Product/src/ClearVision.Product.Desktop/ClearVision.Product.Desktop.csproj` PASS，0 warnings，0 errors；publish 到 `.tmp/publish-check/g02a` PASS；build/publish 输出审计 PASS；`dotnet build ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/ClearVision.Product.Desktop.Tests.csproj` PASS；`& "./scripts/run-dotnet-test-serial.ps1" -Project "ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/ClearVision.Product.Desktop.Tests.csproj" -FullyQualifiedName Studio2ArchitectureGuardTests,BuildFromPlanArchitectureGuardTests -NoBuild -NoRestore` PASS，11/11；完整 CI NOT RUN。
- 截图/Benchmark/Artifact：无截图和 benchmark；临时发布目录 `.tmp/publish-check/g02a` 已用于审计，提交前清理。
- API / Project format / Runtime / Station / AgentRun 影响：无 API 变更；无 Project format 变更；无 Runtime Package 语义变更；Station 生产代码未改；AgentRun 未改。
- 技术债与非阻断事项：`SkipFrontendV2Build=true` 仅用于受控复用已生成 `obj` 资产，默认 build/publish 不跳过且会清理 dist；G02A 不实现 runtime flag、不挂载 V2、不创建 HostBridge；后续 G02B 负责挂载和通信适配。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G02B`
