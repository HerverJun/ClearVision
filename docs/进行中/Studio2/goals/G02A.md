# G02A：FrontendV2 构建与发布底座

> 阶段：Foundation
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：引入 Vue 3/TypeScript/Vite 的构建产物，但不挂载业务、不建立第二通信栈。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/ClearVision.Product.Desktop.csproj`
- `ClearVision.Product/src/ClearVision.Product.Desktop/WebView2Host.cs`
- `ClearVision.Product/tests/ClearVision.Product.UI.Tests/package.json`
- `.github/workflows/ci.yml`
- `.gitignore`

## 本轮范围

[ ] 建立独立 `FrontendV2` 源目录、package/lock、strict TS、Vue、Pinia、Vite、lint/typecheck/unit/build。
[ ] 设计确定性的 Vite 输出与 MSBuild copy/publish 链。
[ ] CI cache 同时覆盖现有 UI 测试 lockfile 和 FrontendV2 lockfile。

## 明确不做

- 把 Vue 依赖塞进现有 CommonJS Playwright 测试包
- 把生成 dist 作为无清理策略的手工复制目录
- 修改业务页面或 Flow 语义

## 执行清单

[ ] 优先输出到 `obj/FrontendV2/dist` 或等价生成目录；由 MSBuild 明确复制到发布 `wwwroot/v2`。
[ ] Node 只在构建机存在，目标机不要求 Node。
[ ] clean clone 下执行 npm build → dotnet build → publish。
[ ] 为生成目录补充 ignore/clean 规则，禁止陈旧资产混入发布。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] 在无 Node 环境模拟检查发布目录，仅运行已构建静态资产。

## 完成条件

[ ] Release/Publish 包包含 V2 静态资产。
[ ] 旧入口行为完全不变。
[ ] 没有 tracked 的陈旧 dist。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G02B`
