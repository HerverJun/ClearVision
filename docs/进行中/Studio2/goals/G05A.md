# G05A：Execution Observation 投影与身份

> 阶段：Observation
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：在现有 node preview/debug 结果上建立无持久化、只读、可丢弃的展示投影。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/Endpoints/PreviewNodeEndpoints.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Services/FlowExecutionService.cs`
- `ClearVision.Product/src/ClearVision.Product.Core/Operators/IOperatorExecutor.cs`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/previewCoordinator.js`

## 本轮范围

[ ] 定义 Desktop/Application 边界的 `ExecutionObservationEnvelopeV1`。
[ ] Summary/Detail 的有界序列化。
[ ] 身份采用 projectId + targetNodeId + debugSessionId + client request sequence/flow revision；正式 runId 仅在可用时附加。

## 明确不做

- 进入 Core 聚合、EF 或 RuntimeHost
- 替换 `OperatorExecutionOutput`
- 创建第二执行引擎或第二 debug session authority

## 执行清单

[ ] 先适配 `/api/flows/preview-node`。
[ ] 旧 response 保持兼容，可用 additive 字段或新版本 endpoint。
[ ] 未知类型、循环引用、深层集合、OpenCv 对象必须 fail-soft/有界。
[ ] 明确 result path 的 canonical 语义供 G07 复用。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] 增加 serializer golden、identity stale rejection、旧客户端兼容测试。

## 完成条件

[ ] Observation 不持久化。
[ ] 旧 preview 客户端继续工作。
[ ] 迟到响应不能串工程/节点。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G05B`
