# G12B：N 点标定工作台 draft 与可视化

> 阶段：Vertical Product
> 状态：`DONE`
> 目标：完成九点采集、编辑、求解和 Scene 展示；正式资产保存留给 G13A。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/NPointCalibrationOperator.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Calibration/CalibrationBundleV2.cs`
- `docs/进行中/Studio2/goals/G09C.md`

## 本轮范围

[x] CalibrationSession 仅作为 UI draft。
[x] Sample：pixel/world/source/enabled/inlier/error/note。
[x] 图像点击、上游 Point/Circle Center、世界坐标录入、九点模板、排序、排除/恢复。

## 明确不做

- 本轮实现 ProjectSave participant
- 真实 PLC/机器人 provider
- 平行 CalibrationArtifact

## 执行清单

[x] 工作台通过 Application DTO 调 solver。
[x] Scene 显示 sample、顺序、内外点、重投影和误差向量。
[x] Inspector 显示 matrix/quality/frames/unit。
[x] 提供导出候选 Bundle 但标记未正式保存。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md`；提交/推送未在本工作树执行。
[x] 九点 happy path、异常点、退化点、重算、工程切换丢弃 draft 测试。

## 完成条件

[x] 九点全过程可在 Studio 完成到“候选 Bundle”。
[x] draft 不冒充正式资产。

## 回填区

- 状态：`DONE`
- 开始时间：`2026-07-04 10:11:13 +08:00`
- 完成时间：`2026-07-04 11:03:16 +08:00`
- Initial SHA：`04ed17aafa97608fedc5bfc4374714d62f8c4ce5`
- Final SHA：提交自身 SHA 不写入 tracked 文件；以 push 后核对值为准
- 远端 SHA：`04ed17aafa97608fedc5bfc4374714d62f8c4ce5`
- 修改文件：Desktop draft endpoint/registration/feature flag/WebView2/visual scene；frontend workbench/property panel/styles/flag support；NPoint solver inlier flags；Desktop/Product/UI/Playwright tests；`TODO.md`/G12B/G13A docs。
- 新增/变更契约：`POST /api/calibration/npoint-draft/solve`；`CalibrationDraftSession`/sample draft DTO；`calibration-draft-session.v1`、`calibration-sample-table.v1`、`calibration-candidate-bundle.v1` artifacts（512 KiB cap）；`Studio:NPointCalibrationWorkbenchEnabled`。
- active owner / 唯一写入口：PropertyPanel owns `CalibrationDraftWorkbench`; endpoint owns draft DTO -> `NPointCalibrationSolver` -> Observation/Artifact; no `ProjectSaveCoordinator` participant.
- Legacy mounted / subscription / timer 状态：flag off falls back to generic parameters; workbench `destroy()` aborts solve, unsubscribes preview, removes canvas listener, destroys `ImageCanvas`; no extra timer.
- 测试命令与结果：Desktop focused PASS（61/61）；Product focused PASS（21/21）；Product full serial PASS（3242 passed, 4 skipped）；Desktop full serial PASS（476/476）；UI unit PASS（625/625）；Playwright `npoint-calibration-workbench.spec.ts` PASS（2/2）；calibration regression PASS（111/111）；desktop endpoints PASS（40/40）；measurement accuracy PASS（122/122）；Station Debug/Release build PASS；Desktop Release publish PASS；`git diff --check` PASS。
- 截图/Benchmark/Artifact：`test_results/g12b-playwright/g12b-npoint-happy-path.png`、`g12b-npoint-outlier-disabled.png`、`g12b-npoint-degenerate-failure.png`、`g12b-npoint-candidate-export-ready.png`；publish audit PASS；`test_results/calibration-all-20260704-104129.trx`。
- API / Project format / Runtime / Station / AgentRun 影响：新增 Desktop-only draft API；Project JSON/schema unchanged；Runtime Package unchanged；Station builds unchanged; AgentRun unchanged.
- 技术债与非阻断事项：完整 GitHub CI NOT RUN；真实 WebView2 NOT PERFORMED；未提交/未推送。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G13A`
