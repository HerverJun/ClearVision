# G12B：N 点标定工作台 draft 与可视化

> 阶段：Vertical Product
> 状态：`READY`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：完成九点采集、编辑、求解和 Scene 展示；正式资产保存留给 G13A。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/NPointCalibrationOperator.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Calibration/CalibrationBundleV2.cs`
- `docs/进行中/Studio2/goals/G09C.md`

## 本轮范围

[ ] CalibrationSession 仅作为 UI draft。
[ ] Sample：pixel/world/source/enabled/inlier/error/note。
[ ] 图像点击、上游 Point/Circle Center、世界坐标录入、九点模板、排序、排除/恢复。

## 明确不做

- 本轮实现 ProjectSave participant
- 真实 PLC/机器人 provider
- 平行 CalibrationArtifact

## 执行清单

[ ] 工作台通过 Application DTO 调 solver。
[ ] Scene 显示 sample、顺序、内外点、重投影和误差向量。
[ ] Inspector 显示 matrix/quality/frames/unit。
[ ] 提供导出候选 Bundle 但标记未正式保存。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] 九点 happy path、异常点、退化点、重算、工程切换丢弃 draft 测试。

## 完成条件

[ ] 九点全过程可在 Studio 完成到“候选 Bundle”。
[ ] draft 不冒充正式资产。

## 回填区

- 状态：`READY`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G13A`
