# G15.8：Project 页面 capability 迁移

> 阶段：Productization
> 状态：`DONE`
> 目标：迁移 Project 页面 的外壳和交互，同时保证 flag on 时旧实现完全不挂载；删除动作需独立证据门禁。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/project/projectManager.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/project`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/app.js`

## 本轮范围

[ ] 先盘点旧 owner、订阅、timer、写入口和合同。
[ ] V2 capability flag 实现与 parity。
[ ] 显式切换 active owner。

## 明确不做

- 同轮迁移其他 capability
- 仅用 CSS 隐藏旧实现
- 未经人工批准立即删除复杂旧实现

## 执行清单

[ ] 写行为清单和切换矩阵。
[ ] flag on：旧模块不 mounted、不订阅、不运行 timer、不持有资源。
[ ] 完成合同测试、Playwright、WebView2 人工验收。
[ ] 达到证据门禁后再执行删除；不能等待“时间窗口”，改用明确的 N 次 clean CI/人工批准。
[ ] 删除旧 CSS、临时 adapter 和 flag 后再次回归。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[ ] 该 capability 只有一个生产 owner。
[ ] 旧实现删除或明确保留底层库及理由。
[ ] 其他 capability 无回退。

## 回填区

- 状态：`DONE`
- 开始时间：`2026-07-04 22:53:57 +08:00`
- 完成时间：`2026-07-04 23:22:07 +08:00`
- Initial SHA：`2b8043c2b7ccfcb63f9d67a6261fdc33fa57a73e`
- Final SHA：提交自身 SHA 不写入 tracked 文件；最终报告写
- 远端 SHA：提交后最终报告写
- 修改文件：
  - `ClearVision.Product/src/ClearVision.Product.Desktop/Configuration/StudioOptions.cs`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/WebView2Host.cs`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/appsettings.json`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/app.js`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/project/projectPageCapabilityOwner.mjs`
  - `ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/unit/g15x-capability-owners.test.mjs`
  - `ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/Architecture/Studio2ArchitectureGuardTests.cs`
- 新增/变更契约：
  - 新增默认关闭 flag：`Studio2.ProjectPage` / `Studio:ProjectPageCapabilityEnabled=false`。
  - 新增 `ProjectPageCapabilityOwner` 与 `ProjectPageCapabilityAdapter`；Project DTO、Project file format、assets/calibration/evidence manifest 未改。
- 行为清单和切换矩阵：
  - all flags off：legacy `ProjectView` 唯一 owner。
  - `Studio2.ProjectPage=true`：只挂载 `ProjectPageCapabilityOwner`；list/search/create/open/save/import/export 经 adapter 或既有 global import/export functions。
  - `AiPanel on + ProjectPage on`：AI 不绕过 project authority；Project write entry 仍是 `projectManager.saveProject()`。
  - all G15 flags on：Project 页面只有 V2 owner、V2 adapter、一个 delegated DOM listener set。
- active owner / 唯一写入口：`ProjectPageCapabilityOwner -> ProjectPageCapabilityAdapter -> projectManager`，正式保存仍经 ProjectService / ProjectSaveCoordinator。
- Legacy mounted / subscription / timer 状态：flag on 时 legacy `ProjectView` 不构造、不 mounted、不绑定按钮事件、不写 project；flag off 继续原行为。
- flag 登记：owner `ProjectPageCapabilityOwner`；default `false`；off behavior legacy ProjectView；on behavior V2 owner；cutover G15.8；deletion goal `G16`。
- 测试命令与结果：
  - `node --check` touched JS/MJS：PASS。
  - focused UI：PASS（492/492）。
  - Desktop focused `WebView2HostTests,Studio2ArchitectureGuardTests`：PASS（33/33）。
  - `git diff --check`：PASS（仅 LF→CRLF normalization warnings）。
- 截图/Benchmark/Artifact：Playwright NOT RUN；真实 WebView2 NOT PERFORMED；screenshot NOT PERFORMED；benchmark NOT RUN；artifact NOT GENERATED。
- API / Project format / Runtime / Station / AgentRun 影响：Project format 未改；Application Service/ProjectSaveCoordinator authority 未改；Runtime/Station/AgentRun 未改。
- 技术债与非阻断事项：V2 Project page 是默认关闭 shell；legacy deletion、full parity visual QA、real WebView2 验收留至 G16。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G15.6`
