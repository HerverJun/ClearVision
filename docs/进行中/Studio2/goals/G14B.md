# G14B：结果对比、基线与 Scene 回放

> 阶段：Productization
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：在正式历史投影之上实现当前/历史对比和可解释回放。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `docs/进行中/Studio2/goals/G14A.md`
- `docs/进行中/Studio2/goals/G08.md`
- `ClearVision.Product/src/ClearVision.Product.Core/Entities/InspectionResult.cs`

## 本轮范围

[ ] 当前/历史、固定基线、失败前成功。
[ ] 结构化字段 diff。
[ ] 有 Scene evidence 时回放；没有时降级 annotated image/summary。

## 明确不做

- 重新执行历史流程作为默认回放
- 忽略 FlowHash/BundleId 差异
- 把所有 Artifact 永久入库

## 执行清单

[ ] 对比页明确版本不一致警告。
[ ] 结果 path 使用 G07 canonical 规则。
[ ] 大型列表分页。
[ ] 回放缺资产 fail-soft。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[ ] 对比可追踪版本差异。
[ ] 缺 Scene/图像仍可审计摘要。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G14C`
