# G05B：Preview Artifact 生命周期与安全读取

> 阶段：Observation
> 状态：`DONE`（G05B follow-up 已完成：Artifact 不可变性、批次原子性、取消回滚和前端资源回收缺口已收口；G06 暂未执行）
> 目标：把图像、Mask、剖面和大型点集在执行时物化为可回收 Artifact，而不是塞入主 JSON。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/Endpoints/PreviewNodeEndpoints.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Services/FlowExecutionService.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/ImageWrapper.cs`

## 本轮范围

[x] 建立明确 Desktop-scoped `PreviewArtifactStore`，不复用 `FlowExecutionService` debug cache。
[x] Artifact 在 `Mat`/`ImageWrapper` 释放前编码/复制为 immutable bytes。
[x] opaque ID、TTL、容量、checksum、content type、清理和取消。

## 明确不做

- 持有 live Mat 作为长期缓存
- 允许客户端提供任意相对/绝对路径
- 把 Artifact 默认放入 SSE

## 执行清单

[x] 记录选择独立 Desktop store 的 ADR。
[x] Artifact key 绑定 debugSessionId/request identity。
[x] 工程关闭、请求取消、TTL/容量淘汰时释放。
[x] 读取 endpoint 只接受 opaque artifactId 并做 owner/expiry 验证。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[x] 容量、TTL、并发读取、过期、路径注入、内存释放测试。

## 完成条件

[x] 无 OpenCv 生命周期悬挂。
[x] 主 JSON/SSE 不含大对象。
[x] 过期和取消后资源可回收。

## 回填区

- 状态：`DONE`
- 开始时间：2026-07-02
- 完成时间：2026-07-02
- Initial SHA：`1c8f1c25194d8e2d169ff710bfa3c604e262bf92`
- Final SHA：见 G05B follow-up 完成提交与最终报告（提交 SHA 不自嵌到同一提交内容中）
- 远端 SHA：见 G05B follow-up 完成提交与最终报告
- 修改文件：`ClearVision.Product/src/ClearVision.Product.Desktop/PreviewArtifacts/**`；`ClearVision.Product/src/ClearVision.Product.Desktop/Endpoints/PreviewNodeEndpoints.cs`；`ClearVision.Product/src/ClearVision.Product.Desktop/Endpoints/ApiEndpoints.cs`；`ClearVision.Product/src/ClearVision.Product.Desktop/Program.cs`；`ClearVision.Product/src/ClearVision.Product.Desktop/Observation/**`；`ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/messaging/httpClient.js`；`ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/previewCoordinator.js`；`ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/inspection/inspectionController.js`；`ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/**`；`ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/unit/preview-coordinator-memory.test.mjs`；`docs/进行中/Studio2/adr/**`；`TODO.md`
- 新增/变更契约：新增 `PreviewArtifactReferenceV1`、Desktop-only `PreviewArtifactStore`/`PreviewArtifactMaterializer`、`GET/DELETE /api/preview-artifacts/{artifactId}`；`PreviewNodeRequest.ArtifactMode = "references"` 为 additive 模式；legacy 未传 `ArtifactMode` 时保留 Base64 兼容。
- active owner / 唯一写入口：owner identity 为 `ProjectId/TargetNodeId/DebugSessionId/ClientRequestSequence/FlowRevision`；写入只在 preview-node materialization batch commit；前端只通过既有 `NodePreviewCoordinator` 读取和释放 artifact。
- Legacy mounted / subscription / timer 状态：旧入口未挂载新 Inspector，未新增第二 preview coordinator，未新增 SSE artifact，未执行 G06。
- 测试命令与结果：G05B 初次完成记录保留；follow-up 定向 Desktop tests PASS（60）；FlowExecutionService targeted PASS（13）；UI `test:unit` PASS（563）；`test:preview-smoke` PASS；Desktop Debug build PASS；Release publish PASS；build/publish V2 asset audit PASS；publish no Node/source/dev artifacts PASS；`git diff --check` PASS；GitHub CI NOT RUN；真实 WebView2 NOT PERFORMED。
- Follow-up 修复：`PreviewArtifactPendingEntry` 改为 assembly-internal 实现细节，public batch API 不再暴露 pending bytes；Store commit 先完整预检和容量规划，再替换 owner、淘汰旧项并插入整个新批次；commit 时正式 entry 再复制 bytes 并基于正式 bytes 计算 SHA-256；preview-node 成功/失败响应均在 commit 前检查 linked cancellation token，未 commit batch 由 finally rollback；`NodePreviewCoordinator` 使用 bundle-local 幂等释放，覆盖 loading/idle/error、uncached、live camera bypass、cache replacement/eviction、node switch、stale response、partial artifact read failure 和 destroy。
- 截图/Benchmark/Artifact：无截图或 benchmark；follow-up 发布审计使用 `.tmp/publish-check/g05b-followup` 临时目录，提交前清理。
- API / Project format / Runtime / Station / AgentRun 影响：新增 Desktop preview artifact blob API；不改变 Project format、Runtime Package、Station、AgentRun/EventStore、FlowExecutionService debug cache authority。
- 技术债与非阻断事项：legacy Base64 preview 兼容保留，登记为 G16 删除债；G06 Result Inspector、ResultPath、Scene、正式运行历史未实现。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G06`
