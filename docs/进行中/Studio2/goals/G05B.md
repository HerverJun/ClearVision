# G05B：Preview Artifact 生命周期与安全读取

> 阶段：Observation
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：把图像、Mask、剖面和大型点集在执行时物化为可回收 Artifact，而不是塞入主 JSON。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/Endpoints/PreviewNodeEndpoints.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Services/FlowExecutionService.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/ImageWrapper.cs`

## 本轮范围

[ ] 优先扩展现有 debug-session 生命周期，或建立明确 Desktop-scoped PreviewArtifactStore。
[ ] Artifact 在 Mat/ImageWrapper 释放前物化。
[ ] opaque ID、TTL、容量、checksum、content type、清理和取消。

## 明确不做

- 持有 live Mat 作为长期缓存
- 允许客户端提供任意相对/绝对路径
- 把 Artifact 默认放入 SSE

## 执行清单

[ ] 记录选择扩展现有 cache 还是独立 Desktop store 的 ADR。
[ ] Artifact key 绑定 debugSessionId/request identity。
[ ] 工程关闭、请求取消、TTL/容量淘汰时释放。
[ ] 读取 endpoint 只接受 opaque artifactId 并做 owner/expiry 验证。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] 容量、TTL、并发读取、过期、路径注入、内存释放测试。

## 完成条件

[ ] 无 OpenCv 生命周期悬挂。
[ ] 主 JSON/SSE 不含大对象。
[ ] 过期和取消后资源可回收。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G06`
