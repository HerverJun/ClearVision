# G12A：NPoint CalibrationSolver 抽取与 parity

> 阶段：Vertical Product
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：从现有 NPointCalibrationOperator 抽取纯求解器，保持输出和质量语义一致。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/NPointCalibrationOperator.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Calibration/CalibrationBundleV2.cs`
- `scripts/run-tests-calibration-regression.ps1`

## 本轮范围

[ ] Affine/Perspective、RANSAC、误差统计、acceptance 纯 solver。
[ ] 算子改为调用 solver。
[ ] DTO mapping 边界，前端不直接依赖 Infrastructure 类型。

## 明确不做

- 标定 UI
- 正式资产保存
- PLC/机器人 provider

## 执行清单

[ ] 为抽取前后建立 golden parity。
[ ] 保留 PointPairs 解析和旧 outputs。
[ ] CalibrationBundleV2 仍是唯一正式产物。
[ ] 异常矩阵/退化点集 fail closed。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] 运行 calibration regression、NPoint unit、旧契约审计。

## 完成条件

[ ] 抽取前后结果在容差内一致。
[ ] 无第二套 solver。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G12B`
