# G12A：NPoint CalibrationSolver 抽取与 parity

> 阶段：Vertical Product
> 状态：`DONE`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：从现有 NPointCalibrationOperator 抽取纯求解器，保持输出和质量语义一致。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/NPointCalibrationOperator.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Calibration/CalibrationBundleV2.cs`
- `scripts/run-tests-calibration-regression.ps1`

## 本轮范围

[x] Affine/Perspective、RANSAC、误差统计、acceptance 纯 solver。
[x] 算子改为调用 solver。
[x] DTO mapping 边界，前端不直接依赖 Infrastructure 类型。

## 明确不做

- 标定 UI
- 正式资产保存
- PLC/机器人 provider

## 执行清单

[x] 为抽取前后建立 golden parity。
[x] 保留 PointPairs 解析和旧 outputs。
[x] CalibrationBundleV2 仍是唯一正式产物。
[x] 异常矩阵/退化点集 fail closed。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[x] 运行 calibration regression、NPoint unit、旧契约审计。

## 完成条件

[x] 抽取前后结果在容差内一致。
[x] 无第二套 solver。

## 回填区

- 状态：`DONE`
- 开始时间：`2026-07-04 03:27:18 +08:00`
- 完成时间：`2026-07-04 03:54:15 +08:00`
- Initial SHA：`d2d247a30b4ff34fbc09832976dd2428e66e5275`
- Final SHA：提交自身 SHA 不写入 tracked 文件；以 push 后核对值为准
- 远端 SHA：提交自身 SHA 不写入 tracked 文件；以 push 后核对值为准
- 修改文件：`NPointCalibrationOperator.cs`、`NPointCalibrationSolver.cs`、`NPointCalibrationSolverTests.cs`、本卡与 `TODO.md`；`G12B.md` 仅状态解锁为 READY。
- 新增/变更契约：新增唯一纯 `NPointCalibrationSolver` request/options/result 边界；solver 生成唯一正式 `CalibrationBundleV2`，覆盖 Affine/Perspective、RANSAC、误差统计、acceptance、ProducerOperator 与退化/fail-closed 诊断；operator 保留旧 `PointPairs` JSON/list 解析和旧 output key。
- active owner / 唯一写入口：`NPointCalibrationSolver` 是唯一求解路径；`NPointCalibrationOperator` 仅负责参数适配、solver 调用、旧 outputs 映射、可选 `SavePath` 写入和图像标注输出。
- Legacy mounted / subscription / timer 状态：无 UI mounted/subscription/timer 变更；旧 `PointPairs`、disabled samples、legacy output dictionary、`CalibrationBundleV2` JSON 保存契约保留。
- 测试命令与结果：`dotnet build ClearVision.Product/tests/ClearVision.Product.Tests/ClearVision.Product.Tests.csproj --no-restore -v:minimal` PASS；`& "./scripts/run-dotnet-test-serial.ps1" -Project "ClearVision.Product/tests/ClearVision.Product.Tests/ClearVision.Product.Tests.csproj" -FullyQualifiedName NPointCalibrationOperatorTests,NPointCalibrationSolverTests -NoBuild -NoRestore -Verbosity normal` PASS（21/21）；`& "./scripts/run-tests-calibration-regression.ps1" -Gate all -NoBuild -NoRestore -Verbosity normal` PASS（111/111）；`git diff --check` PASS。
- 截图/Benchmark/Artifact：TRX `test_results/calibration-all-20260704-035243.trx`；截图 NOT RUN；新增 benchmark NOT RUN。
- API / Project format / Runtime / Station / AgentRun 影响：无前端/Application 直接依赖 Infrastructure 内部 solver；无 Project format migration；Runtime/Station/AgentRun 未修改；正式资产保存未实现。
- 技术债与非阻断事项：完整 GitHub CI：NOT RUN；真实 WebView2：NOT PERFORMED；G12B 未在本提交启动，保持 READY。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G12B`
