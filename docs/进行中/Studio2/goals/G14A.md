# G14A：正式 Inspection 历史投影与分页

> 阶段：Productization
> 状态：`DONE`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：基于现有 InspectionResult/Repository 建立正式运行历史，不混入 preview debug cache。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Core/Entities/InspectionResult.cs`
- `ClearVision.Product/src/ClearVision.Product.Application/Services/InspectionService.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Repositories/InspectionResultRepository.cs`

## 本轮范围

[x] 分页列表、详情、图像引用、AnalysisData/OutputData 安全展示。
[x] FlowVersionHash、CalibrationBundleId、SessionId traceability。
[x] 缺图仍可看摘要。

## 明确不做

- 节点 preview 历史持久化
- 比较/回放
- 证据 ZIP

## 执行清单

[x] 先区分正式 Inspection history 与临时 preview history。
[x] 大字段按需加载。
[x] 旧数据库行缺 traceability 兼容。
[x] 大量历史虚拟化。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[x] 分页、空/缺图、旧数据、权限和大数据量测试。

## 完成条件

[x] 历史不一次性载入内存。
[x] 不同 Flow/Bundle 不混淆。

## 回填区

- 状态：`DONE`
- 开始时间：`2026-07-04 16:26:03 +08:00`
- 完成时间：`2026-07-04 16:53:57 +08:00`
- Initial SHA：`8e2237c1892d4b7747866cf351856a267b481122`
- Final SHA：提交自身 SHA 不写入 tracked 文件；以 push 后核对值为准
- 远端 SHA：提交后以最终报告核对值为准
- 修改文件：
  - `ClearVision.Product/src/ClearVision.Product.Application/Analysis/SafeJsonPreviewBuilder.cs`
  - `ClearVision.Product/src/ClearVision.Product.Application/Queries/Inspections/GetInspectionHistoryQuery.cs`
  - `ClearVision.Product/src/ClearVision.Product.Application/Services/InspectionService.cs`
  - `ClearVision.Product/src/ClearVision.Product.Core/Interfaces/IInspectionResultRepository.cs`
  - `ClearVision.Product/src/ClearVision.Product.Core/Services/IInspectionService.cs`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/Endpoints/ApiEndpoints.cs`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/app.js`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/results/resultPanel.js`
  - `ClearVision.Product/src/ClearVision.Product.Infrastructure/Repositories/InspectionResultRepository.cs`
  - `ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/ApiEndpointsInspectionHistoryTests.cs`
  - `ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/Architecture/Studio2ArchitectureGuardTests.cs`
  - `ClearVision.Product/tests/ClearVision.Product.Tests/Queries/GetInspectionHistoryQueryHandlerTests.cs`
  - `ClearVision.Product/tests/ClearVision.Product.Tests/Repositories/InspectionResultRepositoryTests.cs`
  - `ClearVision.Product/tests/ClearVision.Product.Tests/Services/InspectionResultBackgroundServiceTests.cs`
  - `ClearVision.Product/tests/ClearVision.Product.Tests/Services/InspectionServiceSingleRunTests.cs`
  - `ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/unit/result-panel-memory.test.mjs`
  - `TODO.md`
  - `docs/进行中/Studio2/goals/G14A.md`
- 新增/变更契约：
  - `GET /api/inspection/history/{projectId}` 保持 server-side paging，默认 `InspectionTime desc, Id desc` 稳定排序，`pageSize` clamp 到 200，新增 `flowVersionHash` 过滤；列表只返回轻量字段、traceability 与 `hasOutputData`/`hasAnalysisData`/`hasImage` 标志，不返回完整 OutputData/AnalysisData/图像。
  - 新增 `GET /api/inspection/history/{projectId}/{resultId}`，按 projectId/resultId 读取正式 `InspectionResult` 详情，返回 safe JSON preview、图像引用、缺图提示、traceability 与 diagnostics 摘要。
  - 新增 `SafeJsonPreviewBuilder`，对详情 JSON 进行 depth/object/array/string 上限、secret-like key/value 脱敏、本机绝对路径脱敏、图像/Scene/artifact payload 省略。
  - 单次正式检测结果补齐 `FlowVersionHash`、`CalibrationBundleId`、`SessionId` traceability；旧行字段 nullable，UI 显示“旧数据未记录”。
- active owner / 唯一写入口：正式 history 来源为 `InspectionResult` / `InspectionResultRepository` / `InspectionService`；列表和详情唯一 Desktop API 入口为 `/api/inspection/history/{projectId}` 与 `/api/inspection/history/{projectId}/{resultId}`；前端唯一结果历史 owner 仍为 `ResultPanel`，详情按需经既有 `httpClient` 加载。
- Legacy mounted / subscription / timer 状态：未修改 G13X `operatorResultViewModel.mjs`、`previewPanel.js` 或 PropertyPanel preview mount；未新增 Preview history persistence、ImageCanvas、Scene renderer、subscription 或 timer；既有 result panel refresh timer 继续用于 server-paged history refresh。
- 测试命令与结果：
  - `& "./scripts/dotnet.ps1" build ClearVision.Product/tests/ClearVision.Product.Tests/ClearVision.Product.Tests.csproj --no-restore --verbosity minimal`：PASS（0 warnings, 0 errors）
  - `& "./scripts/run-dotnet-test-serial.ps1" -Project "ClearVision.Product/tests/ClearVision.Product.Tests/ClearVision.Product.Tests.csproj" -FullyQualifiedName "InspectionResultRepositoryTests","GetInspectionHistoryQueryHandlerTests","InspectionServiceSingleRunTests" -NoBuild -NoRestore`：PASS（19/19）
  - `& "./scripts/dotnet.ps1" build ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/ClearVision.Product.Desktop.Tests.csproj --no-restore --verbosity minimal`：PASS（0 warnings, 0 errors）
  - `& "./scripts/run-dotnet-test-serial.ps1" -Project "ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/ClearVision.Product.Desktop.Tests.csproj" -FullyQualifiedName "ApiEndpointsInspectionHistoryTests","Studio2ArchitectureGuardTests" -NoBuild -NoRestore`：PASS（26/26）
  - `node --test tests/unit/result-panel-memory.test.mjs`（cwd `ClearVision.Product/tests/ClearVision.Product.UI.Tests`）：PASS（12/12）
  - `git diff --check`：PASS
  - Product full serial：NOT RUN
  - Desktop full serial：NOT RUN
  - UI full unit：NOT RUN
  - GitHub CI：NOT RUN
  - 真实 WebView2：NOT PERFORMED
  - Release publish：NOT RUN
- 截图/Benchmark/Artifact：截图 NOT RUN；Benchmark NOT RUN；新增正式 Artifact NONE。
- API / Project format / Runtime / Station / AgentRun 影响：API 新增 history detail endpoint、history list 变为 light contract；Project format NONE；EF/schema migration NONE（traceability nullable fields already existed）；Runtime NONE；Station NONE；AgentRun/EventStore NONE；Studio Preview/G13X NONE。
- 技术债与非阻断事项：未做 G14B comparison/replay/baseline；未做 G14C Evidence ZIP/retention；真实图片缓存缺失只能在无 `ImageId`/引用缺失时由详情提示，实际 `/api/images/{id}` 404 仍由现有图像接口表现；完整 CI 未运行。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G14B`
