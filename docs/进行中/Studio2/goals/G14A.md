# G14A：正式 Inspection 历史投影与分页

> 阶段：Productization
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：基于现有 InspectionResult/Repository 建立正式运行历史，不混入 preview debug cache。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Core/Entities/InspectionResult.cs`
- `ClearVision.Product/src/ClearVision.Product.Application/Services/InspectionService.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Repositories/InspectionResultRepository.cs`

## 本轮范围

[ ] 分页列表、详情、图像引用、AnalysisData/OutputData 安全展示。
[ ] FlowVersionHash、CalibrationBundleId、SessionId traceability。
[ ] 缺图仍可看摘要。

## 明确不做

- 节点 preview 历史持久化
- 比较/回放
- 证据 ZIP

## 执行清单

[ ] 先区分正式 Inspection history 与临时 preview history。
[ ] 大字段按需加载。
[ ] 旧数据库行缺 traceability 兼容。
[ ] 大量历史虚拟化。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] 分页、空/缺图、旧数据、权限和大数据量测试。

## 完成条件

[ ] 历史不一次性载入内存。
[ ] 不同 Flow/Bundle 不混淆。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G14B`
