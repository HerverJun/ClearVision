# G08：Visual Scene V1（只读投影）

> 阶段：Observation
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：基于现有 ImageCanvas 建立结构化只读 Scene，不创建第二图像渲染内核。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/imageCanvas.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/roiEditorPanel.js`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/CircleMeasurementOperator.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/NPointCalibrationOperator.cs`

## 本轮范围

[ ] 版本化 scene DTO。
[ ] 扩展/适配现有 ImageCanvas renderer。
[ ] deterministic primitiveId、layer、z-order、visibility、selection、resultPath mapping。

## 明确不做

- 新建第二 Canvas2D 图像查看器
- 持久化历史 Scene
- 本轮实现几何编辑

## 执行清单

[ ] 当前 ImageCanvas 随机 overlay id 改为允许外部稳定 ID。
[ ] 首批 adapter：ROI、CircleMeasurement、NPoint 可用结果。
[ ] 保留 annotated PNG 切换。
[ ] Unknown primitive 和 scene render failure fail-soft。
[ ] 建立 renderer command golden。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] 300 primitive 基线测试并记录硬件、p95 frame time、内存。

## 完成条件

[ ] 字段与 primitive 双向定位。
[ ] 节点切换不串 Scene。
[ ] Scene 异常不改变算子成功状态。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G09A`
