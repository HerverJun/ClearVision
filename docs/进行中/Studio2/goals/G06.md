# G06：节点预览结果检查器 MVP

> 阶段：Observation
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：替换当前节点预览详情的展示能力；不把正式 Inspection 历史结果页一并重做。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/previewCoordinator.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/nodePreviewOverlay.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/Endpoints/PreviewNodeEndpoints.cs`

## 本轮范围

[ ] Summary/Detail/Artifact UI。
[ ] 安全树、搜索、copy value/path、分页/虚拟列表。
[ ] 字段选择写入 selectionStore。

## 明确不做

- 迁移正式 ResultPanel/history
- 把无限对象 JSON.stringify 到 DOM
- 新建结果业务权威 Store

## 执行清单

[ ] Renderer 覆盖 scalar、Point、Circle、Line、Rectangle、Matrix、Detection List、Calibration Quality。
[ ] Unknown renderer 必须有大小上限。
[ ] 四类状态：loading/success/error/empty。
[ ] flag on 时只停用对应旧 node-preview owner，不停用历史结果页。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] Playwright 覆盖选节点、快速切换、工程切换、Artifact 过期。

## 完成条件

[ ] 安全可序列化输出可查看。
[ ] 大数组不阻塞主线程。
[ ] 新旧 node-preview owner 不双挂载。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G07A`
