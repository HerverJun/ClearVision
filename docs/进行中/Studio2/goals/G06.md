# G06：节点预览结果检查器 MVP

> 阶段：Observation
> 状态：`DONE`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：替换当前节点预览详情的展示能力；不把正式 Inspection 历史结果页一并重做。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/previewCoordinator.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/nodePreviewOverlay.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/Endpoints/PreviewNodeEndpoints.cs`

## 本轮范围

[x] Summary/Detail/Artifact UI。
[x] 安全树、搜索、copy value/path、分页/虚拟列表。
[x] 字段选择写入 selectionStore。

## 明确不做

- 迁移正式 ResultPanel/history
- 把无限对象 JSON.stringify 到 DOM
- 新建结果业务权威 Store

## 执行清单

[x] Renderer 覆盖 scalar、Point、Circle、Line、Rectangle、Matrix、Detection List、Calibration Quality。
[x] Unknown renderer 必须有大小上限。
[x] 四类状态：loading/success/error/empty。
[x] flag on 时只停用对应旧 node-preview owner，不停用历史结果页。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[x] Playwright 覆盖选节点、快速切换、工程切换、Artifact 过期。

## 完成条件

[x] 安全可序列化输出可查看。
[x] 大数组不阻塞主线程。
[x] 新旧 node-preview owner 不双挂载。

## 回填区

- 状态：`DONE`
- 开始时间：2026-07-02 17:50:50 +08:00
- 完成时间：2026-07-02 18:17:32 +08:00
- Initial SHA：`414dc70d7644326140e97e966af976a6f54413bd`
- Final SHA：见 G06 follow-up 完成提交与最终报告
- 远端 SHA：见 G06 follow-up 完成提交与最终报告
- 修改文件：`WebView2Host.cs`、`wwwroot/src/app.js`、`nodePreviewInspector.js`、`WebView2HostTests.cs`、`Studio2ArchitectureGuardTests.cs`、`node-preview-inspector.test.mjs`、`node-preview.spec.ts`、`TODO.md`、Studio2 ADR 与本卡。
- 新增/变更契约：`Studio:NodePreviewInspectorEnabled` 只以 frozen `window.__CLEARVISION_STARTUP__.featureFlags["Studio:NodePreviewInspectorEnabled"]` 为 owner 决策来源；顶层 alias 仅诊断；新增 Artifact 文本预览上限 `MAX_ARTIFACT_TEXT_PREVIEW_BYTES=64KB` 与 `MAX_ARTIFACT_TEXT_DISPLAY_CHARS=4096`。
- active owner / 唯一写入口：flag off 只创建唯一 `NodePreviewCoordinator` 与 legacy `NodePreviewOverlay`，不创建/注册 Inspector 或 selectionStore；flag on 创建唯一 `NodePreviewInspector` 与 capability-local `nodePreviewSelectionStore`，两者仍复用同一个 coordinator。
- Legacy mounted / subscription / timer 状态：flag on 时 legacy Overlay 不构造、不注册、不订阅、不运行 timer；flag off 时 Inspector/selectionStore 未注册，legacy global accessor 查询为未注册状态。
- 测试命令与结果：`node --test tests/unit/preview-coordinator-memory.test.mjs tests/unit/node-preview-inspector.test.mjs` PASS（25 tests）；`npx playwright test tests/e2e/node-preview.spec.ts --reporter=list` PASS（9 tests：legacy 6，Inspector 3）；`& "./scripts/run-dotnet-test-serial.ps1" -Project "ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/ClearVision.Product.Desktop.Tests.csproj" -FullyQualifiedName WebView2HostTests,Studio2ArchitectureGuardTests,ExecutionObservationProjectorTests,PreviewArtifactStoreTests,PreviewNodeEndpointsTests` PASS（74 tests）；`npm run test:unit` PASS（575 tests）；`npm run test:preview-smoke` PASS；Desktop Debug build PASS；Release publish PASS；build/publish V2 asset audit PASS；publish no Node/source/dev artifacts PASS；`git diff --check` PASS；changed-file secret/large-file scans PASS。
- 截图/Benchmark/Artifact：Playwright 覆盖 flag off/on owner、flag mutation、快速节点切换 stale、工程切换、Artifact 404、大文本 Artifact no-GET 与大树 DOM 行数；未生成截图；未做 benchmark。
- API / Project format / Runtime / Station / AgentRun 影响：无 API endpoint、Project format、Runtime、Station、AgentRun 变更；未新增 Preview/Artifact endpoint；不实现 ResultPath parser，不迁移正式 history。
- 技术债与非阻断事项：完整 GitHub CI NOT RUN；真实 WebView2：NOT PERFORMED；G15.2/G16 负责 Preview Panel cutover 与 flag 删除。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G07A`
