# G13C：Station/Runtime 标定加载与 PixelToWorld E2E

> 阶段：Vertical Product
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：验证 Station 独立加载正式 Bundle 并执行坐标转换，默认仍只返回摘要。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Runtime`
- `ClearVision.Product/src/ClearVision.Product.Station`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/PixelToWorldTransformOperator.cs`

## 本轮范围

[ ] Package load、bundle validation、PixelToWorld。
[ ] Station default summary-only。
[ ] Studio save→restart→export→Station load E2E。

## 明确不做

- 让 Station 依赖 Vue/Node/Studio
- 默认上传 Scene/图像
- 远程部署新功能扩张

## 执行清单

[ ] 无 Studio 环境运行。
[ ] 验证 FlowHash、CalibrationBundleId 和坐标结果。
[ ] 缺/坏 Bundle 明确失败。
[ ] 旧 package 仍运行。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[ ] 纵向闭环可复现。
[ ] Station 独立。
[ ] 默认不传完整图像/Scene。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G14A`
