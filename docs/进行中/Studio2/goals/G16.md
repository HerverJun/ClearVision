# G16：性能、发布与唯一入口收口

> 阶段：Release
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：在所有 capability cutover 后，把 Studio V2 收敛为唯一生产 composition root。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/app.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/WebView2Host.cs`
- `ClearVision.Product/src/ClearVision.Product.Desktop/ClearVision.Product.Desktop.csproj`
- `.github/workflows/ci.yml`

## 本轮范围

[ ] benchmark、DPI/分辨率、生命周期、clean clone build/publish、无 Node 目标机。
[ ] 删除 dead code/过期 adapter/flag。
[ ] 旧底层 JS 只作为无副作用 importable library 保留。

## 明确不做

- 在 G15 未完成时切默认入口
- 把旧 app.js 留作第二业务 composition root
- 凭主观“流畅”通过性能门禁

## 执行清单

[ ] 先记录基线硬件、分辨率、primitive 数、preview/SSE 频率、p95 frame time 和内存。
[ ] 性能门禁采用相对基线/已批准预算；300/1000 primitive 场景均有数据。
[ ] 验证 100/125/150/200% DPI 和 1366/1080/1440/4K。
[ ] clean clone frontend build→dotnet build→publish。
[ ] 无 Node 目标机离线启动。
[ ] 旧工程/旧 package/Station/Agent/Project save 全回归。
[ ] 通过 PR checks 或 workflow_dispatch 验证完整 CI；普通 `codex初稿` push 不得冒充 full CI。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[ ] Studio V2 是唯一生产前端入口。
[ ] 发布包无 Node 运行依赖。
[ ] 兼容、性能、生命周期、纵向业务链路全部通过。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`RELEASE`
