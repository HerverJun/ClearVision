# G16：性能、发布与唯一入口收口

> 阶段：Release
> 状态：`BLOCKED`（当前 verdict：`BLOCKED_RELEASE_EVIDENCE_GAP`）
> 目标：以当前可交付 UI 栈收敛唯一 production root、owner/flag 处置和完整 release evidence。
>
> 2026-08-28 当前性纠偏（覆盖下方历史回填中的旧 cutover 目标）：FrontendV2 源码仍只有不完整 Workspace Shell，Tool/Review 等业务 capability 未形成产品 parity；Wave 0 已删除 `Studio:WorkspaceV2Enabled` 及 production build/publish 路径，并让 `/v2` 固定返回 404。本次 release 以 `wwwroot/index.html + app.js + capability owners` 为唯一 production root。未来 Vue/StudioUI 迁移必须另立完整 parity/migration epic，不属于本卡收尾。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/app.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/WebView2Host.cs`
- `ClearVision.Product/src/ClearVision.Product.Desktop/ClearVision.Product.Desktop.csproj`
- `.github/workflows/ci.yml`

## 本轮范围

[ ] 当前 production root 的批准 DPI/分辨率、真实 input-to-paint 性能和真实 no-Node 目标机仍缺；本机 clean clone、局部 WebView2 与 CPU/layout signal 已完成。
[x] Settings/Inspection/AI 的不完整实验 owner、adapter、双重门禁和 flag 已删除；各自 legacy 实现是唯一 production owner。
[x] FrontendV2 production flag/build/publish 路径已退役；源码仅作为项目文件和 release Gate 均不消费的 non-production 资产。
[x] 无 owner 的实验代码、adapter/flag 已删除；仍承担 production 行为的 legacy library 保留 lazy import 与显式 lifecycle cleanup。

## 明确不做

- 直接把不具产品 parity 的 `/v2` 切成默认入口
- 在 G16 内偷渡 Vue/StudioUI 全量迁移
- 为 non-production FrontendV2 补 production coverage 或把缓存分支事实写进当前验收
- 凭主观“流畅”通过性能门禁

## 执行清单

[x] 冻结唯一 production root 为 `wwwroot/index.html + app.js + capability owners`，并用 host/config/publish architecture guard 防止 `/v2` 误切。
[x] Settings、Inspection、AI 均完成实验路径删除，legacy mounted owner、配置与测试一致；Property/Preview/Globals/Project/Results owner 继续保留。
[x] 当前 UI 的 capability owner/legacy replacement matrix 已按 production `wwwroot` 重写，并有既有 unit/Playwright 与本机 WebView2 runtime smoke 证据；这不等于批准的 DPI/性能矩阵。
[x] Node `run-g16-release-benchmark.mjs` 已只作为 component CPU/layout bookkeeping signal 记录，未冒充 DOM/SVG/RAF/paint 或真实 WebView2 frame-time 发布证据。
[ ] 在最终 publish 的当前 production root + 真实 WebView2 记录 runtime/OS/GPU/硬件、warmup/sample、primitive 数、preview/SSE 频率、input-to-paint、RAF long-frame、p95 和 working set；性能门禁采用相对基线/已批准预算，300/1000 primitive 均有数据。
[ ] 对 `1366x768`、`1920x1080`、`2560x1440`、`3840x2160` 与 100/125/150/200% OS scale 建立批准组合矩阵；未执行组合不得用显示探针或模拟 mutation 代替。
[x] 最终 implementation SHA 的本机临时 clean clone 已完成 locked restore、Release build、RID locked restore、publish hygiene 与 canonical portable packaging；它不是远端 clean-clone CI。
[ ] 无 Node 目标机离线启动。
[ ] 旧工程/旧 package/Station/Agent/Project save 全回归。
[ ] 通过 PR checks 或 workflow_dispatch 验证完整 CI；普通 `codex初稿` push 不得冒充 full CI。

## 验证清单

[x] Wave 0 implementation 与文档 diff 均执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目按仓库串行测试脚本合并执行，未并行启动同一 csproj。
[x] 测试结束确认无仓库/Playwright 关联的 `dotnet`、`testhost`、Node/http-server 或浏览器服务进程。
[x] Wave 0 回填本卡和统一 TODO，并拆分 implementation/documentation 两个本地提交；按本轮要求不 push。G16 最终 release 仍需同 SHA 远端证据。

## 完成条件

[x] 当前 production root 唯一且不可被 non-production `/v2` 配置误切；未来 UI 迁移已从本卡解耦。
[x] 当前 disposition 中每个 capability 只有一个 mounted production owner，保留 flag 与回归证据一致。
[x] 当前工作树本地 Release publish 的静态资产不含 `wwwroot/v2`、FrontendV2、Node runtime、`node_modules` 或 package manifest/lockfile。
[ ] 最终 release candidate 在真实无 Node 目标机完成离线启动。
[ ] 兼容、真实 WebView2 性能、生命周期、纵向业务链路全部通过；Node 微基准只作辅助信号。

## 2026-08-28 Wave 0 当前 disposition

- 状态：`BLOCKED`
- 当前 verdict：`BLOCKED_RELEASE_EVIDENCE_GAP`；`BLOCKED_OWNER_DISPOSITION_GAP` 已解除。
- Wave 0 implementation/evidence SHA：`1e2342c3909cb1f157d902aef1338e92f1ac44a3`。
- 唯一 production root：`wwwroot/index.html + app.js + current capability owners`。`StudioOptions` 不再含 `WorkspaceV2Enabled`，startup resolver 固定 `/index.html`，Host 不再注入 V2 root 配置，`Program` 对 `/v2` 返回 404，Desktop project/CI 不再 build/copy/publish FrontendV2。
- effective owner：
  - Property、Preview、GlobalVariables、Project、ResultsReview：保留各自 capability owner 与唯一 startup flag snapshot。
  - Settings：legacy `SettingsView`；实验 `SettingsCapabilityOwner`、adapter、服务端 flag 与客户端 experimental global 已删除。
  - Inspection：legacy `InspectionPanel`；实验 `InspectionCapabilityOwner`、adapter、服务端 flag 与客户端 experimental global 已删除。
  - AI：legacy `AiPanel`；实验 `AiPanelCapabilityOwner`、adapter、服务端 flag 与客户端 experimental global 已删除。
- lifecycle：Settings `destroy()` 清 tracked timeout/modal；Inspection `dispose()` 取消 completion/error subscription；AI `dispose()` 清 subscriptions、EventSource/transports、timers 与 animation frame。
- 当前验证：Desktop focused `62/62` PASS；UI owner/lifecycle focused `10/10` PASS；UI 完整 unit lane `988/988` PASS。RuntimePreview Pilot 契约测试已改为读取归档路径并明确验证 archived/superseded historical gate，不恢复旧 active 计划。
- `Playwright`：7 个受影响 spec 已实际执行。首次 full run 为 `67 passed / 6 failed`；修正 PLC sole-owner 旧断言后 PLC 精确 `1/1` PASS，最终非视觉功能/布局回归 `68/68` PASS。剩余 5 个 AI visual baseline 在当前 Chromium 环境为字体抗锯齿像素差异（约 1%–4%），未覆盖治理快照，因此 Playwright full Gate 仍不绿并保留 release blocker。
- 当前工作树本地 Release publish：PASS（198 files / 332,738,267 bytes）；`wwwroot/index.html` 与 `wwwroot/src/app.js` 存在，`wwwroot/v2`、FrontendV2、Node runtime、`node_modules`、package manifest/lockfile 均为 0，临时输出已清理。
- clean clone build/publish：`NOT PERFORMED`；上述本地 publish 不能替代 clean clone 或最终 release candidate 证据。
- 真实 WebView2：`NOT PERFORMED`。
- DPI/分辨率批准矩阵：`NOT PERFORMED`。
- no-Node 目标机离线启动：`NOT PERFORMED`。
- 最终 publish 的 300/1000 primitive WebView2 性能：`NOT PERFORMED`。
- 旧工程/旧 package/Station/Agent/Project save 完整纵向回归：`NOT PERFORMED`；本轮只完成 Wave 0 Product/Desktop focused acceptance。
- 同 SHA GitHub CI：`NOT RUN`。

## 2026-09-01 Wave 3C 本机 Release 证据

- 最终 implementation/integration SHA：`fc1f11bc605a8c9b4e16ba78af4de62457b27802`；后续 canonical 计划回填使用独立 docs-only 提交。未 push，未触发 tag Release。
- 唯一 packaging：`scripts/package-portable-deployment.ps1` 是 Studio/Station wrapper、本地 review 与 tag workflow 的 canonical implementation；正式 profile 固定为 `Studio / win-x64 / field-self-contained`，framework-dependent 仅为不同命名的 diagnostic profile。PR/main raw artifact 明确为 diagnostic。
- 正式本机候选：`ClearVision-Studio-1.0.3-wave3c.4-fc1f11bc-win-x64-field-self-contained.zip`，940 个 archive files、938 个 content-manifest governed files、`291,339,424` bytes、SHA-256 `acd41ab7838792c3ebe246c953beb3a6ef721f3a274b8ccf0e5ec2bd3ce18320`。同工作树第二次 ZIP SHA 因容器时间戳为 `cfe69a68d967ea946361626984580e76dd4fe679d1365a9322086d3fae698df3`，但两次 content fingerprint 均为 `sha256:e2fdae17d7dc3e7199151e26e4acd1883064347a6abe7b5978d04a5655bc3903`，content manifest SHA-256 均为 `0e8f41f6d74c27195601609f0982d3bdbac25d77c18b2c2ef68f238ae5a22488`。
- package boundary：OperatorLibrary locked restore/build/pack/installed smoke `47/47` PASS，package-public 固定为 156；正式 `.nupkg` 为 `1,422,413` bytes、SHA-256 `67c1332189bdf52eae2551c9d2defd0220b606d8e092acee6f2fa4962165faa2`。Desktop 发布纵向回归 `193/193`、AuthService `27/27` PASS。
- 供应链：从本轮最终 ZIP/nupkg 实际依赖生成 SPDX JSON、THIRD-PARTY-NOTICES、dependency report JSON/Markdown、identity manifest 和 checksums；SBOM/report 均为 79 个组件，结构与实际 artifact 一致。正式包不捆绑本机 ignored Huaray SDK，站点 camera SDK 仍是单独批准/部署的 prerequisite。
- 策略 disposition：`releaseEligible=false`。AutoMapper 15.1.3 与 Microsoft.Data.SqlClient.SNI.runtime 5.2.0 是未批准许可证；BCrypt.Net-Next 4.0.3、HslCommunication 12.7.0、S7NetPlus 0.20.0 为 `NOASSERTION`；漏洞源 unavailable，漏洞数量保持 `null`。S7NetPlus 没有伪造 Owner、批准依据或 expiry。
- clean clone：本机 `git clone --no-local` 的初始/最终 HEAD 均为 `fc1f11bc...` 且 Git 状态 clean；solution locked restore、Release build（12 个既有 warning / 0 error）、RID locked restore、Desktop publish hygiene（935 files）与 canonical package/self-test PASS。深目录 repo-local NuGet cache 首次受 Windows 长路径影响，改用显式共享 packages cache 后重跑通过。此结论是 local clean clone evidence，不是 GitHub CI。
- package repeat 边界：clean clone 也生成 940/938 文件的结构一致包；其 content fingerprint 因 checkout 字节进入两个 deterministic managed assemblies 与 packaging implementation byte hash而与主工作树不同。本轮要求的“两次打包 manifest/content hash 稳定”由同一工作树两次正式候选满足；未宣称跨机器逐字节 reproducible build。
- WebView2：本机 Runtime smoke `4/4` PASS（100% full、100% reopen、125% layout、150% layout），证据为 `hostKind=desktop-webview2` 且 `window.chrome.webview=true`。它不是 100/125/150/200% OS scale × 四分辨率批准矩阵，也没有 input-to-paint/working-set 结论。
- 300/1000 primitive：Node 24.11.0、i9-13900H、100 warmup / 1000 samples；300 的 p95 frame/update 为 `0.072/0.051 ms`，1000 为 `0.213/0.191 ms`。仅为 JS CPU/layout bookkeeping signal。
- no-Node：最终 ZIP 经从正常 PATH 中移除全部可解析 Node 目录的受限 PATH 模拟，`where node` 不可解析，`Launch-ClearVision.cmd` 启动包进程且 `/health=Healthy`，记为 `PASS_RESTRICTED_PATH_SIMULATION`；不是实际 no-Node 目标机。
- 仍阻断 G16：真实 no-Node 目标机、批准 DPI/分辨率矩阵、真实 WebView2 input-to-paint/RAF long-frame/p95/working set、现场设备/模型与性能、完整纵向现场回归、同 SHA GitHub necessary checks 和真实 GitHub Release。状态保持 `BLOCKED_RELEASE_EVIDENCE_GAP`。

## 2026-07 历史回填区

- 状态：`BLOCKED`
- 开始时间：2026-07-04T16:00:00Z
- 完成时间：2026-07-04T16:20:00Z
- Initial SHA：`dc2c618f3b48a369d3de6c4aabf9ef5ceb61acce`
- Final SHA：提交自身 SHA 不写入 tracked 文件；以 push 后核对值为准
- 远端 SHA：提交前 `origin/codex初稿` / GitHub 远端均为 `dc2c618f3b48a369d3de6c4aabf9ef5ceb61acce`；push 后以最终核对值为准
- 修改文件：
  - `ClearVision.Product/src/ClearVision.Product.Desktop/appsettings.json`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/app.js`
  - `ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/Architecture/Studio2ArchitectureGuardTests.cs`
  - `ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/unit/node-preview-inspector.test.mjs`
  - `ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/unit/preview-panel-memory.test.mjs`
  - `ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/unit/property-panel-capability-owner.test.mjs`
  - `scripts/run-g16-release-benchmark.mjs`
  - `docs/进行中/Studio2/architecture/Studio2-capability-迁移台账.md`
  - `docs/进行中/Studio2/release/G16-发布收口报告-2026-07-04.md`
  - `docs/进行中/Studio2/release/artifacts/G16-benchmark-2026-07-04.{json,md}`（ignored path，提交时 force-add）
- 新增/变更契约：
  - `Studio:PropertyPanelCapabilityEnabled`、`Studio:PreviewPanelCapabilityEnabled`、`Studio:GlobalVariablesCapabilityEnabled`、`Studio:SettingsCapabilityEnabled`、`Studio:ProjectPageCapabilityEnabled`、`Studio:InspectionCapabilityEnabled`、`Studio:ResultsReviewCapabilityEnabled`、`Studio:AiPanelCapabilityEnabled` 生产默认值切为 `true`。
  - `Studio:WorkspaceV2Enabled` 保持 `false`；`app.js` 仍是 release 阶段 root，不声明真正 `/v2` production root 完成。
  - legacy Property Panel、NodePreview Overlay/Inspector、selection store 改为 owner 分支内 lazy import；G15X legacy panels 继续仅经 flag-off dynamic import。
- active owner / 唯一写入口：
  - Property：`PropertyPanelCapabilityOwner -> PropertyPanelCapabilityAdapter -> FlowCanvasAdapter.patchNodeParameters()`
  - Preview：`PreviewPanelCapabilityOwner -> PreviewPanelCapabilityAdapter -> NodePreviewCoordinator`
  - Global Variables：`GlobalVariablesCapabilityOwner -> GlobalVariablesCapabilityAdapter -> projectManager.saveGlobalVariables()`，后端仍经 `ProjectService + ProjectSaveCoordinator`
  - Settings：`SettingsCapabilityOwner -> SettingsCapabilityAdapter -> settingsApi.saveSettings()`
  - Project：`ProjectPageCapabilityOwner -> ProjectPageCapabilityAdapter -> projectManager`，正式保存仍经 `ProjectService + ProjectSaveCoordinator`
  - Inspection：`InspectionCapabilityOwner -> InspectionCapabilityAdapter -> inspectionController`
  - Results/Review：`ResultsReviewCapabilityOwner -> ResultsReviewCapabilityAdapter -> formal history/detail/compare/previous-success/evidence loaders`
  - AI：`AiPanelCapabilityOwner -> AiPanelCapabilityAdapter -> existing AgentRun endpoints`
- Legacy mounted / subscription / timer 状态：
  - flag on 默认下，legacy PropertyPanel、Preview overlay/inspector、GlobalVariablePanel、SettingsView、ProjectView、InspectionPanel、ResultPanel、AiPanel 不应 mounted。
  - Preview legacy resources disabled via `disposeLegacyNodePreviewSurfaces()` and `previewResourcesEnabled: !isPreviewPanelCapabilityEnabled()`.
  - ResultsReview owner keeps `serverPaged = true`; legacy analytics refresh timer is skipped by `if (!resultPanel.serverPaged)`.
  - AI owner owns and cleans `EventSource`/replay timer via `closeEventStream()` / `window.clearTimeout`; legacy AI panel remains flag-off library.
- 测试命令与结果：
  - `node --test tests/unit/g15x-capability-owners.test.mjs ... tests/unit/ai-panel-canvas-sync.test.mjs` PASS（510/510）
  - `& "./scripts/run-dotnet-test-serial.ps1" -Project "ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/ClearVision.Product.Desktop.Tests.csproj" -FullyQualifiedName WebView2HostTests,Studio2ArchitectureGuardTests` PASS（34/34）
  - Product focused regression `ProjectServiceTests,ProjectSaveCoordinatorTests,ProjectPersistenceConcurrencyTests,ProjectCreationSchemaTests,InspectionServiceSingleRunTests,InspectionServiceRealtimeTests,InspectionServiceHistoryComparisonTests,InspectionResultPersistenceSnapshotTests,InspectionEvidenceManifestServiceTests,GetInspectionHistoryQueryHandlerTests,RuntimePackageExporterValidationTests,RuntimeMvpTests,PixelToWorldTransformOperatorTests` PASS（151/151）
  - Desktop focused regression `ApiEndpointsInspectionHistoryTests,StationPackageStoreTests,StationPackageDeploymentServiceTests,StationResultMapperTests,AgentRunEndpointsTests` PASS（78/78）
  - `npm ci` PASS；`npm run build` PASS；solution `dotnet restore --locked-mode` PASS；solution Debug build PASS；Desktop Release publish PASS
  - `node --check` touched JS/MJS PASS；`git diff --check` PASS（仅 Git LF→CRLF normalization warnings）
- 截图/Benchmark/Artifact：
  - `scripts/run-g16-release-benchmark.mjs` generated `docs/进行中/Studio2/release/artifacts/G16-benchmark-2026-07-04.{json,md}`
  - 300 primitives：p95 frame `0.079 ms`，p95 update `0.057 ms`，heap delta `1.286 MB`
  - 1000 primitives：p95 frame `0.054 ms`，p95 update `0.049 ms`，heap delta `2.106 MB`
  - Current display probe PERFORMED：DPI `96x96` / scale `100%` / screens `1152x2048`, `2194x1234`, `1920x1200`
  - 100/125/150/200% DPI and 1366/1080/1440/4K matrix mutation：`NOT PERFORMED`
- API / Project format / Runtime / Station / AgentRun 影响：
  - API contract：无变更
  - Project file format：无变更
  - Runtime / Station / PixelToWorld：无代码变更；focused regression PASS
  - AgentRun / EventStore / Workspace Snapshot / terminal recovery：无代码变更；`AgentRunEndpointsTests` focused regression PASS
- 技术债与非阻断事项：
  - `app.js` 仍是 release 阶段 composition root；`/v2/index.html` 未成为唯一 production frontend root。
  - `wwwroot/src` 仍随发布包作为 production static module tree 存在；不是 Node/Vite runtime dependency。
  - 真正删除 legacy flag-off libraries 需要多轮 clean CI、Playwright、真实 WebView2 和 rollback 验收。
- 阻断（无则写 `NONE`）：`BLOCKED_RELEASE_CUTOVER_GAP`; `BLOCKED_RELEASE_EVIDENCE_GAP`
- 下一 Goal：`RELEASE-FOLLOWUP`

## RELEASE-FOLLOWUP-1 回填区

- 状态：`BLOCKED`
- Verdict：`BLOCKED_RELEASE_CUTOVER_GAP`
- 开始时间：2026-07-05T00:51:55+08:00
- 完成时间：2026-07-05T01:22:09+08:00
- Initial SHA：`0394a2ba0e994b5e6720de2ee65e08b87edda1d7`
- Final SHA：提交自身 SHA 不写入 tracked 文件；以 push 后核对值为准
- 远端 SHA：启动门禁中本地 HEAD、`origin/codex初稿`、GitHub 远端均为 `0394a2ba0e994b5e6720de2ee65e08b87edda1d7`；提交前再次 fetch 核对
- 新增报告：`docs/进行中/Studio2/release/RELEASE-FOLLOWUP-1-总体验收报告-2026-07-05.md`
- 修复项：
  - Playwright NPoint ROI E2E guard 更新为 draft workbench 默认接管语义。
  - 迁移台账补回 G15.1/G15.2 runtime evidence token，保留 G16 default-on partial 状态。
  - AI operator knowledge graph/cards/report 由 runner 重新生成，补齐 PixelToWorldTransform runtime calibration 参数。
  - CircleCaliperFitV2 dataset checksum 刷新为当前 manifest bytes。
- 测试与验证：
  - FrontendV2 `npm ci` / lint / typecheck / unit / build PASS（unit 43/43）。
  - UI focused PASS（120/120）；UI full unit PASS（655/655）；preview smoke PASS；Playwright full PASS（85 passed / 1 skipped）；edited Playwright targeted rerun PASS（1/1）。
  - Desktop focused WebView2/architecture/endpoints/AgentRun/Station PASS（188/188）；Desktop full serial PASS（501/501）。
  - Product services regression PASS（65/65）；calibration regression PASS（115/115）；broad Product regression PASS（363/363）；Product full serial PASS（3289 passed / 4 skipped）。
  - OperatorLibrary package smoke PASS（40/40）；operator contract quick suite PASS（86/86）。
  - Desktop Release publish PASS；publish static no-Node scan PASS；`node --check` JS/MJS sweep PASS（129 files）；JSON artifact parse PASS；`git diff --check` PASS。
- Benchmark：
  - `docs/进行中/Studio2/release/artifacts/G16-benchmark-2026-07-04.{json,md}` refreshed。
  - 300 primitives：p95 frame `0.078 ms`，p95 update `0.051 ms`，heap delta `0.317 MB`。
  - 1000 primitives：p95 frame `0.164 ms`，p95 update `0.160 ms`，heap delta `1.355 MB`。
  - Current display probe PERFORMED：`1920x1200`，DPI `96x96`，scale `100%`。
- 未完成 / 证据缺口：
  - `Studio:WorkspaceV2Enabled=false`；旧 `app.js` 仍是 release production root；`/v2/index.html` 不是唯一 production root。
  - 真实 WebView2：`NOT PERFORMED`。
  - No-Node target-machine startup：`NOT PERFORMED`；本轮只做 publish static scan。
  - GitHub CI：`NOT RUN`；workflow_dispatch 存在但本机 `gh` 未登录。
  - DPI/分辨率矩阵：`NOT PERFORMED`；本轮只记录当前显示。
