# G11B：Circle Search Tool、Geometry 与 Scene

> 阶段：Vertical Product
> 状态：`DONE`
> 目标：把已验证 kernel 接入大图工具工作面。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `docs/进行中/Studio2/goals/G09B.md`
- `docs/进行中/Studio2/goals/G08.md`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/propertyPanel.js`

## 本轮范围

[x] Tool mode 参数区。
[x] Circle/Annulus/Arc geometry。
[x] Scene 显示 nominal、calipers、candidate、accepted/rejected、fit circle。

## 明确不做

- 改变 kernel
- 正式历史 Review
- 全量连续运行优化

## 执行清单

[x] Geometry commit 经 StudioFlowEditorPort。
[x] 参数、Scene、Inspector 与一次 preview observation 对齐。
[x] Artifact 提供有界大型点集；profile 未在不重复 kernel 的条件下新增。
[x] 旧 Method 工程默认不切换。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[x] Playwright 覆盖参数编辑、快速切换、失败状态、flag off。

## 完成条件

[x] Tool/Scene/Inspector 一致。
[x] 新旧 owner 不双挂载。

## 回填区

- 状态：`DONE`
- 开始时间：`2026-07-03 22:37:44 +08:00`
- 完成时间：`2026-07-04 00:39:35 +08:00`
- Initial SHA：`2a8b44d0cb70324924fc0ff13ed060c63f869a85`
- Final SHA：提交自身 SHA 不写入 tracked 文件；以 push 后核对值为准
- 远端 SHA：提交自身 SHA 不写入 tracked 文件；以 push 后核对值为准
- 修改文件：`propertyPanel.js`、`roiEditorPanel.js`、`roiEditorSupport.mjs`、`roiGeometry.mjs`、`imageCanvas.js`、`featureRegistry.js`、Desktop preview/observation/scene/artifact/feature-flag 文件与对应 UI/Desktop 测试。
- 新增/变更契约：`Studio:CircleSearchV2ToolEnabled` feature flag；preview observation/scene input `FeatureFlags`；CaliperFitV2 visual scene layers `circle-search-region`、`circle-search-nominal`、`circle-search-calipers`、`circle-search-candidates`、`circle-search-accepted`、`circle-search-rejected`、`circle-search-fit`、`circle-search-quality`；`Position`/`CircleCaliperFitV2Point` finite list materialized as bounded JSON `pointSet` artifact。
- active owner / 唯一写入口：`ImageCanvas` 仍为唯一 overlay 渲染内核；`RoiEditorPanel` 仍为 PropertyPanel 内唯一 geometry 挂载；`ExecutionVisualSceneProjector` 仍为唯一 Scene owner；geometry commit 经 PropertyPanel 到 `StudioFlowEditorPort` 单次参数提交。
- Legacy mounted / subscription / timer 状态：flag off 或 Method 非 `CaliperFitV2` 时保留旧 PropertyPanel/legacy Circle Scene；不挂载 Circle Search V2 ROI editor；未新增 timer、轮询、连续 preview 或第二 canvas。
- 测试命令与结果：`node --check` 相关 JS PASS；UI focused `property-panel-memory.test.mjs` PASS（8/8）与 `roi-geometry.test.mjs` PASS（28/28）；UI `npm run test:unit` PASS（618/618）；`npm run test:preview-smoke` PASS；Playwright `roi-editor.spec.ts` PASS（20/20）；Playwright `roi-editor.spec.ts node-preview.spec.ts` PASS（33/33）；Desktop focused `ExecutionObservationProjectorTests,PreviewArtifactStoreTests,WebView2HostTests,Studio2ArchitectureGuardTests` PASS（70/70）；Product CaliperFitV2/CircleMeasurement focused PASS（41/41）；measurement accuracy PASS（122/122）；measurement regression PASS（144/144）；Product full serial PASS（3229 passed, 4 skipped）；Desktop full serial PASS（472/472）；Desktop Debug build PASS；Station Debug/Release build PASS；Desktop Release publish PASS；publish audit PASS。
- 截图/Benchmark/Artifact：Playwright screenshot NOT CAPTURED；benchmark 未新增，沿用 G11A baseline；Desktop publish scratch `.tmp/publish-check/g11b` 已清理；`pointSet` artifact 覆盖 `EdgePoints` 等有界点集。
- API / Project format / Runtime / Station / AgentRun 影响：仅 Desktop preview/observation additive contract 与 startup flag；不修改 Project schema、Runtime Package、Station 或 AgentRun；不修改 CircleCaliperFitV2 数学内核。
- 技术债与非阻断事项：G11C 登记：Auto polarity 总采样预算应计入两套 hypothesis；Huber 收敛应使用 center+radius 同时稳定或 weights 稳定；MAD 达到迭代上限时 convergence diagnostic 需准确表达。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G11C`
