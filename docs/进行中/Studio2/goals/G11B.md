# G11B：Circle Search Tool、Geometry 与 Scene

> 阶段：Vertical Product
> 状态：`READY`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：把已验证 kernel 接入大图工具工作面。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `docs/进行中/Studio2/goals/G09B.md`
- `docs/进行中/Studio2/goals/G08.md`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/propertyPanel.js`

## 本轮范围

[ ] Tool mode 参数区。
[ ] Circle/Annulus/Arc geometry。
[ ] Scene 显示 nominal、calipers、candidate、accepted/rejected、fit circle。

## 明确不做

- 改变 kernel
- 正式历史 Review
- 全量连续运行优化

## 执行清单

[ ] Geometry commit 经 StudioFlowEditorPort。
[ ] 参数、Scene、Inspector 与一次 preview observation 对齐。
[ ] Artifact 提供剖面和大型点集。
[ ] 旧 Method 工程默认不切换。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] Playwright 覆盖参数编辑、快速切换、失败状态、flag off。

## 完成条件

[ ] Tool/Scene/Inspector 一致。
[ ] 新旧 owner 不双挂载。

## 回填区

- 状态：`READY`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G11C`
