# G10B：RoiManager Crop 空间传播

> 阶段：Geometry/Spatial
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：让现有 Rectangle/Circle/Polygon Crop 输出携带 local→parent 变换；Mask 保持 Full frame。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/RoiManagerOperator.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Services/FlowExecutionService.cs`
- `ClearVision.Product/tests/ClearVision.Product.Tests/Operators/RoiManagerOperatorTests.cs`

## 本轮范围

[ ] Rectangle/Circle/Polygon Crop。
[ ] Mask 模式。
[ ] 多级 Crop composition。

## 明确不做

- 复制 Mat 只为携带元数据
- 改写 ROI 像素处理实现
- 正式资产持久化

## 执行清单

[ ] 每种 Crop 计算真实 bounding rectangle 偏移。
[ ] Mask 输出不添加平移。
[ ] spatial sidecar 随 output port 传播。
[ ] Scene 可投影回 Full Image。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] ROI local↔Full round-trip、多级 Crop、Mask、旧流程测试。

## 完成条件

[ ] 坐标恢复准确。
[ ] 无额外图像复制。
[ ] 旧 output contract 兼容。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G10C`
