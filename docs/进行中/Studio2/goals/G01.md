# G01：ADR、状态权威与迁移白名单

> 阶段：Foundation
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：把已有架构边界写成可测试规则，防止后续形成第二套业务框架。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/app.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/app/serviceRegistry.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/app/eventBus.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/flowCanvasAdapter.js`
- `ClearVision.Product/src/ClearVision.Product.Application/Services/ProjectSaveCoordinator.cs`

## 本轮范围

[ ] 编写 ADR：宿主、前端 composition root、Flow、Project、Variables、Agent、Observation、Scene、Geometry 的 owner。
[ ] 建立 capability 迁移白名单和 Feature Flag 台账。
[ ] 明确 UI revision 与 `PersistenceRevision` 不是同一概念。

## 明确不做

- 实现新业务功能
- 改写 AgentRun 恢复体系
- 新增第二保存服务或第二 EventBus

## 执行清单

[ ] 为每个状态域填写 authority、projection、write entry、lifecycle owner。
[ ] 登记旧实现路径、未来 V2 owner、切换 Goal、删除 Goal。
[ ] 增加架构守卫：V2 禁止定义 project/flow/agent/variable 权威 Store。
[ ] 记录 full CI 在 PR/main/develop 触发的现实，禁止把普通分支 push 误报为完整 CI。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[ ] ADR 与当前代码一致。
[ ] 迁移白名单可直接用于后续 Goal 审查。
[ ] 没有新运行时代码。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G02A`
