# G01：ADR、状态权威与迁移白名单

> 阶段：Foundation
> 状态：`DONE`
> 目标：把已有架构边界写成可测试规则，防止后续形成第二套业务框架。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/app.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/app/serviceRegistry.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/app/eventBus.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/flowCanvasAdapter.js`
- `ClearVision.Product/src/ClearVision.Product.Application/Services/ProjectSaveCoordinator.cs`

## 本轮范围

[x] 编写 ADR：宿主、前端 composition root、Flow、Project、Variables、Agent、Observation、Scene、Geometry 的 owner。
[x] 建立 capability 迁移白名单和 Feature Flag 台账。
[x] 明确 UI revision 与 `PersistenceRevision` 不是同一概念。

## 明确不做

- 实现新业务功能
- 改写 AgentRun 恢复体系
- 新增第二保存服务或第二 EventBus

## 执行清单

[x] 为每个状态域填写 authority、projection、write entry、lifecycle owner。
[x] 登记旧实现路径、未来 V2 owner、切换 Goal、删除 Goal。
[x] 增加架构守卫：V2 禁止定义 project/flow/agent/variable 权威 Store。
[x] 记录 full CI 在 PR/main/develop 触发的现实，禁止把普通分支 push 误报为完整 CI。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[x] ADR 与当前代码一致。
[x] 迁移白名单可直接用于后续 Goal 审查。
[x] 没有新运行时代码。

## 回填区

- 状态：`DONE`
- 开始时间：2026-07-01 16:42:21 +08:00
- 完成时间：2026-07-01 16:48:21 +08:00
- Initial SHA：`789e9ec643390f5a79c68cfa6c4b401c1a679be3`
- Final SHA：见 G01 完成提交与最终报告
- 远端 SHA：见 G01 完成提交与最终报告
- 修改文件：`TODO.md`；`docs/进行中/Studio2/README.md`；`docs/进行中/Studio2/goals/G01.md`；`docs/进行中/Studio2/architecture/Studio2-架构边界-ADR.md`；`docs/进行中/Studio2/architecture/Studio2-capability-迁移台账.md`；`ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/Architecture/Studio2ArchitectureGuardTests.cs`
- 新增/变更契约：新增唯一 Studio 2.0 架构 ADR；新增唯一 capability 迁移白名单与 Feature Flag 台账；新增 Desktop.Tests 内最小 Studio 2.0 Architecture Guard。
- active owner / 唯一写入口：`app.js` 仍是当前 composition root；`EventBus` 与 `ServiceRegistry` 复用现有单例模块；`FlowCanvasAdapter` 是 V2 接入 FlowCanvas 的唯一业务 facade；`ProjectService + ProjectSaveCoordinator` 是 Project/Flow/GlobalVariables 正式保存权威；AgentRun/EventStore/Projection Journal/Workspace Snapshot 不迁移、不重构。
- Legacy mounted / subscription / timer 状态：本轮未新增 V2 root，旧实现仍是唯一 mounted owner；未修改 legacy subscription/timer；Feature Flag 仅登记，未实现 runtime flag。
- 测试命令与结果：`dotnet build ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/ClearVision.Product.Desktop.Tests.csproj` PASS，0 warnings，0 errors；首次 targeted test 发现 Station `System.Text.Json.Nodes` 误报，已收窄规则；`& "./scripts/run-dotnet-test-serial.ps1" -Project "ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/ClearVision.Product.Desktop.Tests.csproj" -FullyQualifiedName Studio2ArchitectureGuardTests,BuildFromPlanArchitectureGuardTests -NoBuild -NoRestore` PASS，11/11；`dotnet build-server shutdown` 后 `dotnet`/`testhost` 进程复查为 0；完整 CI NOT RUN。
- 截图/Benchmark/Artifact：NOT RUN，本轮无 UI 或性能改动。
- API / Project format / Runtime / Station / AgentRun 影响：无 API 变更；无 Project format 变更；无 Runtime Package 变更；无 Station 运行时变更；无 AgentRun 运行时变更。
- 技术债与非阻断事项：`FrontendV2` 尚不存在，架构守卫当前以明确的空 scope 行为通过，待 G02A 创建 V2 目录后自动扫描；若后续选择不同 V2 目录，必须先更新 ADR、台账和守卫 scope；Feature Flag 本轮只登记，runtime 实现留给对应 cutover Goal。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G02A`
