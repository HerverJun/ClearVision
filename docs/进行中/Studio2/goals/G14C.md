# G14C：Evidence manifest、导出与留存策略

> 阶段：Productization
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：以可选 manifest 管理证据包、容量、脱敏和 checksum。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Core/Entities/InspectionResult.cs`
- `ClearVision.Product/src/ClearVision.Product.Application/Services/IInspectionImagePersistenceService.cs`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Services/QueuedInspectionImagePersistenceService.cs`

## 本轮范围

[ ] OK/NG/Error 分级留存。
[ ] manifest 引用图像/Scene/Artifact，不把大对象塞数据库行。
[ ] retention、容量、checksum、脱敏。

## 明确不做

- Studio 与 Station 共用一个不可配置留存策略
- 无限留存
- 导出敏感路径/密钥

## 执行清单

[ ] Studio/Station 策略隔离。
[ ] 证据包可独立校验。
[ ] 删除/过期不破坏 Inspection summary。
[ ] 容量压力下确定性淘汰。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[ ] 证据包可审计。
[ ] 留存有边界。
[ ] 无敏感信息泄漏。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G15.1`
