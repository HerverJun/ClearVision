# G11C：Circle Search 连续预览、性能与兼容收口

> 阶段：Vertical Product
> 状态：`READY`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：补齐连续预览节流、资源治理和纵向回归。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/previewCoordinator.js`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/CircleMeasurementOperator.cs`

## 本轮范围

[ ] 连续预览 debounce/cancel/latest-wins。
[ ] 性能预算。
[ ] 变量 resultPath 与正式执行回归。

## G11B 登记技术债

[ ] Auto polarity 的总请求采样预算应计入两套 hypothesis。
[ ] Huber 收敛应使用 center 与 radius 同时稳定，或使用 weights 稳定。
[ ] MAD 达到迭代上限时的 convergence diagnostic 需准确表达。

## 明确不做

- 引入 WebGL 除非基准证明 Canvas2D 不足
- 改变 Station
- 迁移其他算子

## 执行清单

[ ] 连续预览不得并行堆积旧请求。
[ ] Artifact/Scene 诊断等级可降级。
[ ] 验证旧工程、旧 Method、新 Method 保存重启。
[ ] 输出 benchmark 环境和 p95。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[ ] 连续预览稳定。
[ ] 内存无持续增长。
[ ] 新稳定标量可绑定变量。

## 回填区

- 状态：`READY`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：Auto polarity 总采样预算计入两套 hypothesis；Huber 收敛使用 center+radius 同时稳定或 weights 稳定；MAD 达到迭代上限时 convergence diagnostic 准确表达。
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G12A`
