# G11C：Circle Search 连续预览、性能与兼容收口

> 阶段：Vertical Product
> 状态：`DONE`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：补齐连续预览节流、资源治理和纵向回归。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/previewCoordinator.js`
- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/CircleMeasurementOperator.cs`

## 本轮范围

[x] 连续预览 debounce/cancel/latest-wins。
[x] 性能预算。
[x] 变量 resultPath 与正式执行回归。

## G11B 登记技术债

[x] Auto polarity 的总请求采样预算应计入两套 hypothesis。（G11B-FOLLOWUP 已收口）
[x] Huber 收敛应使用 center 与 radius 同时稳定，或使用 weights 稳定。（G11B-FOLLOWUP 已收口）
[x] MAD 达到迭代上限时的 convergence diagnostic 需准确表达。（G11B-FOLLOWUP 已收口）

## 明确不做

- 引入 WebGL 除非基准证明 Canvas2D 不足
- 改变 Station
- 迁移其他算子

## 执行清单

[x] 连续预览不得并行堆积旧请求。
[x] Artifact/Scene 诊断等级可降级。
[x] 验证旧工程、旧 Method、新 Method 保存重启。
[x] 输出 benchmark 环境和 p95。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[x] 连续预览稳定。
[x] 内存无持续增长。
[x] 新稳定标量可绑定变量。

## 回填区

- 状态：`DONE`
- 开始时间：`2026-07-04 02:38:07 +08:00`
- 完成时间：`2026-07-04 03:03:20 +08:00`
- Initial SHA：`38e9c1a19197556b316a4c93e95c19d9e8354748`
- Final SHA：提交自身 SHA 不写入 tracked 文件；以 push 后核对值为准
- 远端 SHA：提交自身 SHA 不写入 tracked 文件；以 push 后核对值为准
- 修改文件：`previewCoordinator.js`、`preview-coordinator-memory.test.mjs`、`CircleCaliperFitV2BenchmarkTests.cs`、`CircleMeasurementCaliperFitV2OperatorTests.cs`、`quality/evals/reports/circle-caliper-fit-v2-benchmark.md`、本卡与 `TODO.md`。
- 新增/变更契约：Preview scope key 纳入 project/node/type/parameter snapshot，同一节点 Method 或工程切换会释放旧 artifact/cache、取消旧请求并丢弃迟到响应；Circle Caliper benchmark 覆盖 typical/upper-bounded profile、p50/p95 与 work-budget fail-closed；CircleMeasurement Method 参数 JSON 保存/重启兼容新增 focused 审计。
- active owner / 唯一写入口：继续复用 `NodePreviewCoordinator` 单一 preview owner；未新增第二套 Preview owner、WebGL、Station 改动或 ProjectSave participant。
- Legacy mounted / subscription / timer 状态：同节点 scope 变化、节点/工程切换与 destroy 均取消旧 abort controller/timer 并释放 artifact/object URL；旧 HoughCircle/FitEllipse 路径保持 mounted 兼容。
- 测试命令与结果：`node --check src/features/flow-editor/previewCoordinator.js` PASS；`node --test tests/unit/preview-coordinator-memory.test.mjs` PASS（17/17）；`node --test tests/unit/preview-coordinator-memory.test.mjs tests/unit/preview-panel-memory.test.mjs tests/unit/property-panel-memory.test.mjs tests/unit/roi-geometry.test.mjs` PASS（54/54）；Product focused `CircleCaliperFitV2BenchmarkTests,CircleCaliperFitV2KernelTests,CircleCaliperFitV2DatasetTests,CircleMeasurementCaliperFitV2OperatorTests,CircleMeasurementLegacyGoldenTests,CircleMeasurementOperatorTests,FlowNodePreviewServiceTests,FlowExecutionProjectVariableBindingTests,ResultPathV1Tests` PASS（97/97）；Desktop focused `ExecutionObservationProjectorTests,PreviewArtifactStoreTests,Studio2ArchitectureGuardTests` PASS（63/63）；`git diff --check` PASS。
- 截图/Benchmark/Artifact：Benchmark report updated at `quality/evals/reports/circle-caliper-fit-v2-benchmark.md`; screenshots NOT RUN.
- API / Project format / Runtime / Station / AgentRun 影响：无 API 或 persisted project format migration；runtime/Station/AgentRun 未修改；保存重启兼容通过 CircleMeasurement Method 参数 JSON shape focused test。
- 技术债与非阻断事项：G11B 登记的 Auto polarity、Huber convergence、MAD iteration-cap diagnostic 债务已由 G11B-FOLLOWUP 收口；本 Goal 剩余重点为连续预览、性能、内存、保存重启与兼容回归。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G12A`
