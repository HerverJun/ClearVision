# G13B：Runtime Package 可选 Calibration/Spatial 扩展

> 阶段：Vertical Product
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：把正式资产加入现有 RuntimeFieldExtensions 和包验证，保持旧包兼容。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Runtime/RuntimePackageExporter.cs`
- `ClearVision.Product/src/ClearVision.Product.Runtime.Abstractions/RuntimeContracts.cs`
- `ClearVision.Product/src/ClearVision.Product.Runtime/RuntimePathGuard.cs`

## 本轮范围

[ ] optional relative paths、manifest version compatibility、checksum/path guard。
[ ] 资源 bundle 与模型资源同类安全处理。
[ ] 旧包缺字段继续加载。

## 明确不做

- 假定 cvpkg 必须是单文件压缩包
- 新增平行 manifest
- Station UI

## 执行清单

[ ] 明确当前 exporter 生成目录包的实际形态。
[ ] 扩展 RuntimeFieldExtensions，不重复造 ProjectAssetIndex，除非 G13A 证明必要。
[ ] 禁止绝对路径。
[ ] 损坏/缺失资产 fail closed，仅当流程确实依赖。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] 新包、旧包、缺字段、checksum 错、路径穿越、clean export 测试。

## 完成条件

[ ] 新旧 package 均可判定性加载。
[ ] manifest 无绝对路径泄漏。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G13C`
