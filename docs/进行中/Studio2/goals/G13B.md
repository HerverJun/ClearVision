# G13B：Runtime Package 可选 Calibration/Spatial 扩展

> 阶段：Vertical Product
> 状态：`DONE`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：把正式资产加入现有 RuntimeFieldExtensions 和包验证，保持旧包兼容。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Runtime/RuntimePackageExporter.cs`
- `ClearVision.Product/src/ClearVision.Product.Runtime.Abstractions/RuntimeContracts.cs`
- `ClearVision.Product/src/ClearVision.Product.Runtime/RuntimePathGuard.cs`

## 本轮范围

[x] optional relative paths、manifest version compatibility、checksum/path guard。
[x] 资源 bundle 与模型资源同类安全处理。
[x] 旧包缺字段继续加载。

## 明确不做

- 假定 cvpkg 必须是单文件压缩包
- 新增平行 manifest
- Station UI

## 执行清单

[x] 明确当前 exporter 生成目录包的实际形态。
[x] 扩展 RuntimeFieldExtensions，不重复造 ProjectAssetIndex，除非 G13A 证明必要。
[x] 禁止绝对路径。
[x] 损坏/缺失资产 fail closed，仅当流程确实依赖。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[x] 新包、旧包、缺字段、checksum 错、路径穿越、clean export 测试。

## 完成条件

[x] 新旧 package 均可判定性加载。
[x] manifest 无绝对路径泄漏。

## 回填区

- 状态：`DONE`
- 开始时间：`2026-07-04 12:57:28 +08:00`
- 完成时间：`2026-07-04 13:17:06 +08:00`
- Initial SHA：`1a86837d3efce57f45de716888f7a8536b87bd8f`
- Final SHA：提交自身 SHA 不写入 tracked 文件；以 push 后核对值为准
- 远端 SHA：提交后以最终报告核对值为准
- 修改文件：Runtime `RuntimePackageExporter`/`RuntimePackageValidator`/`RuntimePathGuard`/`RuntimePackageExportRequest`；Abstractions `RuntimeContracts`；Application `ProjectService` metadata reader；Desktop runtime package export endpoint；Product/Desktop tests；`TODO.md`/G13B/G13C docs。
- 新增/变更契约：`RuntimePackageManifest.assets` optional `RuntimePackageAssets`（schemaVersion/calibrationAssets/spatialAssets）；`RuntimePackageProjectAsset`（assetId/kind/version/projectRevision/contentHash/fileHash/relativePath/required/status）；`RuntimeFieldExtensions.projectAssets` optional relative root；`RuntimePackageExportRequest` optional `ProjectAssetStorageMetadata`/required flag。
- active owner / 唯一写入口：Project authority remains `ProjectDto.Assets` / `IProjectAssetStorage` from G13A; RuntimePackageExporter only validates and serializes authority assets into the package; no PreviewArtifact/draft/candidate source is read.
- Legacy mounted / subscription / timer 状态：无 UI 变更；无 Station UI；无新增 subscription/timer。
- 测试命令与结果：Product focused `RuntimePackageExporterValidationTests,RuntimeParameterContractsTests,RuntimePackageLoaderTests` PASS（21/21）；Product affected `ProjectSaveCoordinatorTests,ProjectServiceTests,RuntimeMvpTests` PASS（56/56）；Desktop focused `ProjectGlobalVariableEndpointsTests,Studio2ArchitectureGuardTests` PASS（42/42）；`git diff --check` PASS。
- 截图/Benchmark/Artifact：无截图；无 benchmark artifact；Runtime exporter confirmed directory package root (`package.json`/`flow.json`/`runtime-profile.json`/`quality`/`field`/`assets`) while Station package store separately zips that directory into `.cvpkg` for deployment.
- API / Project format / Runtime / Station / AgentRun 影响：Project format unchanged beyond G13A assets; runtime manifest additive optional assets section; export endpoint now passes asset storage metadata and returns RuntimePackageException errors for asset export failures; old packages with missing assets section and empty assets section load; Station consumption/UI, PixelToWorld E2E, PLC/机器人 provider, AgentRun unchanged.
- 技术债与非阻断事项：Product full serial NOT RUN；Desktop full serial NOT RUN；Station Debug/Release build NOT RUN；Desktop Release publish NOT RUN；完整 GitHub CI NOT RUN；真实 WebView2 NOT PERFORMED。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G13C`
