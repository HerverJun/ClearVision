# G09A：Geometry 纯数学内核与矩形等价迁移

> 阶段：Geometry/Spatial
> 状态：`DONE`
> 目标：先把现有矩形 ROI 交互迁入通用数学内核，不同时实现全部形状。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/imageCanvas.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/roiGeometry.mjs`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/roiEditorPanel.js`

## 本轮范围

[x] 无 DOM 几何模型、screen↔image、clamp、hit-test、handles。
[x] Rectangle draft/commit 与旧参数格式 parity。
[x] 本地 draft history。

## 明确不做

- Circle/Polygon/Arc
- 全局 Flow undo command bus
- 改变算子参数格式

## 执行清单

[x] pointer move 只改 draft；commit 才经 StudioFlowEditorPort。
[x] 矩形创建/移动/缩放/键盘微调/cancel/commit。
[x] undo/redo 只在当前节点 draft 会话内。
[x] 旧 ROI Editor 通过 adapter 迁入。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[x] 数学 round-trip、DPI、hit-test、旧矩形 Playwright parity。

## 完成条件

[x] 矩形功能无回退。
[x] Geometry 无 Project/Flow 保存权威。
[x] 无第二画布。

## 回填区

- 状态：`DONE`
- 开始时间：2026-07-03 00:53:00 +08:00
- 完成时间：2026-07-03 01:07:13 +08:00
- Initial SHA：`893acf7d0205bf549622913c74a7b2dc5460a67a`
- Final SHA：提交后核对
- 远端 SHA：提交并 push 后核对
- 修改文件：
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/roiGeometry.mjs`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/imageCanvas.js`
  - `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/propertyPanel.js`
  - `ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/unit/roi-geometry.test.mjs`
  - `ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/e2e/roi-editor.spec.ts`
  - `TODO.md`
  - `docs/进行中/Studio2/goals/G09A.md`
- 新增/变更契约：`roiGeometry.mjs` 扩展 DOM-free rectangle geometry kernel：finite validation、screen/image round-trip、DPI-aware handle hit-test、nudge、draft history undo/redo/cancel；旧 `X/Y/Width/Height` 与 `RegionX/RegionY/RegionW/RegionH` 参数格式不变。
- active owner / 唯一写入口：`ImageCanvas` 仍是唯一图像 canvas 交互 owner；`PropertyPanel.handleRoiRectChanged` 仅在 `commit` phase 更新 operator 参数并触发既有 flow change/preview 链，drag/cancel 只更新本地表单 draft。
- Legacy mounted / subscription / timer 状态：未新增第二 canvas、未新增全局 undo bus、未新增 Project/Flow 保存权威；ROI editor 继续由现有 `RoiEditorPanel` 挂载并复用 `ImageCanvas`。
- 测试命令与结果：
  - `node --test tests/unit/roi-geometry.test.mjs`：PASS，18 tests。
  - `node --test tests/unit/canvas-core.test.mjs`：PASS，25 tests。
  - `npx playwright test tests/e2e/roi-editor.spec.ts --reporter=list --workers=1`：PASS，6 tests。
  - `npx playwright test tests/e2e/roi-editor.spec.ts --reporter=list`：PASS，6 tests。
  - `npm run test:unit`：PASS，597 tests。
  - `npm run test:preview-smoke`：PASS。
  - `git diff --check`：PASS。
  - `dotnet build-server shutdown` + process audit：PASS，无残留 `dotnet`/`testhost`/`node`；仅发现用户 Chrome/Edge 常驻进程。
- 截图/Benchmark/Artifact：未新增截图或 benchmark；本阶段为 rectangle parity 和 draft lifecycle。
- API / Project format / Runtime / Station / AgentRun 影响：无 API、Project format、Runtime、Station 或 AgentRun 影响。
- 技术债与非阻断事项：G09B 继续在同一 kernel 上扩展 Circle/Annulus/Arc；本阶段未实现 Circle/Polygon/Arc。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G09B`
