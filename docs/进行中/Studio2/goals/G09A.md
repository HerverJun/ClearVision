# G09A：Geometry 纯数学内核与矩形等价迁移

> 阶段：Geometry/Spatial
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：先把现有矩形 ROI 交互迁入通用数学内核，不同时实现全部形状。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/imageCanvas.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/roiGeometry.mjs`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/features/flow-editor/roiEditorPanel.js`

## 本轮范围

[ ] 无 DOM 几何模型、screen↔image、clamp、hit-test、handles。
[ ] Rectangle draft/commit 与旧参数格式 parity。
[ ] 本地 draft history。

## 明确不做

- Circle/Polygon/Arc
- 全局 Flow undo command bus
- 改变算子参数格式

## 执行清单

[ ] pointer move 只改 draft；commit 才经 StudioFlowEditorPort。
[ ] 矩形创建/移动/缩放/键盘微调/cancel/commit。
[ ] undo/redo 只在当前节点 draft 会话内。
[ ] 旧 ROI Editor 通过 adapter 迁入。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] 数学 round-trip、DPI、hit-test、旧矩形 Playwright parity。

## 完成条件

[ ] 矩形功能无回退。
[ ] Geometry 无 Project/Flow 保存权威。
[ ] 无第二画布。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G09B`
