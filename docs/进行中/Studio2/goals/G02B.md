# G02B：V2 挂载、HostBridge 与现有通信适配

> 阶段：Foundation
> 状态：`DONE`
> 目标：让空白 V2 岛可受控挂载/销毁，并复用现有 HTTP、EventBus、ServiceRegistry 和宿主注入。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/WebView2Host.cs`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/app.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/messaging/httpClient.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/app/eventBus.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/app/serviceRegistry.js`

## 本轮范围

[x] typed API 仅作为现有 `httpClient` 的 facade。
[x] HostBridge 复用 WebView2 现有注入和消息能力。
[x] flag off 时 V2 代码、订阅、timer、observer 均不初始化。

## 明确不做

- 重新实现 fetch/auth/端口发现/网络错误策略
- 新建第二个全局 EventBus
- 本轮迁移任何业务模块

## 执行清单

[x] 新增宿主配置 `Studio:WorkspaceV2Enabled` 并注入只读启动变量；不得以 localStorage 作为发布权威。
[x] flag on 挂载无业务测试岛；flag off 保留旧 composition root。
[x] V2 资产缺失时给出明确诊断，禁止静默混合运行。
[x] 定义统一 dispose contract 和请求取消 helper。
[x] 重复挂载/卸载验证 listener、timer、AbortController、Blob URL 无增长。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[x] typed client 保留现有 httpClient 语义。
[x] 同一时刻只有一个测试岛实例。
[x] flag off 无 V2 后台资源。

## 回填区

- 状态：`DONE`
- 开始时间：`2026-07-01 18:54:17 +08:00`
- 完成时间：`2026-07-01 19:15:31 +08:00`
- Initial SHA：`656645d3a653eee238fe41f1764e1254f8c879a3`
- Final SHA：见 G02B 完成提交与最终报告
- 远端 SHA：见 G02B 完成提交与最终报告
- 修改文件：`ClearVision.Product/src/ClearVision.Product.Desktop/Program.cs`；`ClearVision.Product/src/ClearVision.Product.Desktop/MainForm.cs`；`ClearVision.Product/src/ClearVision.Product.Desktop/WebView2Host.cs`；`ClearVision.Product/src/ClearVision.Product.Desktop/DesktopWebRootResolver.cs`；`ClearVision.Product/src/ClearVision.Product.Desktop/appsettings.json`；`ClearVision.Product/src/ClearVision.Product.Desktop/Configuration/StudioOptions.cs`；`ClearVision.Product/src/ClearVision.Product.Desktop/StudioStartupPageResolver.cs`；`ClearVision.Product/src/ClearVision.Product.Desktop/FrontendV2/**`；`ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/messaging/httpClient.js`；`ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/**` 的 G02B targeted tests；`docs/进行中/Studio2/**`；`TODO.md`
- 新增/变更契约：`Studio:WorkspaceV2Enabled` 默认 `false`；flag off 初始页为 `/index.html`，flag on 初始页为 `/v2/index.html`；`/v2` 静态 provider 独立映射到输出目录 `AppContext.BaseDirectory/wwwroot/v2` 并先于 legacy provider 注册；WebView2 文档注入冻结的 `window.__CLEARVISION_STARTUP__`，包含 `workspaceV2Enabled`、`apiBaseUrl`、`hostKind`、`frontendV2BasePath`；旧 `window.__API_BASE_URL__` 和 CSS version 注入保留。
- active owner / 唯一写入口：flag off 时旧 `app.js` 是唯一 composition root；flag on 时只导航 V2 测试岛，旧 `/index.html` 不作为初始页加载；V2 typed API 包裹既有 `httpClient`，HostBridge 包裹既有 `webMessageBridge`，EventBus 与 ServiceRegistry 均复用 legacy 单例。
- Legacy mounted / subscription / timer 状态：flag off 未请求 `/v2/index.html`，不会加载 V2 JS、Pinia、listener、timer、observer 或 AbortController；flag on 不加载旧 `/index.html`，V2 只注册测试岛自己的 EventBus/WebMessage 订阅和 ServiceRegistry 项，dispose 时按 scope 清理，不清空 legacy 全局单例。
- 测试命令与结果：FrontendV2 `npm ci` PASS；`npm run lint` PASS；`npm run typecheck` PASS；`npm run test:unit` PASS，4 files/8 tests；`npm run build` PASS；`dotnet build ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/ClearVision.Product.Desktop.Tests.csproj` PASS；`& "./scripts/run-dotnet-test-serial.ps1" -Project "ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/ClearVision.Product.Desktop.Tests.csproj" -FullyQualifiedName WebView2HostTests,DesktopWebRootResolverTests,StudioStartupPageResolverTests,ProgramStaticAssetsTests,Studio2ArchitectureGuardTests,BuildFromPlanArchitectureGuardTests -NoBuild -NoRestore` PASS，30/30；`dotnet build ClearVision.Product/src/ClearVision.Product.Desktop/ClearVision.Product.Desktop.csproj` PASS；`dotnet publish ... -o .tmp/publish-check/g02b` PASS；build/publish V2 asset audit PASS；WebView2 人工启动 NOT PERFORMED；完整 CI NOT RUN。
- 截图/Benchmark/Artifact：无截图和 benchmark；临时发布目录 `.tmp/publish-check/g02b` 已用于审计，提交前清理。
- API / Project format / Runtime / Station / AgentRun 影响：无后端 API endpoint 变更；无 Project format 变更；无 Runtime Package 语义变更；Station 生产代码未改；AgentRun/EventStore/recovery 未改。
- 技术债与非阻断事项：本轮 V2 仅为无业务测试岛，不实现 Workspace Shell、Flow、Project、Variables、Inspection、AI 或结果能力；真实 WebView2 人工启动未执行，需人工后续验证。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G03`
