# G02B：V2 挂载、HostBridge 与现有通信适配

> 阶段：Foundation
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：让空白 V2 岛可受控挂载/销毁，并复用现有 HTTP、EventBus、ServiceRegistry 和宿主注入。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/WebView2Host.cs`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/app.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/messaging/httpClient.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/app/eventBus.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/app/serviceRegistry.js`

## 本轮范围

[ ] typed API 仅作为现有 `httpClient` 的 facade。
[ ] HostBridge 复用 WebView2 现有注入和消息能力。
[ ] flag off 时 V2 代码、订阅、timer、observer 均不初始化。

## 明确不做

- 重新实现 fetch/auth/端口发现/网络错误策略
- 新建第二个全局 EventBus
- 本轮迁移任何业务模块

## 执行清单

[ ] 新增宿主配置 `Studio:WorkspaceV2Enabled` 并注入只读启动变量；不得以 localStorage 作为发布权威。
[ ] flag on 挂载无业务测试岛；flag off 保留旧 composition root。
[ ] V2 资产缺失时给出明确诊断，禁止静默混合运行。
[ ] 定义统一 dispose contract 和请求取消 helper。
[ ] 重复挂载/卸载验证 listener、timer、AbortController、Blob URL 无增长。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。

## 完成条件

[ ] typed client 保留现有 httpClient 语义。
[ ] 同一时刻只有一个测试岛实例。
[ ] flag off 无 V2 后台资源。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G03`
