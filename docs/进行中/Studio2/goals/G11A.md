# G11A：Circle Search V2 kernel、契约与数据集

> 阶段：Vertical Product
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：在保留现有 CircleMeasurement/Hough/FitEllipse 的前提下，引入版本化 CaliperFitV2 内核。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/CircleMeasurementOperator.cs`
- `ClearVision.Product/tests/ClearVision.Product.Tests/Operators/CircleMeasurementOperatorTests.cs`
- `scripts/run-tests-measurement-accuracy.ps1`
- `scripts/run-tests-measurement-regression.ps1`

## 本轮范围

[ ] Method additive 增加 CaliperFitV2。
[ ] 纯 kernel、输入/输出 contract、诊断和样本集。
[ ] 兼容现有 Center/Radius/Circle/CircleCount 等字段。

## 明确不做

- Tool UI
- 修改旧 Method 行为
- 统一重写其他测量算子

## 执行清单

[ ] 定义搜索环、极性、采样、拟合、outlier 和质量门禁。
[ ] 失败不伪造圆。
[ ] 建立黑白极性、低对比、反光、缺口、遮挡、多圆、边界、无圆数据集。
[ ] 记录旧 Method golden。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] 运行现有 measurement accuracy/regression，并新增 V2 golden/benchmark。

## 完成条件

[ ] 旧 Method bit/contract 行为无回退。
[ ] V2 有真实样本和失败诊断。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G11B`
