# G11A：Circle Search V2 kernel、契约与数据集

> 阶段：Vertical Product
> 状态：`DONE`
> 目标：在保留现有 CircleMeasurement/Hough/FitEllipse 的前提下，引入版本化 CaliperFitV2 内核。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Infrastructure/Operators/CircleMeasurementOperator.cs`
- `ClearVision.Product/tests/ClearVision.Product.Tests/Operators/CircleMeasurementOperatorTests.cs`
- `scripts/run-tests-measurement-accuracy.ps1`
- `scripts/run-tests-measurement-regression.ps1`

## 本轮范围

[x] Method additive 增加 CaliperFitV2。
[x] 纯 kernel、输入/输出 contract、诊断和样本集。
[x] 兼容现有 Center/Radius/Circle/CircleCount 等字段。

## 明确不做

- Tool UI
- 修改旧 Method 行为
- 统一重写其他测量算子

## 执行清单

[x] 定义搜索环、极性、采样、拟合、outlier 和质量门禁。
[x] 失败不伪造圆。
[x] 建立黑白极性、低对比、反光、缺口、遮挡、多圆、边界、无圆数据集。
[x] 记录旧 Method golden。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[x] 运行现有 measurement accuracy/regression，并新增 V2 golden/benchmark。

## 完成条件

[x] 旧 Method bit/contract 行为无回退。
[x] V2 有真实样本和失败诊断。

## 回填区

- 状态：`DONE`
- 开始时间：`2026-07-03 18:43:26 +08:00`
- 完成时间：`2026-07-03 20:09:40 +08:00`
- Initial SHA：`5251fe65b8ec059a8521384c8f48a3a5ee929413`
- Final SHA：提交自身 SHA 不写入 tracked 文件；以 push 后核对值为准。
- 远端 SHA：提交自身 SHA 不写入 tracked 文件；以 push 后核对值为准。
- 修改文件：G11A 白名单为 CircleMeasurement operator/CaliperFitV2 kernel、对应 Product tests、`quality/datasets/circle-caliper-fit-v2/`、operator generated docs/catalog/knowledge artifacts、`TODO.md` 与本卡片。
- 新增/变更契约：新增纯 `CircleCaliperFitV2Kernel`、`CircleCaliperFitV2Request`、`CircleCaliperFitV2Result`、`CircleCaliperFitV2Diagnostic`，`ContractVersion=caliper-circle-fit.v2`；`CircleMeasurement.Method` additive 增加 `CaliperFitV2|卡尺圆拟合 V2`，新增 V2 搜索中心、nominal、caliper、极性、outlier 与质量门禁参数；新增输出 `CaliperFitV2Result`、`EdgePoints`、`InlierPoints`、`OutlierPoints`、`CaliperDiagnostics`；`CircleMeasurement` metadata version `1.1.0`。
- active owner / 唯一写入口：无 UI owner 变更；V2 仅由 `Method=CaliperFitV2` 显式进入，legacy `HoughCircle`/`FitEllipse` 与 Hough 后置 caliper refinement 保持原路径。
- Legacy mounted / subscription / timer 状态：无前端、Tool UI、Canvas、Geometry、订阅或 timer 变更；未执行 G11B。
- 测试命令与结果：Legacy golden pre-change PASS（3/3）；CaliperFitV2 focused combined PASS（45/45）；final focused CircleMeasurement/CaliperFitV2 rerun PASS（29/29）；measurement accuracy PASS（122/122）；measurement regression PASS（144/144）；Product full serial PASS（3219 passed, 4 skipped）；Desktop full serial PASS（466/466）；OperatorDocGenerator PASS（最终无 source-hash/version warning）；OperatorKnowledgeGraphRunner PASS（156 cards, 1845 edges，既有 `System.Collections.Immutable` warning）；Desktop Debug build PASS；Station Debug/Release build PASS；Desktop Release publish PASS；publish audit PASS；`git diff --check` PASS；diff hygiene PASS；secret scan PASS；large-file audit PASS；untracked/scratch/process audit PASS。完整 GitHub CI：NOT RUN；真实 WebView2：NOT PERFORMED。
- 截图/Benchmark/Artifact：dataset `quality/datasets/circle-caliper-fit-v2/manifest.json`，SHA-256 `73ac0fb7384fcc453f9bc804b30aecb02560d81a92bef043dd70b75a5f41384e`，16 cases PASS；benchmark report `quality/evals/reports/circle-caliper-fit-v2-benchmark.md`，128 calipers avg 18.690 ms（320x240）、18.853 ms（640x480）、16.241 ms（1920x1080），avg allocation 2,442,173 bytes flat across sizes。
- API / Project format / Runtime / Station / AgentRun 影响：无后端 endpoint/API、Project format、Runtime Package、Station、AgentRun/EventStore/Workspace Snapshot/terminal/recovery 变更；仅算子 metadata/outputs/parameters additive。
- 技术债与非阻断事项：完整 GitHub CI 未运行；真实 WebView2 人工验证未执行；AI operator knowledge graph runner 保留既有 `System.Collections.Immutable` warning。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G11B`
