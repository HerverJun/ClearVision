# G03：Workspace Shell MVP

> 阶段：Foundation
> 状态：`LOCKED`（仅当根目录 `TODO.md` 将本 Goal 标记为 `READY` 时执行）
> 目标：建立工作面骨架，并让现有 FlowCanvas 在 V2 容器中仍是唯一画布实例。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/app.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/flowCanvas.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/flowCanvasAdapter.js`
- `ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/unit/canvas-core.test.mjs`

## 本轮范围

[ ] Flow/Tool/Review 模式和 dock shell。
[ ] 扩展现有 `FlowCanvasAdapter` 的生命周期与视图能力。
[ ] 布局状态仅保存纯 UI 信息。

## 明确不做

- 重写 FlowCanvas
- 创建第二 FlowCanvas facade
- 迁移 Property/Preview/AI/Results 业务

## 执行清单

[ ] 证明单 Workspace 生命周期只有一个 FlowCanvas 实例。
[ ] 模式切换不得反复销毁/重建 FlowCanvas。
[ ] V2 CSS 全部限定在根命名空间。
[ ] 支持 1366×768、1920×1080、2560×1440。
[ ] flag off 可完整回滚。

## 验证清单

[ ] 执行 `git diff --check`。
[ ] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[ ] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[ ] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[ ] 运行现有 canvas unit tests 和新增实例/销毁测试。

## 完成条件

[ ] FlowCanvas 节点、连线、选择和 view state 不丢失。
[ ] 无第二个画布实例。
[ ] Dock 反复切换无泄漏。

## 回填区

- 状态：`LOCKED`
- 开始时间：
- 完成时间：
- Initial SHA：
- Final SHA：
- 远端 SHA：
- 修改文件：
- 新增/变更契约：
- active owner / 唯一写入口：
- Legacy mounted / subscription / timer 状态：
- 测试命令与结果：
- 截图/Benchmark/Artifact：
- API / Project format / Runtime / Station / AgentRun 影响：
- 技术债与非阻断事项：
- 阻断（无则写 `NONE`）：
- 下一 Goal：`G04A`
