# G03：Workspace Shell MVP

> 阶段：Foundation
> 状态：`DONE`
> 目标：建立工作面骨架，并让现有 FlowCanvas 在 V2 容器中仍是唯一画布实例。

## 本轮只读上下文

执行前只读取：

1. 根目录 `AGENTS.md`；
2. 根目录 `TODO.md` 的架构红线、当前执行项和本 Goal 行；
3. 本卡片；
4. 下列真实代码锚点；
5. 上位总纲同名章节仅在本卡不足时按需读取。

### 代码锚点

- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/app.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/flowCanvas.js`
- `ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/flowCanvasAdapter.js`
- `ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/unit/canvas-core.test.mjs`

## 本轮范围

[x] Flow/Tool/Review 模式和 dock shell。
[x] 扩展现有 `FlowCanvasAdapter` 的生命周期与视图能力。
[x] 布局状态仅保存纯 UI 信息。

## 明确不做

- 重写 FlowCanvas
- 创建第二 FlowCanvas facade
- 迁移 Property/Preview/AI/Results 业务

## 执行清单

[x] 证明单 Workspace 生命周期只有一个 FlowCanvas 实例。
[x] 模式切换不得反复销毁/重建 FlowCanvas。
[x] V2 CSS 全部限定在根命名空间。
[x] 支持 1366×768、1920×1080、2560×1440。
[x] flag off 可完整回滚。

## 验证清单

[x] 执行 `git diff --check`。
[x] 遵守 `AGENTS.md`：同一测试项目禁止并行运行；优先使用仓库串行测试脚本。
[x] 测试结束确认无遗留 `dotnet`/`testhost`/浏览器服务进程。
[x] 回填本卡和根目录 `TODO.md` 后，再提交并推送。
[x] 运行现有 canvas unit tests 和新增实例/销毁测试。

## 完成条件

[x] FlowCanvas 节点、连线、选择和 view state 不丢失。
[x] 无第二个画布实例。
[x] Dock 反复切换无泄漏。

## 回填区

- 状态：`DONE`
- 开始时间：2026-07-01 21:51:13 +08:00
- 完成时间：2026-07-01 22:14:14 +08:00
- Initial SHA：`b359a35aafc7c37ec24bb4f27f1f4364040495d2`
- Final SHA：见 G03 完成提交与最终报告
- 远端 SHA：见 G03 完成提交与最终报告
- 修改文件：`ClearVision.Product/src/ClearVision.Product.Desktop/FrontendV2/src/components/WorkspaceShell.vue`；`FrontendV2/src/workspace/**`；`FrontendV2/src/foundation/**` lifecycle 和 bootstrap；`FrontendV2/src/adapters/legacyModules.ts`；`FrontendV2/src/tests/**`；删除未使用的 `FrontendV2/src/components/FoundationIsland.vue`；`ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/core/canvas/flowCanvasAdapter.js`；`ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/unit/canvas-core.test.mjs`；`ClearVision.Product/tests/ClearVision.Product.Desktop.Tests/Architecture/Studio2ArchitectureGuardTests.cs`；`docs/进行中/Studio2/**`；`TODO.md`。
- 新增/变更契约：`/v2/index.html` 在 `Studio:WorkspaceV2Enabled=true` 时挂载 Workspace Shell MVP；Shell 包含顶部 toolbar、左 Tool Dock、中央 Workspace、右 Review Dock、底部 status bar 和 Flow/Tool/Review 模式；`flowCanvasAdapter.js` 新增 `createHostedFlowCanvasAdapter(canvasId, options)`，由 legacy adapter 内部创建现有 `FlowCanvas` 并在 dispose 时调用 `FlowCanvas.destroy()`；`Studio2LifecycleScope.trackPendingRequest` 只通过显式 cancel callback/AbortController 取消请求。
- active owner / 唯一写入口：flag off 仍由旧 `/index.html` 和 `app.js` 作为唯一 root；flag on 只有 V2 Shell root，Flow 模式只注册 `studio2.flowCanvasAdapter`，不注册 raw canvas；Pinia 只保存 `activeMode`、dock 折叠和宽度，不保存 Flow/Project/Variables/AgentRun/结果数据。
- Legacy mounted / subscription / timer 状态：本轮未修改旧 `app.js` 启动语义，未迁移 Property/Preview/AI/Results/Project/Inspection/GlobalVariables；Tool/Review 为占位；Shell dispose 释放本 Workspace 自己的 EventBus/WebMessage handler、ServiceRegistry 注册、AbortController、app unmount 和 hosted FlowCanvas，不清空共享 singleton。
- 测试命令与结果：`npm ci` PASS；`npm run lint` PASS；`npm run typecheck` PASS；`npm run test:unit` PASS，15/15；`npm run build` PASS；`node --test tests/unit/canvas-core.test.mjs` PASS，21/21；`dotnet build ClearVision.Product/src/ClearVision.Product.Desktop/ClearVision.Product.Desktop.csproj --nologo` PASS；`dotnet publish ... -c Release -o .tmp/publish-check/g03 --nologo` PASS；build/publish `wwwroot/v2` asset audit PASS；`Studio2ArchitectureGuardTests` + `BuildFromPlanArchitectureGuardTests` targeted PASS，16/16；`git diff --check` PASS；进程清理检查 PASS。
- 截图/Benchmark/Artifact：浏览器级截图保存在 `.tmp/studio2-g03-screenshots/g03-shell-1366x768.png`、`.tmp/studio2-g03-screenshots/g03-shell-1920x1080.png`、`.tmp/studio2-g03-screenshots/g03-shell-2560x1440.png`；三档均验证 1 个 V2 root、1 个 V2 FlowCanvas、旧 `#flow-canvas` 未挂载、Flow/Tool/Review/Flow 切换后 canvas 尺寸非零。
- API / Project format / Runtime / Station / AgentRun 影响：API 无变更；Project format 无变更；Runtime Package 无变更；Station 生产代码无变更；AgentRun/EventStore/Workspace Snapshot/terminal/recovery 权威无变更。
- 技术债与非阻断事项：G04A 仍需实现 V2 Flow 编辑端口与本地 stale 防护；业务面板迁移保持后续 Goal；真实 WebView2 人工启动 NOT PERFORMED；完整 GitHub CI NOT RUN。
- 阻断（无则写 `NONE`）：`NONE`
- 下一 Goal：`G04A`
