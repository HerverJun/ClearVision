# 文档治理台账（2026-05-31）

## 治理范围

本次治理覆盖项目主文档入口、历史计划资料、算子名片相关资料、项目审计资料和文档生成镜像。重点不是重写所有历史正文，而是建立清晰的单一事实源、生命周期规则、目录 README 和可验证的镜像同步口径。

## 已落地调整

- 新增文档治理标准：`docs/参考资料/规范/文档治理标准.md`。
- 新增文档审计报告：`docs/文档审计报告-2026-05-31.md`，覆盖完成情况、专业性、正确性和残留边界。
- 补齐目录入口：`docs/算子资料/README.md`、审计资料各分区 README、归档历史分区 README、当前计划与未闭环事项 README。
- 收口主导航：`docs/README.md`、`docs/导航.md` 明确当前计划、算子资料、审计资料、归档和治理标准入口。
- 修复算子目录镜像：`docs/operators/`、`docs/CATALOG.md`、`docs/OPERATOR_CATALOG.md`、`docs/CHANGELOG.md` 重新同步到 `docs/算子资料/算子名片/` 的 156 个正式算子口径。
- 修复根 `算子资料/` 镜像中 `算子手册.md` 与 `算子文档现状对齐说明-2026-04.md` 的两处内容漂移。
- 修正明显元数据漂移：归档内误标 active/closing 的计划转为 closed，模板文档转为 template。
- 修复 `docs/参考资料/指南/用户指南.md` 的乱码小节和算子资料旧链接。
- 更新 `scripts/OperatorDocGenerator/Program.cs`，让后续生成自动同步 `docs/operators/` 和根级算子目录入口，避免再次出现镜像漂移。

## 当前目录责任

| 目录 | 责任 | 当前治理状态 |
|---|---|---|
| `docs/进行中/` | 当前计划、未闭环、待复核 | 已补规则入口，当前计划收敛为单主线 |
| `docs/算子资料/` | 算子目录、名片、手册、知识笔记 | 已设为唯一事实源，镜像已同步 |
| `docs/operators/` | 英文路径兼容镜像 | 已同步到 156 正式算子口径 |
| `算子资料/` | 根目录历史兼容镜像 | 已修复已知漂移，后续由生成器同步 |
| `docs/审计资料/` | 算子、算法、系统、外部审计与报告 | 已补分区 README 和生命周期说明 |
| `docs/归档/` | 已关闭事项、过期计划、历史资料 | 已补历史资料和历史归档入口 |
| `docs/参考资料/` | 长期规范、指南、手册 | 已新增文档治理标准 |

## 仍保留的边界

- 历史归档和外部审计快照中的旧路径不批量改写，避免破坏当时语境。
- `quality/`、`artifacts/`、`test_results/` 中的评估报告和测试产物不纳入本次目录迁移，只通过主文档链接引用。
- 面试资料仍保留在 `docs/面试/`，本次未合并到产品工程文档主线。

## 后续维护规则

- 新增长期文档时，同步更新 `docs/README.md` 或对应分区 README。
- 新增计划前先检查 `docs/进行中/当前计划/` 是否已有同主题 active 文档。
- 关闭计划时写闭环说明，再迁入 `docs/归档/已关闭事项/`。
- 更新算子名片时优先运行或修复 `scripts/OperatorDocGenerator`，不要只改镜像。
