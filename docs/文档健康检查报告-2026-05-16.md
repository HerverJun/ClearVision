# 文档健康检查报告（2026-05-16）

## 检查范围

- Markdown 扫描口径：仓库内 `*.md`，排除 `.git`、`.tmp`、`.dotnet_cli_home`、`.codex_tmp`、`artifacts`、`bin`、`obj`、`node_modules`、`test_results`。
- 活跃文档断链口径：在上述基础上，仅排除历史归档 `docs/归档/` 与外部审计快照 `docs/审计资料/外部审计/`。
- 算子文档口径：以当前 `OperatorMetadataScanner` 输出为准，正式算子 156 个。

## 本轮已更新

- 根 `README.md`、`docs/CATALOG.md`、`docs/OPERATOR_CATALOG.md` 已更新到 156 个正式算子口径。
- `docs/算子资料/` 与仓库根 `算子资料/` 的目录、名片、手册、导航和对齐说明已同步到 `2026-05-16 14:52:39 +08:00` 生成口径。
- `docs/operators/` 已从 118 个旧骨架同步为当前 160 个名片/索引文件，保留为 legacy 英文路径镜像。
- `scripts/OperatorDocGenerator/` 下的旧生成样例 Markdown 已改为当前正式文档兼容入口，避免脚本目录继续携带 155 口径旧快照。
- `quality/evals/reports/operator_quality_matrix.md` 已按当前 156 张名片重新生成，`Cards with TODO = 0`。
- `docs/operator-quality/operator_quality_evidence_manifest.md` 已同步当前质量矩阵统计。
- 7 个文档中残留的对话工具专用 citation/entity 标记已清理；实体名保留，无法复验的工具引用标记移除。
- `docs/参考资料/指南/螺钉检测教程.md` 的失效图片链接已改为“截图待补”说明，底部学习链接已指向现有文档。
- `docs/面试/面试资产库/黄金场景包/01-端子线序检测.md` 的场景包相对路径已修正到仓库根 `线序检测/`。

## 自动检查结果

| 检查项 | 结果 |
|---|---:|
| Markdown 文件数（当前扫描口径） | 1056 |
| 活跃文档本地相对链接断链 | 0 |
| 全量扫描剩余断链 | 426 |
| 私有 citation 标记 `U+E200-U+E203` | 0 |
| `U+FFFD replacement character` | 0 |
| 默认文本编码扫描 | 通过 611 个文件 |
| 生成名片 TODO/O(?)/~?ms 占位 | 仅模板文件保留 |

## 剩余边界

- 全量扫描剩余断链集中在历史归档和外部审计快照中，多数是合并前旧路径或对话审计材料中的历史引用。本轮没有批量改写这些历史快照，以避免破坏追溯语境。
- `FrameChangeTrigger` 已进入正式 156 算子目录和名片，但质量矩阵仍显示该算子暂无 evidence signal；后续应补 contract、参数边界和错误契约测试。
- 质量矩阵中的 dataset、field replay 和真实产线签核缺口仍是证据治理问题，不应只靠文档措辞关闭。
