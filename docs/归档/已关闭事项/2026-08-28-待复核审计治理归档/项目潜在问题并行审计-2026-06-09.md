---
title: "项目潜在问题并行审计"
doc_type: "audit"
status: "needs-review"
topic: "前端 UI、后端逻辑、通讯、文案、性能、测试缺口"
created: "2026-06-09"
updated: "2026-06-09"
---

# 项目潜在问题并行审计

本次审计按只读方式执行：启动 6 个 gpt-5.5/xhigh 子代理并行检查前端 UI、AgentRun 前后端通讯、后端逻辑、文案注释、性能隐患和测试覆盖；主代理同步复核关键证据。未修改源码、未回滚其它代理变更、未启动测试进程。以下条目均待人工复核和排期修复。

## 前端 UI 与可访问性

1. **参数禁用提示英文直出**
   - 证据：`ClearVision.Product/src/ClearVision.Product.Desktop/wwwroot/src/shared/parameterDependencyRules.js:10` 写有 `File path is disabled...`；`.../features/flow-editor/propertyPanel.js:492` 将规则提示渲染为 `parameter-rule-hint`。
   - 风险：中文界面中直接出现英文规则说明，现场操作员理解成本高。
   - 建议：为规则原因接入中文资源表，避免直接渲染内部字段名。

2. **参数必填校验消息英文**
   - 证据：`parameterDependencyRules.js:137` 写有 `DeepLearning requires...`；`parameterDependencyRules.js:561` 存在默认英文错误。
   - 风险：表单错误或 Toast 可能混入英文和内部参数名。
   - 建议：为 `atLeastOneMessage` 和默认错误提供中文业务文案。

3. **文件选择按钮只有省略号**
   - 证据：`propertyPanel.js:575-576` 的文件输入旁按钮文本为 `...`。
   - 风险：按钮语义弱，屏幕阅读器无法知道用途。
   - 建议：改为“选择文件”，或图标按钮加 `aria-label="选择文件"`。

4. **枚举缺省显示开发态字符串**
   - 证据：`propertyPanel.js:548` fallback 为 `'undefined'`，`propertyPanel.js:552` 写入 `<option>`。
   - 风险：元数据缺失时用户会看到开发态字符串。
   - 建议：展示“未命名选项”，并记录元数据质量告警。

5. **结果状态英文枚举直接展示**
   - 证据：`wwwroot/src/app.js:599` 返回 `Error`；`resultPanel.js:1447` fallback `Unknown`；`resultPanel.js:1455` 渲染状态徽标。
   - 风险：追溯卡片和详情页会出现 `Error/Unknown`。
   - 建议：展示层统一映射为“合格/不良/异常/未知”。

6. **缺陷 fallback 文案英文**
   - 证据：`app.js:580` 使用 `Target ${index + 1}`；`app.js:581` 使用 `Result did not include...`。
   - 风险：实时结果缺陷详情缺失时，用户看到英文占位。
   - 建议：改为中文占位，并优先显示诊断码和来源说明。

7. **结果详情隐藏缺陷提示英文**
   - 证据：`resultPanel.js:2059` 使用 `More` / `Hidden ... more defects`。
   - 风险：长缺陷列表折叠提示不符合中文界面。
   - 建议：改为“还有 N 条缺陷未显示”，并提供展开或查看全部。

8. **工站监控页混入英文和内部字段**
   - 证据：`stationMonitorView.js:1070` 显示 `OK / NG / ERR`；`stationMonitorView.js:1195` 显示 `Ping`；`stationMonitorView.js:1411` 显示 `executionTimeMs`。
   - 风险：生产监控页暴露工程字段名，降低可读性。
   - 建议：展示为“合格/不良/异常”“连通性检测”“执行耗时”。

9. **Runtime Preview Pilot 设置页英文开发文案**
   - 证据：`features/settings/tabs/aiTab.js:33` 通过本地开关启用开发 UI；`aiTab.js:1127` 使用 `Camera allowlist`；`aiTab.js:1134` 使用 `Refresh catalog`。
   - 风险：开关打开后设置页出现大段英文试点功能。
   - 建议：开发 UI 明确隔离，或统一中文化并加“实验功能”标识。

10. **AI 设置表单英文残留**
    - 证据：`aiTab.js:756` 使用 `Planner`；`aiTab.js:848` 使用 `API Endpoint`；`aiTab.js:862` 使用 `Clear saved key`。
    - 风险：模型配置页中英文混排，术语不统一。
    - 建议：统一为“规划模型 / 接口地址 / 清除已保存密钥”等中文术语。

11. **附件删除按钮辅助名称英文**
    - 证据：`features/ai/aiPanelAttachments.js:137` 写有 `aria-label="remove attachment"`。
    - 风险：屏幕阅读器读出英文操作。
    - 建议：改为 `aria-label="移除附件"`，必要时包含附件名。

12. **登录页禁止用户缩放**
    - 证据：`wwwroot/login.html:5` 设置 `maximum-scale=1.0, user-scalable=no`。
    - 风险：低视力用户无法放大登录界面。
    - 建议：移除缩放限制，依靠响应式布局适配。

13. **密码显示按钮被移出 Tab 顺序**
    - 证据：`login.html:45` 为密码显示按钮，`login.html:49` 设置 `tabindex="-1"`；同类设置还在 `login.html:101`。
    - 风险：键盘用户无法聚焦“显示密码”。
    - 建议：移除 `tabindex="-1"`，并同步 `aria-pressed` 状态。

14. **主导航 active 只靠 CSS**
    - 证据：`wwwroot/index.html:77` 初始按钮仅 class `active`；`core/app/viewManager.js:88-90` 只切换 class。
    - 风险：辅助技术无法知道当前视图。
    - 建议：切换时同步 `aria-current="page"`，或采用 tablist/tabpanel 模式。

15. **自定义 Tooltip 只监听鼠标**
    - 证据：`index.html:135` 主题按钮仅有 `data-tooltip`；`shared/components/tooltip.js:36-42` 只委托 `mouseover`。
    - 风险：键盘和读屏用户拿不到工具提示；图标按钮也缺少稳定辅助名称。
    - 建议：图标按钮补 `aria-label`，Tooltip 支持 `focus/blur` 与 `aria-describedby`。

16. **Toast 不会被读屏主动播报**
    - 证据：`shared/components/uiComponents.js:233` 创建容器仅设置 class；`uiComponents.js:292` Toast 仅创建 div；`uiComponents.js:312` 关闭按钮仅 `×`。
    - 风险：成功/错误通知对非视觉用户不可感知。
    - 建议：容器加 `aria-live`，错误用 `role="alert"`，关闭按钮加 `aria-label="关闭通知"`。

17. **新建工程默认占位名为英文**
    - 证据：`wwwroot/src/app.js:1227` 使用 `placeholder: Project_${Date.now()}`。
    - 风险：创建工程弹窗中出现英文默认命名。
    - 建议：改为 `工程_${yyyyMMdd_HHmmss}` 或“未命名工程”。

18. **图像查看器错误提示英文**
    - 证据：`features/image-viewer/imageViewer.js:335` 调用 `showToast('Image load failed: ' + error.message...)`。
    - 风险：加载失败时用户直接看到英文错误。
    - 建议：改为“图像加载失败：...”，并对底层异常做本地化映射。

## 前后端通讯与 AgentRun 契约

19. **附件在 AgentRun 生成链路被丢弃**
    - 证据：`aiPanelGenerateRequest.js:93` 读取附件路径；`aiPanelAgentRun.js:472` 传 `attachments: []`；`AgentRunEndpoints.cs:885` 后端固定 `Array.Empty<string>()`。
    - 风险：带图/文件需求在 AgentRun 路径与旧 GenerateFlow 路径结果不一致。
    - 建议：复用既有附件授权链路，或明确禁用附件入口并提示 metadata-only 限制。

20. **RuntimePreview 授权开关被 AgentRun 强制抹掉**
    - 证据：`aiPanelGenerateRequest.js:479` 传入授权意图；`aiPanelAgentRun.js:476` 固定 `runtimePreviewConsent: false`；`AgentRunEndpoints.cs:895` 后端也固定 `RuntimePreviewConsent = false`。
    - 风险：用户授权状态与工具策略不一致。
    - 建议：保留一次性 consent，后端消费后清除并写入事件诊断。

21. **旧 activeAgentRunId 会过滤下一轮 PlanRun 事件**
    - 证据：`aiPanelAgentRun.js:568` 先按 `activeAgentRunId` 过滤；`aiPanelAgentRun.js:849` 终止只清 `activeAssistantTurn`；`aiPanelAgentWorkspace.js:321` 处理 PlanRun。
    - 风险：完成一次构建后再规划，事件可能被丢弃并卡住。
    - 建议：终止时清理 `activeAgentRunId`，或先判断 PlanRun 再做 build runId 过滤。

22. **旧 activePlanRunId 会导致构建中取消请求打到已完成 PlanRun**
    - 证据：`aiPanelAgentWorkspace.js:674` 记录 plan run；`aiPanelAgentWorkspace.js:1454` 完成后未清 id；`aiPanelGenerateRequest.js:561` 取消逻辑优先取消 plan。
    - 风险：用户以为取消构建，实际构建继续运行。
    - 建议：PlanRun 完成/失败后清 id，进入 Build 前也清理 plan 活动状态。

23. **下层服务可用普通 Append 发布 terminal 事件**
    - 证据：`AgentRunEventStreamService.cs:98` 普通 `Append` 仅在已 terminal 后拒绝；`AgentRunEventStreamService.cs:386` 只有 terminal append 才置 `IsTerminal`；`VisionAgentGenerateFlowService.cs:218` 可发布 `run.failed`；`AgentRunEndpoints.cs:421` endpoint 之后再 `Fail`。
    - 风险：前端先看到 `run.failed` 并关闭流，可能错过更完整失败 payload。
    - 建议：禁止普通 `Append` 发送 terminal 类型，统一通过 `Fail/Complete/Cancel`。

24. **事件流失败不会向上层 reject**
    - 证据：`aiPanelAgentRun.js:269` 回放循环持续运行；`aiPanelAgentRun.js:298` catch 只增加计数；`aiPanelAgentRun.js:541` start catch 只更新状态；`aiPanelAgentWorkspace.js:593` 等待流程依赖事件。
    - 风险：401/404/网络失败后生成态长期卡住。
    - 建议：设置最大重试和总超时，并进入明确失败态。

25. **EventSource 备用通道会漏掉新增事件类型**
    - 证据：`AgentRunEndpoints.cs:847` 后端写 named event；`aiPanelAgentRun.js:54` 使用硬编码事件类型表；`aiPanelAgentRun.js:238` 逐个 addEventListener；`aiPanelAgentRun.js:247` 只用默认 message 兜底。
    - 风险：后端扩展事件后备用通道无感知。
    - 建议：使用默认 `message` 包装事件，或版本化同步事件类型表。

26. **Plan public event 去重粒度过粗**
    - 证据：`AgentRunEndpoints.cs:533` 遍历公开事件；`AgentRunEndpoints.cs:678-679` key 只有 `eventType:stage:status`。
    - 风险：同阶段同状态的多条诊断被静默丢弃。
    - 建议：使用 public event id/序号，或纳入 title、summary、payload hash。

27. **CanBuild=false 和 BlockingReasons 没有阻止构建**
    - 证据：`VisionAgentPlanContracts.cs:34` 定义 `CanBuild`；`aiPanelAgentWorkspace.js:940` 渲染计划；`aiPanelAgentWorkspace.js:1371` 显示构建入口；`aiPanelAgentWorkspace.js:1442` 启动构建时只检查 generating/plan。
    - 风险：阻断计划仍可进入构建。
    - 建议：UI 禁用构建按钮并展示 blocking reasons，后端也校验 BuildFromPlan 快照。

28. **AgentRun 失败路径没有复用 manualRetry 契约**
    - 证据：`aiPanel.js:841-850` 旧路径生成重试草稿；`aiPanelAgentRun.js:793-803` 固定 `manualRetry: null`。
    - 风险：失败修复体验和状态不同步。
    - 建议：把 `run.failed` payload 归一到 `_handleResult` 同一 failure/manualRetry 结构。

29. **AgentRun 成功结果不回写持久化 sessionId**
    - 证据：`aiPanel.js:753` 旧路径保存 session；`aiPanelAgentWorkspace.js:1977-1989` AgentRun 只写 currentResult；`aiPanelAgentRun.js:838` 历史依赖 `this.sessionId`。
    - 风险：新会话历史和后续上下文丢失。
    - 建议：terminal 成功时同步 `this.sessionId` 并保存，后端构建器也补齐会话持久化。

30. **首事件超时会中断健康但空闲的 SSE**
    - 证据：`aiPanelAgentRun.js:163` 连接后启动 10 秒首事件计时；`aiPanelAgentRun.js:371` 超时；`aiPanelAgentRun.js:378` abort；`AgentRunEndpoints.cs:265` 后端读取 live events，无 heartbeat。
    - 风险：长阶段被误判为流失败并降级轮询。
    - 建议：后端发送 heartbeat，或将首事件超时改为连接失败/总 idle 策略。

31. **回放轮询每 1.5 秒拉全量事件**
    - 证据：`aiPanelAgentRun.js:77` 设置 `AGENT_RUN_REPLAY_INTERVAL_MS = 1500`；`aiPanelAgentRun.js:285` GET replay；`aiPanelAgentRun.js:293` 客户端过滤；`AgentRunEventStreamService.cs:242` replay 返回全量事件。
    - 风险：长 run 的网络和主线程成本线性增长。
    - 建议：给 replay API 加 `afterSequence`/分页，只返回增量和 gap 信息。

32. **AgentRun SSE 订阅通道无界**
    - 证据：`AgentRunEventStreamService.cs:188` 为每个订阅创建 unbounded channel；`AgentRunEventStreamService.cs:116` 对订阅者 `TryWrite`。
    - 风险：慢客户端或断连检测滞后时可能堆积内存。
    - 建议：改为 bounded channel，定义溢出策略，合并低价值状态事件并主动关闭滞后订阅。

33. **httpClient 的 PUT/DELETE 缺少自动端口发现兜底**
    - 证据：`core/messaging/httpClient.js:245-267` GET 有端口发现重试；`httpClient.js:311-337` POST 有重试；`httpClient.js:417-424` PUT 直接 fetch；`httpClient.js:430-436` DELETE 直接 fetch。
    - 风险：WebView/file 环境下服务端口变化时，写入/删除接口比 GET/POST 更容易失败。
    - 建议：抽取统一请求方法，所有 HTTP 动词共享端口发现、错误包装和保存端口逻辑。

34. **Inspection SSE 只解析 Header 游标，不支持 query afterSequence**
    - 证据：`InspectionEventEndpoints.cs:151-160` 只读 `Last-Event-ID`；前端 `inspectionSseClient.mjs:1-7` 也只构建 header。
    - 风险：无法设置 header 的 EventSource 或代理环境难以恢复断点。
    - 建议：与 AgentRun 一样支持 `lastEventId`/`afterSequence` query。

## 后端逻辑、并发与资源生命周期

35. **RuntimeHost 停止超时状态会被 finalize 覆盖**
    - 证据：`RuntimeHost.cs:316-318` 停止超时将状态置为 `Faulted` 并设置 `_stopTimeoutPending`；`RuntimeHost.cs:625-630` finalize 在 `_stopTimeoutPending` 为 true 时又恢复为 `Loaded/Idle`。
    - 风险：停止超时被隐藏，Studio/Station 看到“已加载”而不是故障。
    - 建议：为 stop timeout 保留独立终态，或 finalizer 不清除超时 Faulted，需显式 reset。

36. **RuntimeHost 事件回调未隔离订阅者异常**
    - 证据：`RuntimeHost.cs:559` 直接 `ResultAvailable?.Invoke(result)`；`RuntimeHost.cs:729` 直接 `SnapshotChanged?.Invoke`；`RuntimeHost.cs:734` 直接 `LogMessage?.Invoke`。
    - 风险：任一订阅者抛异常会反向破坏结果持久化、加载或停止流程。
    - 建议：复制委托列表，逐个 try/catch，必要时异步投递事件。

37. **取消/故障路径可能无限等待结果写入队列**
    - 证据：`RuntimeHost.cs:515` 和 `RuntimeHost.cs:530` 使用 `CancellationToken.None` 持久化取消/失败结果；结果写入端可能在 bounded channel 等待。
    - 风险：文件 IO 卡住或消费者停滞时，取消/故障记录反而挂住运行线程。
    - 建议：故障记录使用短超时、TryWrite 降级或死信记录，不用无限 token。

38. **实时检测启动失败回滚使用请求 token**
    - 证据：`InspectionService.cs:317` 已注册 session；`InspectionService.cs:337` 回滚 `TryStopAsync(projectId, cancellationToken)` 仍用外部请求 token。
    - 风险：HTTP 请求取消时回滚也取消，Coordinator 可能停在 Starting，后续启动被挡。
    - 建议：回滚用内部短超时或 `CancellationToken.None`，并保证失败清理幂等。

39. **InspectionWorker 先启动后台任务再登记状态**
    - 证据：`InspectionWorker.cs:198` 先 `Task.Run`；`InspectionWorker.cs:214` 才 `TryAdd`；`InspectionWorker.cs:228` 才增加指标。
    - 风险：任务快速退出时 cleanup 可早于指标增加，活跃 worker 计数和生命周期状态错乱。
    - 建议：先登记 entry/指标，再释放启动门闩让后台任务运行。

40. **Coordinator 在锁内同步触发 StateChanged**
    - 证据：`InspectionRuntimeCoordinator.cs:57`、`:98`、`:134`、`:303` 等状态迁移路径同步触发事件。
    - 风险：订阅者重入会卡在同一 `SemaphoreSlim`；订阅者异常会中断状态迁移。
    - 建议：锁内只构造事件参数，释放锁后逐订阅者安全通知。

41. **结果后台持久化在 spool 失败时不释放预算**
    - 证据：`InspectionResultBackgroundService.cs:172` 保存 batch；`InspectionResultBackgroundService.cs:230` append spool 可能抛异常；`InspectionResultBackgroundService.cs:188` 外层只记录日志。
    - 风险：DB 与 spool 同时异常时 batch 留存，`_queuedImageBytes` 不释放，生产端长期背压。
    - 建议：对 spool 写失败设置死信/丢弃策略并释放预算，或进入受控降级状态。

42. **图片持久化队列停机超时后直接丢剩余图片**
    - 证据：`QueuedInspectionImagePersistenceService.cs:120` 完成 writer；`:130` 只等 5 秒；`:141` 然后取消。
    - 风险：慢盘或大量 NG 图停机时无 durable spool，审计证据丢失。
    - 建议：增加图片 spool/manifest，或报告明确丢弃计数并允许配置 drain 策略。
    - 修复：已增加可配置停机 drain timeout、明确 dropped 计数/队列字节诊断，并在超时后释放尚未开始保存的快照预算；覆盖阻塞落盘的回归测试。

43. **CameraFrameStreamCoordinator 的 IdleStopCts 使用不同锁保护**
    - 证据：`CameraFrameStreamCoordinator.cs:128` 在 `Gate` 下改；`:690` stop 中改；`:710` 在 `lock(entry)` 下改。
    - 风险：双重 Dispose、丢失 idle stop、生产者泄漏或偶发 ObjectDisposed。
    - 建议：统一由 `Gate` 或单独私有锁管理 CTS 生命周期。
    - 修复：已统一 `IdleStopCts` 的安装、替换、取消和清理由 producer `Gate` 管理，并补充 direct acquire idle timeout 自动停止回归测试。

44. **相机 producer gate 被多路径 Dispose**
    - 证据：`CameraFrameStreamCoordinator.cs:229`、`:356`、`:433`、`:719` 多处释放 producer gate。
    - 风险：idle-stop、Dispose、Acquire/Release 交错时可能等待已释放 semaphore；后台 idle 任务也未集中取消/等待。
    - 建议：producer entry 增加生命周期 CTS/refcount，gate 延迟到 coordinator Dispose 统一释放。
    - 修复：运行路径移除 producer 时不再抢先 dispose gate，改为精确 retire 当前 entry，并在 coordinator `DisposeAsync` 中集中释放 retired gate。

45. **TCP 通讯连接池无上限/空闲回收，超时后仍复用连接**
    - 证据：`TcpCommunicationOperator.cs:41-44` 静态池；`:139-140` 加入池；读写超时路径没有强制 `InvalidateConnection`。
    - 风险：多目标地址长期积累 socket；读写超时后的半脏连接继续被复用。
    - 建议：增加容量、idle TTL 和清理入口；操作超时一律移除并关闭连接。
    - 修复：已增加连接池容量上限、idle TTL、last-used/active operation 跟踪和公开清理入口；读写取消/超时会失效连接，并覆盖超时重连、idle 清理和容量裁剪测试。

46. **PLC 客户端复用只保护建连，不保护读写/心跳**
    - 证据：`PlcCommunicationOperatorBase.cs:357` 只锁建连；`SiemensS7CommunicationOperator.cs:129`、`:135` 执行业务读写；`PlcCommunicationOperatorBase.cs:223` 心跳直接 ping。
    - 风险：同一 PLC socket 上协议帧并发交错，出现假掉线、读错包、写失败。
    - 建议：按 connectionKey 引入 operation lock，覆盖 Read/Write/Ping 全路径。
    - 修复：已在 PLC 基类按 connectionKey 增加 operation lock，心跳 ping 与 S7/MC/FINS 的 read/write/polling 均串行化；覆盖同 key 操作串行化回归测试。

47. **PLC 心跳启动不是线程安全单例**
    - 证据：`PlcCommunicationOperatorBase.cs:56` 构造器检查普通 bool；`PlcCommunicationOperatorBase.cs:68` StartHeartbeat 也无锁。
    - 风险：并发创建算子时可能启动多个 heartbeat task/CTS 和 ProcessExit 订阅。
    - 建议：用 `Interlocked.CompareExchange` 或 lock 管理启动/停止。
    - 修复：已用 `_heartbeatGate` 串行化心跳启动/停止，避免并发重复创建 task/CTS 和重复注册 ProcessExit；覆盖并发 `StartHeartbeat` 单例回归测试。

48. **StationHubClient 调用与断开/释放没有统一并发边界**
    - 证据：`StationHubClient.cs:48` 只在 EnsureConnected 加锁；`StationHubClient.cs:328` 后续直接用 `_connection!`；`StationHubClient.cs:238` 可并发 DisposeConnection。
    - 风险：正在上报时被 Disconnect/Dispose 置空或释放，导致误判失败、重复 spool。
    - 建议：调用期间持有稳定 connection 引用和 in-flight 计数，断开等待调用完成或串行化。
    - 修复：已将连接建立与 hub invocation 纳入同一 gate 生命周期，断开/释放会等待正在进行的上报完成；通过内部 hub adapter 覆盖调用中断开回归测试。

49. **命令结果本地 journal/spool 失败会阻断远端上报**
    - 证据：`StationSyncHostedService.cs:948` 先写 journal；`StationCommandExecutionJournalStore.cs:121` rewrite 无 catch；远端失败后 `StationSyncHostedService.cs:956` 入 spool；`StationCommandResultSpoolStore.cs:84` rewrite 也无 catch。
    - 风险：本地磁盘短暂异常会让命令终态既报不上去也缓存不了。
    - 建议：journal/spool 写入做 best-effort 捕获，远端上报与本地审计分别降级。
    - 修复：已将命令终态 journal 和远端失败后的 spool 写入改为 best-effort 降级，journal 失败不阻断远端上报，spool 失败只记录错误；覆盖本地写入异常回归测试。

50. **AgentRun 事件存储同步刷盘且无压缩策略**
    - 证据：`AgentRunEventStore.cs:74-86` 每次事件和 summary 都 `File.AppendAllText`；`AgentRunEventStreamService.cs:111-112` 每个事件都 append event 和 summary。
    - 风险：高频事件会形成同步磁盘写放大，summary JSONL 也长期增长。
    - 建议：引入后台批量写、summary latest 文件或周期 compact。
    - 修复：已将 AgentRun 状态锁与 JSONL 持久化拆开，新增 event+summary 单次持久化入口、可配置压缩阈值、summary latest 压缩和长 run 事件保留策略；覆盖压缩、重启续号和终态后 dropped 计数恢复回归测试。

## 性能隐患

51. **WinForms 日志追加是 O(n) 拷贝**
    - 证据：`ClearVision.Product/src/ClearVision.Product.Station/MainForm.cs:1147` 的 `AppendLog` 每条日志读取 `_logTextBox.Lines.ToList()`，再 `Lines = lines.ToArray()`。
    - 风险：高频日志会反复复制整个文本框内容并触发 UI 重排。
    - 建议：改为 `AppendText`，维护环形缓冲或按定时器批量刷新。
    - 修复：已引入 UI 日志内存队列，普通日志走 `TextBox.AppendText` 增量追加；低保留数保持精确裁剪，高保留数按批量裁剪重建文本，避免满窗口后每条日志都复制整个日志窗口。

52. **Station spool 在锁内重写整份 JSONL**
    - 证据：`StationSpoolStore.cs:166` ack 后 `RewriteSpoolLocked()`；`StationSpoolStore.cs:296` 重写文件；`StationCommandResultSpoolStore.cs:55` 每次 enqueue 也 rewrite。
    - 风险：结果/命令频繁时磁盘写放大，锁持有时间随 pending 数增长。
    - 建议：追加日志加 checkpoint/compact，或用 SQLite/轻量队列表。
    - 修复：已将结果 spool 的 ack 改为仅推进 state，并由 state 过滤旧 JSONL 行，累计 stale 行或超过字节阈值后再 compact；命令结果 spool 改为 append-only upsert/ack 操作日志，重启回放后保留最新 pending，达到阈值再 compact。覆盖 ack 不立即重写、overflow 重启过滤、命令结果 append/replay/latest 和 compact 回归测试。

53. **串口算子固定同步阻塞 100ms**
    - 证据：`SerialCommunicationOperator.cs:87`、`:125`、`:131` 的执行路径同步 Open/Write/Read，并 `Thread.Sleep(100)`。
    - 风险：每次执行至少占用线程 100ms，拖慢高频流水线。
    - 建议：使用异步串口读写、可配置响应等待、复用端口或独立通信 worker。
    - 修复：已引入内部串口连接抽象，生产实现通过 async open/write/read 执行，固定 `Thread.Sleep(100)` 改为 `ResponseWaitMs` 可配置、可取消的短轮询等待；`BytesReceived` 改为真实接收字节数。覆盖 ASCII/HEX 响应、发送字节和取消等待回归测试。

54. **变量算子每次循环打 Information 日志**
    - 证据：`VariableReadOperator.cs:78`、`VariableWriteOperator.cs:103`、`VariableIncrementOperator.cs:93` 每次执行都 `LogInformation`。
    - 风险：变量节点通常在每帧路径上，日志吞吐和格式化成本会放大。
    - 建议：降到 Debug/Trace，或加采样、变化才记录、诊断开关。
    - 修复：已将 VariableRead/VariableWrite/VariableIncrement 的成功热路径日志从 Information 降为 Debug，失败/异常仍由算子生命周期日志保留；覆盖三类变量算子不再输出 Information、仍保留 Debug 诊断的回归测试。

55. **StatisticsOperator 每次复制窗口并多次遍历**
    - 证据：`StatisticsOperator.cs:58`、`:73`、`:81`、`:87` 对窗口 `ToArray()` 后 `Average/Min/Max/Select.Sum`。
    - 风险：窗口最大 50,000，高频测量节点会形成 O(window) CPU 与分配。
    - 建议：维护 rolling sum/min/max/variance，必要时用单调队列或分段统计。
    - 修复：已将滚动窗口状态改为维护 sum/sumSquares，并用单调队列维护 min/max；每次追加和裁剪不再 `ToArray()` 或多次 LINQ 遍历。覆盖窗口裁剪后 min/max/stddev、reset 清空历史以及既有 Cpk/独立历史回归测试。

56. **FrameAveraging 每帧克隆并重算全窗口**
    - 证据：`FrameAveragingOperator.cs:57`、`:75`、`:82`、`:208` 每次 `src.Clone()` 入队，窗口 `ToArray()`，均值模式遍历所有帧。
    - 风险：大图和 64 帧窗口下内存带宽、GC、Mat 分配压力高。
    - 建议：均值模式维护滚动累加 Mat，帧出队时减旧帧。
    - 修复：已为 mean 模式维护滚动累加 Mat，新帧入队时累加、旧帧出队时扣减，输出直接由累加 Mat 转换得到；median 模式保留窗口快照路径。覆盖均值窗口裁剪、尺寸变化重置累加状态和既有 median 回归测试。

57. **ShapeMatching NMS 二次复杂度且反复分配**
    - 证据：`ShapeMatchingOperator.cs:1047` 排序候选；`:1058` 循环处理；`:1063` 每轮 `Where(...).ToList()`。
    - 风险：候选多时 O(n^2) 且大量临时 List。
    - 建议：用索引数组/标记删除，或网格/空间索引预筛 IoU。
    - 修复：已将 NMS 改为一次排序后用 `bool[]` 标记被抑制候选，移除每轮 `RemoveAt(0)` 和 `Where(...).ToList()` 重分配，保留原有分数/坐标稳定排序语义；覆盖 ShapeMatching 相关回归测试。

58. **Undistort 缓存 unmanaged Mat 但无释放入口**
    - 证据：`VisionRuntimeServiceCollectionExtensions.cs:67` 注册算子；`UndistortOperator.cs:21-24` 持有 `_mapCache`；`UndistortOperator.cs:172` 仅 eviction 时 Dispose。
    - 风险：进程生命周期内最多 16 组 remap Mat 常驻，服务关闭也不会显式释放。
    - 建议：实现 `IDisposable` 清空缓存，或改用集中 Mat/LUT 缓存服务并统计容量。
    - 修复：已让 `UndistortOperator` 实现 `IDisposable`，释放时清空 remap 缓存；缓存 map 使用期间增加 lease 计数，淘汰/释放会等活跃 `Remap` 完成后再 Dispose native Mat，并覆盖释放后缓存清空/拒绝继续执行的回归测试。

59. **文件输出算子在执行线程做同步磁盘 I/O**
    - 证据：`ImageSaveOperator.cs:131` 调用 `Cv2.ImWrite`；`TextSaveOperator.cs:187`、`:194` 直接 `File.AppendAllText/WriteAllText`。
    - 风险：慢盘/网络盘会阻塞流程执行线程。
    - 建议：提供异步落盘队列、批量写、背压和失败重试策略。
    - 修复：已将 `ImageSaveOperator` 改为 `Cv2.ImEncode` 后 `File.WriteAllBytesAsync` 异步落盘，取消同步 `FileInfo` 获取大小；`TextSaveOperator` 改为按文件路径 `SemaphoreSlim` 串行化异步 `FileStream`/`StreamWriter` 写入。覆盖图像保存和文本并发追加回归测试。

60. **DatabaseWrite 每条记录打开连接且表锁缓存无回收**
    - 证据：`DatabaseWriteOperator.cs:39` 静态缓存；`:235` 创建连接；`:308` 打开连接。
    - 风险：动态表/连接串会累积 semaphore；高频写入有连接开销和 DB 往返。
    - 建议：限制/回收表缓存，复用连接或批量写入，表初始化移到启动/配置阶段。
    - 修复：已将表存在缓存改为有界 FIFO 缓存，并把表初始化锁改为 ref-counted 临时 semaphore，成功/失败/取消路径最后一个调用释放后都会从静态字典移除；高频重复写入仍依赖 ADO.NET provider 物理连接池，避免自建单连接池改变 SQL Server/MySQL/SQLite 并发语义。覆盖缓存裁剪、锁释放和 SQLite 写入回归测试。

61. **PLC 心跳全局 1 秒顺序巡检所有连接**
    - 证据：`PlcCommunicationOperatorBase.cs:53` 心跳间隔；`:189`、`:197` 遍历快照；`:219` ping。
    - 风险：连接多或设备慢时巡检占用后台资源并和业务连接竞争。
    - 建议：按连接活动度自适应心跳、并发限流、空闲连接回收，并配置化。
    - 修复：已将心跳快照巡检改为有并发上限的 `Parallel.ForEachAsync`，单连接仍经 connectionKey operation lock 串行化，避免和业务读写交错；覆盖慢 ping 多连接并发受限、锁不残留和既有 PLC 连接并发回归测试。

62. **Station 主窗体状态刷新可能阻塞 UI**
    - 证据：`MainForm.cs:112` WinForms timer；`MainForm.cs:1167`、`:1277` 刷新状态；`PlcCommunicationOperatorBase.cs:110` 快照方法同步 `_poolLock.Wait(50)`。
    - 风险：连接池竞争时 UI 线程可卡 50ms。
    - 建议：后台采样 PLC 状态，UI 仅读取无锁/volatile 快照。
    - 修复：已将 PLC 连接状态快照读取改为非阻塞尝试获取连接池锁，锁忙时立即返回 last-known 状态，避免 WinForms timer 线程等待 50ms；覆盖锁被占用时快照立即返回的回归测试。

63. **前端自动保存同步序列化整张流程图**
    - 证据：`app.js:182` 自动保存间隔；`app.js:1750` 保存流程；`app.js:218` 写 localStorage；`flowCanvas.js:1685` 序列化流程图。
    - 风险：大流程会在主线程产生序列化和同步存储卡顿。
    - 建议：维护增量 dirty signature，保存到 IndexedDB/异步后端，或用 idle callback 分片序列化。
    - 修复：已将结构变更后的工程 flow 同步改为 dirty 标记 + debounce + idle flush，避免每次节点/连线/参数变更立即完整 `serialize()`；`ProjectManager` 增加保存前 flow snapshot provider，工程保存/切换/导出/本机草稿备份前会同步 flush 最新画布，避免延迟同步造成旧流程落库。覆盖 ProjectManager 保存前 flush 回归测试。

64. **StationMonitor 5 秒全量 DOM 重绘**
    - 证据：`stationMonitorView.js:1022` 定时刷新；`:1040` render；`:1074` 和 `:1464` 多区域 `innerHTML = ...map(...).join()` 重建。
    - 风险：站点/结果多时 DOM churn 和事件状态丢失。
    - 建议：只更新变更节点，拆分相对时间刷新，结果列表虚拟化。
    - 修复：已为 StationMonitor KPI 摘要区增加渲染签名，5 秒刷新时指标数据未变化则跳过 `innerHTML` 重建；结果 workbench 已继续受 `_resultsDirty` 保护，不随普通计时刷新重绘。覆盖 KPI 签名不变跳过 DOM 写入的回归测试。

65. **SplitPanel 拖拽未按帧节流**
    - 证据：`splitPanel.js:81` 监听 mousemove；`:86` 每次读尺寸；`:100` 立即 applyRatio。
    - 风险：拖拽时强制布局和回调频率过高。
    - 建议：缓存容器尺寸，mousemove 只记录目标比例，用 `requestAnimationFrame` 应用。
    - 修复：`SplitPanel` 拖拽开始时缓存容器尺寸，mousemove 只更新待应用比例；`requestAnimationFrame` 合并 DOM 写入和 resize 回调，mouseup/销毁/同步设比时清理待执行帧，避免旧拖拽帧覆盖新比例。新增 `split-panel.test.mjs` 覆盖帧合并、尺寸读取缓存和同步比例变更防回写。

## 文案、注释与文档治理

66. **OperatorBase 注释仍有乱码**
  - 证据：`Infrastructure/Operators/OperatorBase.cs:2`、`:17-18`、`:26`、`:36-39` 仍包含转码乱码文本。
    - 风险：核心基类维护说明不可读，后续生命周期规则容易被误解。
    - 建议：按 UTF-8 中文重写注释，删除“Encoding cleanup”占位。
    - 修复：已清理 `OperatorBase.cs` 中全部 `Encoding cleanup` 占位注释，补齐 `paramName`、`min`、`max`、`useZeroCopy` 等 XML 文档说明，并把生命周期、参数读取、图像输入输出和 JsonElement 回退转换说明改为可读中文。

67. **FlowExecutionService 局部注释仍有乱码**
    - 证据：`FlowExecutionService.cs:509`、`:522`、`:532`、`:707`、`:1744-1768` 多处 mojibake 注释。
    - 风险：执行计划、取消令牌、并行层级等关键逻辑说明不可读。
    - 建议：集中清理注释，优先保留与并发和生命周期相关的解释。
    - 修复：已清理 `FlowExecutionService.cs` 全部 `Encoding cleanup` 占位注释，并重写执行计划缓存、层级并行取消、单算子超时、ImageWrapper 输出归一化、扇出引用计数、输入端口映射、调试断点/单步和调试缓存清理等关键路径说明。

68. **待复核索引文件自身编码损坏**
  - 证据：`docs/进行中/待复核/索引.md:1-20` 标题和 Dataview 查询出现转码乱码文本。
    - 风险：待复核目录入口不可读，Obsidian/Dataview 查询路径也可能失效。
    - 建议：用 UTF-8 重建索引 frontmatter、标题、说明和查询路径。
    - 复核：当前 `docs/进行中/待复核/索引.md` 已为可读 UTF-8，frontmatter、标题、归档说明和 Dataview 查询路径均正常；未发现 `Encoding cleanup`、mojibake 或替换字符残留。

69. **顶层 README 当前状态落后**
    - 证据：`README.md:12` 标注“当前状态（2026-05-22）”，而同文件后续已引用 2026-05-31 文档。
    - 风险：首页状态口径落后，读者可能误判项目最新进展。
    - 建议：改成“最近更新”段或链接到最新状态文档。
    - 修复：`README.md` 已改为“最近更新”滚动段，新增 2026-07-01 G00 基线冻结、2026-06-01 潜在风险排查、2026-05-31 文档审计三条事实入口，并在文档导航中补充对应链接；同时明确真实相机/PLC/Station/产线签收仍以现场验收记录为准。

70. **开发规则引用已不存在的 docs/active 路径**
    - 证据：`ClearVision.Product/DEVELOPMENT_RULES.md:11` 要求更新 `docs/active/项目待办总表.md`，但当前工作区未见该路径。
    - 风险：新代理或开发者会按过时路径执行，造成文档更新漏写。
    - 建议：改指向当前 `docs/进行中/` 体系，并移除旧审计状态块。
    - 修复：`DEVELOPMENT_RULES.md` 已升级为 v2.0，删除 2026-03-02 旧自动审计状态块，移除失效 `docs/active` 路径引用，改为当前 `docs/README.md`、`docs/导航.md`、`docs/进行中/当前计划/README.md`、`docs/进行中/未闭环事项/README.md`、`docs/进行中/待复核/索引.md` 与 `docs/归档/README.md` 文档事实源。

71. **MQTT 算子暴露 placeholder-disabled 英文**
    - 证据：`MqttPublishOperator.cs:91` 返回 `MQTT publish is placeholder-disabled in this build...`。
    - 风险：运行失败消息带临时实现口径，且英文不适合产品文案。
    - 建议：改为“当前版本未启用 MQTT 发布能力”，内部保留结构化错误码。
    - 修复：`MqttPublishOperator` 运行失败消息改为 `[MQTT_PUBLISH_DISABLED] 当前版本未启用 MQTT 发布能力。请启用 MQTT 客户端集成后再使用该算子。`，日志改为中文禁用说明并携带结构化错误码；`MqttPublishOperatorTests` 覆盖中文文案、错误码和不再泄露 `placeholder-disabled`。

72. **旧版 Morphology 元数据仍以英文发布**
    - 证据：`MorphologyOperator.cs:19` `Morphology (Legacy)`；`:20` `Legacy image morphology node...`。
    - 风险：算子目录/属性面板可能显示英文和 Legacy 口径。
    - 建议：元数据源头改为中文显示名“形态学（旧版）”，英文仅作内部别名。
    - 修复：`MorphologyOperator` 的 `OperatorMeta` 已改为中文显示名“形态学（旧版）”、中文说明和中文分类；`OperatorMetadataLocalization` 对 `OperatorType.Morphology` 的覆盖也同步改为中文。新增 `MorphologyOperatorTests.Metadata_ShouldExposeChineseLegacyDisplayName` 防止 attribute 回退到英文旧口径。

73. **Station 命令不支持消息仍是英文**
    - 证据：`StationSyncHostedService.cs:714` 返回 `is not supported by this Station build.`。
    - 风险：命令失败原因可能回传到 Studio/日志/审计界面，非研发用户难以理解。
    - 建议：返回中文业务消息，错误码保留 `NotSupported`。
    - 修复：`StationSyncHostedService` 未支持命令分支改为返回 `当前 Station 版本不支持命令：...`，错误码仍为 `NotSupported`。新增 `StationSyncHostedServiceTests.TryPollAndExecuteCommandAsync_WhenCommandTypeIsUnsupported_ShouldReportChineseFailureMessage` 覆盖中文消息与错误码。

74. **标定错误信息英文返回**
    - 证据：`CalibrationBundleV2Json.cs:131` 写有 `Distortion model ... is not supported by this operator.`；`:157` 同类 `Transform2D model...`。
    - 风险：算子执行/校验失败时错误信息未本地化。
    - 建议：错误码结构化，展示层中文化为“不支持的畸变模型/二维变换模型”。
    - 修复：`CalibrationBundleV2Json` 已将不支持的畸变模型/二维变换模型错误改为中文业务消息，并提供 `CALIBRATION_DISTORTION_MODEL_UNSUPPORTED`、`CALIBRATION_TRANSFORM2D_MODEL_UNSUPPORTED` 稳定错误码；同类 `CalibrationPlanarTransformRuntime` 平面运行时模型不支持错误也改为中文结构化码。新增 `CalibrationBundleV2JsonTests` 覆盖两类 unsupported 模型错误不再回退英文。

## 测试与质量门禁缺口

75. **UI 单测清单漏跑 wire-sequence-assist**
    - 证据：`ClearVision.Product/tests/ClearVision.Product.UI.Tests/package.json:9` 的 `test:unit` 手写文件清单；`tests/unit/wire-sequence-assist.test.mjs:1` 文件存在；`.github/workflows/ci.yml:404` 调用该脚本。
    - 风险：线序辅助前端单测新增后不会进入默认/CI 信号。
    - 建议：改成 glob runner，或把缺失文件补入并加清单一致性检查。
    - 修复：新增 `tests/unit/run-all-unit-tests.mjs` 动态收集所有 `*.test.mjs`，`package.json` 的 `test:unit` 改为调用该 runner，避免后续新增测试漏进默认/CI 信号。补入默认脚本后暴露并修复 `wireSequenceAssist.js` 旧版 BoxNms 上游参数回写缺口；`npm run test:unit` 已通过 674/674。

76. **Preview smoke 脚本未接入 CI/Playwright 默认集**
    - 证据：`package.json:8` 定义 `test:preview-smoke`；`playwright.config.ts:13` 只跑 `tests/e2e`；修复前 CI 只跑 unit/Playwright 默认集。
    - 风险：`preview-regression.smoke.mjs` 的 Node 级预览回归不会自动执行。
    - 建议：CI 增加 `npm run test:preview-smoke`，或迁入 unit/Playwright 项目。
    - 修复：`.github/workflows/ci.yml` 的 `Run UI Tests` 步骤已在 `npm run test:unit` 后追加 `npm run test:preview-smoke`，本地 `npm run test:preview-smoke` 已通过。

77. **Playwright E2E 主要验证静态前端和 mock API**
    - 证据：`playwright.config.ts:30` 用 `http-server` 服务 `wwwroot`；`high-frequency-regression.spec.ts:85-87` mock API。
    - 风险：前端字段名、状态码、SSE/认证行为可与 Desktop API 漂移而不被 E2E 捕获。
    - 建议：增加真实 Desktop API-backed Playwright 项目，或由后端契约生成 mock/fixture。

78. **Desktop endpoint 回归脚本覆盖极窄**
    - 证据：`scripts/run-tests-desktop-endpoints.ps1:21-24` 只跑 `PreviewNodeEndpointsTests` 和 `InspectionEventEndpointsTests`；端点测试还包括 `AgentRunEndpointsTests.cs:25`、`AiModelEndpointsTests.cs:23`、`StationEndpointsTests.cs:22`。
    - 风险：大部分 HTTP endpoint 回归不会被该预设覆盖。
    - 建议：扩展脚本清单，或按 `*Endpoint*Tests`/`*EndpointsTests` 自动发现并设置最小数量。
    - 修复：`scripts/run-tests-desktop-endpoints.ps1` 已改为自动发现 Desktop 测试目录下的 `*Endpoint*Tests.cs` 公共测试类，并增加 `-MinimumEndpointTestClasses` 哨兵避免静默缩水；本地以自动发现类过滤器单次串行运行通过。

79. **User 管理端点缺少明确 HTTP endpoint 测试**
    - 证据：`UserEndpoints.cs:23`、`:52`、`:124` 映射用户 CRUD/重置密码；相邻现有测试是 `AuthEndpointsTests.cs:22`。
    - 风险：管理员用户接口的授权、错误码、响应形状可回归。
    - 建议：新增 `UserEndpointsTests`，覆盖 list/get/create/update/delete/reset-password 与权限边界。
    - 修复：新增 `UserEndpointsTests`，使用内存 `IUserRepository` 与固定 `IPasswordHasher` 覆盖非 Admin 403、Admin list/get/create/update/delete/reset-password、配置化初始密码长度校验和缺失用户 404；该文件已被 Desktop endpoint preset 自动纳入。

80. **数据库维护 HTTP 端点缺少 endpoint 层契约测试**
    - 证据：`SettingsEndpoints.cs:158`、`:165`、`:178`、`:191`、`:210` 映射 status/repair/backup/restore/cleanup；现有是服务测试 `VisionDatabaseMaintenanceServiceTests.cs:11`。
    - 风险：HTTP 参数绑定、状态码、异常映射未被锁定。
    - 建议：补 `SettingsDatabaseEndpointTests`，用 TestServer 覆盖成功/失败路径。
    - 修复：新增 `SettingsDatabaseEndpointTests`，用 TestServer + 临时 SQLite 覆盖 status/repair/backup/restore/cleanup、非 Admin 403、空 `BackupPath` 400、缺失备份文件 404 和 live database 自恢复 400；`SettingsEndpoints` 已将数据库维护的可预期客户端错误映射为 400/404。最新 Desktop endpoint preset 自动发现 21 个 endpoint 测试类，258/258 通过。

81. **Services 回归预设漏掉多类核心服务测试**
    - 证据：`scripts/run-tests-services-regression.ps1:21-29` 只列 7 个类；未纳入的服务测试包括 `AuthServiceTests.cs:11`、`ProjectServiceTests.cs:11`、`InspectionImagePersistenceServiceTests.cs:15`。
    - 风险：服务层回归脚本名称给出覆盖错觉。
    - 建议：按目录或 trait 维护服务回归集，并加清单漂移检测。
    - 修复：`scripts/run-tests-services-regression.ps1` 已改为自动发现 `ClearVision.Product.Tests/Services` 下所有 `*Tests.cs`，并保留 `Events/InMemoryInspectionEventBusTests.cs`；新增 `-MinimumServiceTestClasses` 哨兵避免清单静默缩水。最新本地发现 31 个服务回归类，259/259 通过。

82. **固定测试脚本没有最小测试数校验**
    - 证据：`run-dotnet-test-serial.ps1:131-132`、`:274-283` 只有传入最小数才校验；`run-tests-services-regression.ps1:19-30` 和 `run-tests-desktop-endpoints.ps1:19-26` 未设置结果文件/最小数。
    - 风险：类名重命名或过滤器拼写错误时可能“没有实际测试却成功”。
    - 建议：所有固定预设统一输出 TRX 并传 `-MinimumTotalTests`。
    - 修复：`run-tests-desktop-endpoints.ps1` 与 `run-tests-services-regression.ps1` 已默认写入 `.tmp/test_results/...` TRX，并向串行 runner 传入 `MinimumTotalTests=200`；两个脚本仍可通过参数覆盖最小值。最新 endpoint preset 258/258、services preset 259/259 均通过 TRX 最小数量校验。

83. **operator_contract_check.ps1 覆盖面小于 quality contract lane**
    - 证据：`scripts/operator_contract_check.ps1:23-30` 只列 7 个 xUnit 类；`quality/evals/suites/quick_contract_suite.json:15-19` 起有多个 active contract runner。
    - 风险：接口/算子契约证据分散，脚本名容易让人以为已覆盖全部契约。
    - 建议：让脚本调用 `quick_contract_suite --run`，或明确拆成 xUnit smoke 与 full contract gate。
    - 修复：`operator_contract_check.ps1` 默认改为调用 `quality/tools/run_quality_suite.py --suite quick_contract_suite --run`；保留 `-XunitSmokeOnly` 兼容旧 7 类 xUnit smoke，并提供 `-SuiteMode validate-only|dry-run|list`。本地 validate-only/dry-run 均覆盖 11 个 active suite entry，旧 xUnit smoke 86/86 通过。

84. **CI 未接入 quick contract suite**
    - 证据：`quality/evals/suites/README.md:5` 说明 quick lane “local/CI-friendly”；主 CI 质量步骤只跑 `.github/workflows/ci.yml:312` 的 `agent_engineering_harness_suite`。
    - 风险：quality/tools 中 active contract baseline 不进入主线质量闸。
    - 建议：在 CI 增加 quick contract validate/run lane，重型 dataset suite 可继续手动或计划任务。
    - 修复：`.github/workflows/ci.yml` 已新增 `Run Quick Contract Quality Suite`，在主 CI 中执行 `quick_contract_suite --run`，并通过 `quick-contract-quality-suite` artifact 上传 contract baseline 报告。

85. **run_quality_suite.py 选择不到 runnable entry 时返回成功**
    - 证据：`quality/tools/run_quality_suite.py:226-230` 打印 `No runnable entries selected.` 后 `return 0`。
    - 风险：拼错 `--entry` 或只选中 planned/disabled entry 会被 CI/脚本视作成功。
    - 建议：`--run/--entry` 场景无 runnable entry 返回非零，除非显式 `--allow-empty`。
    - 修复：`run_quality_suite.py` 在 `--run/--dry-run` 选不到 runnable entry 时改为 stderr 输出 `error: no runnable entries selected.` 并返回 2；本地 `--run --entry __missing__` 已验证返回 2。

86. **quality suite 不校验本次 run 是否刷新产物**
    - 证据：`run_quality_suite.py:130-135` 只校验现有 baseline/report；`:187-191` 运行命令后只看 exit code。
    - 风险：runner 返回 0 但未写新文件时，CI 可能上传旧报告。
    - 建议：记录执行前后 mtime/hash，并按 entry 的 baseline/report 做 post-run freshness 校验。
    - 修复：`run_quality_suite.py` 在每个非 dry-run entry 执行前记录时间戳，命令成功后对声明的 `baselineJson`/`reportMarkdown` 做存在性与 freshness 校验，未刷新则返回 2；临时 suite 已验证 stale artifact 失败、fresh artifact 成功，quick contract validate/dry-run 不受影响。

87. **虚拟 PLC 测试默认直接 return**
    - 证据：`ModbusCommunicationOperatorVirtualPlcTests.cs:23-26` 缺环境直接返回；`VirtualMcFinsPlcConnectionTests.cs:12-15` 同样返回；`run-tests-plc-regression.ps1:153-161` 仅在 `-Virtual` 时设置环境并改 filter。
    - 风险：PLC 回归可能只验证非虚拟路径，虚拟连接实际未跑却显示通过。
    - 建议：分离 `plc-unit` 与 `plc-virtual` gate；虚拟 gate 缺环境应 skip/failed 可见，并强制最小数量。
    - 修复：`run-tests-plc-regression.ps1` 默认过滤为 `FullyQualifiedName~PlcComm&Category!=VirtualPLC`，避免无环境虚拟测试通过空 `return` 伪装成验证；`-Virtual` gate 启动 Modbus 与 MC/FINS 虚拟 PLC 后纳入虚拟测试，并默认写 TRX 与强制最小测试数。默认 PLC gate 72/72 通过，virtual gate 80/80 通过且最小数量校验生效。

88. **存在显式永久 Skip 的 xUnit 测试**
    - 证据：`FlowEditorTests.cs:16`、`:29`；`Step123_GapAnalysisImplementationTests.cs:39`、`:59` 存在 Skip。
    - 风险：UI 和 P1 gap 验收永远不提供回归信号。
    - 建议：迁移到 Playwright/可启动 host 的集成测试，或建立跳过测试 owner/到期日审计。
    - 修复：`Step123_GapAnalysisImplementationTests` 中 Comparator 与 Measurement 两个 P1 gap 测试已恢复执行并通过；`FlowEditorTests` 的运行中应用依赖 Skip 已补 `owner=ui` 与 `expires=2026-08-31`。新增 `XunitSkipGovernanceTests` 扫描 xUnit Skip 属性，强制包含 owner 和未过期 expiry； targeted 4/4 通过，当前显式 Skip 仅剩两条带治理字段的 FlowEditor UI 迁移项。

89. **测试中存在异常后强制成功的静默跳过**
    - 证据：`SubPixelEdgeDetectorTests.cs:375-378` catch 后 `Assert.True(true, "...跳过...")`。
    - 风险：OpenCV 对比基准失败会被记为成功。
    - 建议：改为显式 Skip 机制或失败，并把依赖可用性检查前置到 fixture。
    - 修复：移除 `SubPixelEdgeDetectorTests` 中 catch 后强制成功的分支；原用例暴露出 1D 灰度重心与 2D `cornerSubPix` 角点优化比较不等价，已改为与 OpenCV moments 的强度重心基准比较。`SubPixelEdgeDetectorTests` 25/25 通过，`Assert.True(true)` 强制成功模式已扫空。

90. **工业样本 benchmark 缺样本时直接 return**
    - 证据：`IndustrialMeasurementBenchmarkTests.cs:28-31`、`:53-56` 缺样本直接返回；实际样本路径来自 `IndustrialMeasurementBenchmarkTests.cs:100-105`。
    - 风险：真实工业样本回归可在样本缺失时静默变成空跑。
    - 建议：缺样本应失败或显式 trait skip，并在回归脚本里校验样本资产存在。
    - 修复：`IndustrialMeasurementBenchmarkTests` 已移除 `SampleExists()` 与缺样本 `return`，不再依赖 `publish/.../10.png` 外部产物；基准 JSON 改为声明确定性工业几何场景，由测试内 `IndustrialMeasurementSceneFactory` 生成圆/线输入并执行真实算子断言。targeted `IndustrialMeasurementBenchmarkTests` 2/2 通过。

91. **Phase42 固定回归预设缺少默认最小测试数**
    - 证据：`run-tests-phase42-regression.ps1` 修复前虽输出 TRX，但未向串行 runner 传入 `MinimumTotalTests`，类名过滤器大幅漂移时可能仍以成功退出掩盖覆盖缩水。
    - 风险：AGENTS 推荐的固定预设会给出“已跑 Phase42 回归”的错觉，但实际测试数可显著低于预期。
    - 建议：固定预设默认设置 TRX 计数门槛，并保留参数覆盖能力。
    - 修复：`scripts/run-tests-phase42-regression.ps1` 已新增 `-MinimumTotalTests`（默认 120）、`-ResultsDirectory`、`-LogFileName` 与 `-ReturnExitCode`，并默认把最小数量传给 `run-dotnet-test-serial.ps1`。本地 `& "./scripts/run-tests-phase42-regression.ps1" -NoBuild -NoRestore` 通过 132/132，TRX 最小数量校验生效。

92. **Recognition 固定回归预设缺少默认最小测试数**
    - 证据：`run-tests-recognition-regression.ps1` 修复前只固定 `CodeRecognitionOperatorTests`、`OcrRecognitionOperatorTests` 过滤器并写 TRX，未启用 `MinimumTotalTests`。
    - 风险：识别/OCR 回归过滤器漂移或测试类重命名时，可能在覆盖缩水后仍显示脚本成功。
    - 建议：与其他固定回归预设一致，默认启用 TRX 最小数量校验并保留覆盖参数。
    - 修复：`scripts/run-tests-recognition-regression.ps1` 已新增 `-MinimumTotalTests`（当前默认 12）、`-ResultsDirectory`、`-LogFileName` 与 `-ReturnExitCode`。本地 `& "./scripts/run-tests-recognition-regression.ps1" -NoBuild -NoRestore` 通过 12/12，TRX 最小数量校验生效。

93. **工业测量 benchmark 未纳入 measurement regression**
    - 证据：`run-tests-measurement-regression.ps1` 修复前手写 19 个测量测试类，未包含 `IndustrialMeasurementBenchmarkTests`；`run-operator-library-industrial-gate.ps1` 对 measurement-regression 只传 `MinimumTotalTests=19`。
    - 风险：已修复的工业几何 benchmark 只能被 targeted run 覆盖，常用测量回归和 industrial gate 仍可能漏掉该基准。
    - 建议：把 benchmark 纳入 measurement regression，并把默认/industrial gate 的最小测试数改为真实测试数量口径。
    - 修复：`scripts/run-tests-measurement-regression.ps1` 已加入 `IndustrialMeasurementBenchmarkTests`，默认写 `.tmp/test_results/measurement-regression` TRX 并启用 `MinimumTotalTests=140`；`run-operator-library-industrial-gate.ps1` 的 measurement-regression 门槛同步升到 140。本地 `& "./scripts/run-tests-measurement-regression.ps1" -NoBuild -NoRestore` 通过 146/146，TRX 最小数量校验生效。

94. **Measurement accuracy/stability gate 最小测试数使用类数量口径**
    - 证据：`run-operator-library-industrial-gate.ps1` 修复前对 `measurement-accuracy` 传 `MinimumTotalTests=17`、对 `measurement-stability` 传 `MinimumTotalTests=1`；两个子脚本 standalone 默认仍为 0。
    - 风险：industrial gate 中测量精度/稳定性测试可大幅缩水而仍满足过低门槛。
    - 建议：按真实测试数量设置默认门槛，并让 standalone 预设默认写 TRX。
    - 修复：`run-tests-measurement-accuracy.ps1` 默认写 `.tmp/test_results/measurement-accuracy` 并启用 `MinimumTotalTests=120`；`run-tests-measurement-stability.ps1` 默认写 `.tmp/test_results/measurement-stability` 并启用 `MinimumTotalTests=15`；industrial gate 同步使用 120/15。两条预设本地分别通过 122/122、16/16，TRX 最小数量校验生效。

95. **Detection/Calibration gate 最小测试数使用类数量口径**
    - 证据：`run-operator-library-industrial-gate.ps1` 修复前对 `calibration` 传 `MinimumTotalTests=11`、对 `detection-regression` 传 `16`、对 `detection-accuracy` 传 `9`；对应真实测试数分别为 109、126、134。
    - 风险：industrial gate 中标定/检测回归可大幅缩水而仍满足类数量式低门槛。
    - 建议：多 gate 脚本按 gate 设置默认测试数量门槛，industrial gate 同步使用真实测试数量口径。
    - 修复：`run-tests-detection-regression.ps1` 已按 gate 设置默认门槛：regression=120、detection-accuracy=130、detection-stability=25、all=260，并默认写 `.tmp/test_results/detection-*` TRX；`run-tests-calibration-regression.ps1` 已按 gate 设置默认门槛：regression=105、integration=7、all=115，并默认写 `.tmp/test_results/calibration-*` TRX；industrial gate 同步使用 calibration=105、detection-regression=120、detection-accuracy=130。本地三条 industrial gate 子路径分别通过 109/109、126/126、134/134，TRX 最小数量校验生效。

96. **Performance gate 相对报告目录会写入测试输出目录**
    - 证据：`run-tests-measurement-performance.ps1`、`run-tests-detection-performance.ps1` 修复前直接把 `-ReportDirectory` 或 `CV_*_PERF_REPORT_DIR` 传给测试；相对路径会被测试进程按 `bin/Debug/net8.0` 当前目录解析。两个脚本 standalone 默认也未写 TRX/启用最小测试数。
    - 风险：调用者以为报告在仓库根相对路径，实际 artifact 写到测试输出目录，CI/industrial gate 可漏收性能报告；standalone performance gate 缺少 TRX 计数证据。
    - 建议：脚本层将 report/failure archive 路径归一化为仓库根相对的绝对路径，并默认写 TRX、校验 1 个 performance budget suite。
    - 修复：`run-tests-measurement-performance.ps1` 与 `run-tests-detection-performance.ps1` 已将 `-ReportDirectory`、对应 `CV_*_PERF_REPORT_DIR`、通用 `CV_PERF_REPORT_DIR` 归一化为绝对路径；detection failure archive 也跟随归一化 report dir。两个脚本 standalone 默认写 `.tmp/test_results/*-performance` TRX 并启用 `MinimumTotalTests=1`。本地以相对 `-ReportDirectory .tmp/test_results/.../reports` 验证通过，报告文件落在仓库根 `.tmp` 下，两个 performance gate 均 1/1 通过并完成 TRX 校验。

97. **Performance gate 不校验报告是否为本次运行刷新**
    - 证据：`run-tests-measurement-performance.ps1`、`run-tests-detection-performance.ps1` 修复前只依赖 dotnet test/TRX 结果；预算测试虽然会写 markdown/json 报告，但脚本层未校验报告存在性与 LastWriteTime，旧报告可被误收作本次证据。
    - 风险：测试返回 0 但报告未写入、写到错误位置或沿用旧文件时，CI/industrial gate 仍可能上传过期性能证据。
    - 建议：脚本内部强制以 `-ReturnExitCode` 调用串行 runner，dotnet 成功后继续做报告 artifact 存在性与 freshness 校验，再统一返回/退出。
    - 修复：两个 performance 脚本已在运行前记录 UTC 时间，dotnet 成功后校验 markdown/json 报告存在且 `LastWriteTimeUtc` 不早于本次运行启动时间；报告校验失败会将脚本退出码置为 1。已验证普通调用和 `-ReturnExitCode` 调用均通过：measurement performance 1/1、detection performance 1/1，并输出 `Report artifact validation passed`；`-ReturnExitCode` 模式下 `LASTEXITCODE=0`。

98. **主 CI performance budget 步骤绕过报告 freshness gate**
    - 证据：`.github/workflows/ci.yml` 修复前只用 `run-dotnet-test-serial.ps1 -Filter "Category=PerformanceBudget"` 跑产品性能分类，`MinimumTotalTests=1`；该分类当前实际包含 10 个测试，且 measurement/detection 报告 freshness 只在专用脚本中校验。CI 后续只单独运行 detection performance gate，未运行 measurement performance gate。
    - 风险：性能分类缩水到少量测试仍可通过；measurement performance 报告可能未刷新但不影响主 CI；detection performance TRX 未显式进入 `TestResults` artifact。
    - 建议：主 CI 保留泛化 performance category 测试，但把最小数量提升到当前真实规模；同时用专用脚本分别运行 measurement/detection 报告型 gate，并上传对应报告 artifact。
    - 修复：CI 的 `Run Product Performance Budget Tests` 已改为 `MinimumTotalTests=10`、`MinimumPassedTests=10`；新增 `Run Measurement Performance Gate`，通过 `run-tests-measurement-performance.ps1` 执行 freshness-checking gate，并上传 `measurement-performance-report` artifact；detection performance gate 显式写 `TestResults/detection-performance/detection-performance.trx` 并指定 `ReportDirectory=ClearVision.Product/test_results`。本地已验证 product performance category 10/10 通过，measurement/detection CI 参数形态均 1/1 通过并完成报告 freshness 校验。

99. **Data processing remediation 固定脚本缺少 TRX 与最小测试数**
    - 证据：`run-tests-data-processing-remediation.ps1` 修复前手写 phase1/phase2 类清单后直接调用串行 runner，未提供 `ResultsDirectory`、`LogFileName`、`MinimumTotalTests` 或 `ReturnExitCode` 参数。
    - 风险：数据处理专项回归过滤器漂移、测试类改名或测试数缩水时，脚本缺少可审计 TRX 证据和数量闸；phase2 的 Docker 集成前置失败虽然会报错，但通过路径没有数量保障。
    - 建议：与其它固定回归预设一致，默认输出 `.tmp` TRX，按 gate 设置测试数量门槛，并保留参数覆盖能力。
    - 修复：`run-tests-data-processing-remediation.ps1` 已新增 `-ResultsDirectory`、`-LogFileName`、`-MinimumTotalTests`（默认自动：phase1=140、phase2=150）和 `-ReturnExitCode`，默认写 `.tmp/test_results/data-processing-remediation-*`。已通过 `--list-tests` 发现 phase2 153 条；本地 Docker 不可用，完整 phase2 gate 明确失败并提示 Docker 前置条件；非 Docker 主体 150/150 通过并完成 TRX 最小数量校验。

100. **Data processing remediation 完整 gate 被 Docker 前置条件阻断**
    - 证据：`run-tests-data-processing-remediation.ps1` 的 phase1/phase2 默认清单包含 `DatabaseWriteOperatorMultiDbIntegrationTests`，在 Docker 不可用时会在执行前失败；但非 Docker 主体已有 150 条 phase2 测试可稳定执行。
    - 风险：CI/本地无 Docker 环境无法运行数据处理专项主体回归，容易在“完整 gate 跑不了”和“主体测试未纳入 CI”之间形成覆盖空洞。
    - 建议：保留完整 gate 的 Docker 硬要求，同时提供显式非 Docker gate，并把该子集接入主 CI。
    - 修复：`run-tests-data-processing-remediation.ps1` 新增 `-SkipDockerIntegration`，选择该模式时从 phase 清单中移除 `DatabaseWriteOperatorMultiDbIntegrationTests`，Docker 预检仅在完整 gate 中执行；CI 新增 `Run Data Processing Remediation Gate`，以 phase2 + `-SkipDockerIntegration` + `MinimumTotalTests=150` 覆盖非 Docker 主体。已验证非 Docker phase2 150/150 通过并完成 TRX 校验；完整 phase2 在本机无 Docker 时仍明确失败并提示可用 `-SkipDockerIntegration` 跑子集。

101. **主 Product coverage gate 最小测试数过低且全量套件存在隐性红点**
    - 证据：`.github/workflows/ci.yml` 的 `Run Product Tests with Coverage` 修复前只要求 `MinimumTotalTests=1`、`MinimumPassedTests=1`；本地同过滤器 `Category!=PerformanceBudget` 实际应覆盖 3318 条测试。首次全量复跑暴露 3 个失败：`OperatorKnowledgeGraphTests` 发现 `SerialCommunication` 静态知识图谱缺 `ResponseWaitMs`，串口取消测试在并行压力下可能等到外层 timeout，结果后台服务 dead-letter 文件出现与图片预算释放之间存在断言竞态。
    - 风险：主 Product 测试过滤器漂移或大量测试未执行时，CI 仍可能因 1/1 通过而放行；同时不稳定测试会让真实回归与测试时序抖动混在一起。
    - 建议：先修复全量套件红点，再按当前真实规模设置保守最小测试数。
    - 修复：使用 `OperatorKnowledgeGraphRunner` 重生成 `operator_knowledge_graph.json`/`operator_knowledge_cards.json`/报告，使 `SerialCommunication.ResponseWaitMs` 与元数据对齐；`SerialCommunicationOperatorTests` 改为由 fake 串口在响应字节轮询点同步触发取消，避免依赖 20ms timer；`InspectionResultBackgroundServiceTests` 改为等待 dead-letter 出现后下一条带图结果可重新入队，消除 dead-letter 写入与预算释放之间的竞态；CI 主 Product coverage gate 提升为 `MinimumTotalTests=3300`、`MinimumPassedTests=3300`。已验证 targeted 3/3 通过，Product 非性能全量 3318 total / 3316 passed / 2 skipped 通过并完成 3300/3300 TRX 校验。

102. **主 Desktop coverage gate 最小测试数过低**
    - 证据：`.github/workflows/ci.yml` 的 `Run Desktop Tests with Coverage` 修复前只要求 `MinimumTotalTests=1`、`MinimumPassedTests=1`；本地同项目全量实际为 521 条测试。
    - 风险：Desktop endpoint、station sync、camera stream、settings/user 等桌面集成测试如果因项目路径、过滤器或发现问题缩水，CI 仍可能因 1/1 通过而放行。
    - 建议：按当前真实规模设置保守门槛，并保留 coverage artifact 输出。
    - 修复：CI Desktop coverage gate 提升为 `MinimumTotalTests=500`、`MinimumPassedTests=500`。本地已验证 `ClearVision.Product.Desktop.Tests` 全量 521/521 通过，TRX 计数校验可覆盖该门槛。

103. **OperatorLibrary package smoke gate 最小测试数过低**
    - 证据：`.github/workflows/ci.yml` 的 `Smoke Test OperatorLibrary Package with Coverage` 修复前只要求 `MinimumTotalTests=1`、`MinimumPassedTests=1`；本地按 CI pack/restore 形态运行 smoke project 实际为 40 条测试。
    - 风险：NuGet 包级 smoke 测试如果因包版本、restore、测试发现或过滤问题只跑少量用例，CI 仍可能因 1/1 通过而放行，削弱 OperatorLibrary 发布包可用性信号。
    - 建议：按当前 package smoke 真实规模设置严格门槛，并保持 package version 属性传递。
    - 修复：CI OperatorLibrary package smoke gate 提升为 `MinimumTotalTests=40`、`MinimumPassedTests=40`。本地已按 CI 流程临时 pack `ClearVision.OperatorLibrary`、restore smoke project 并验证 40/40 通过；随后以 `-NoBuild -NoRestore` 复跑并完成 40/40 TRX 校验。

104. **Operator performance benchmark smoke 未校验 artifact 是否本次刷新**
    - 证据：`.github/workflows/ci.yml` 修复前直接运行 `OperatorPerformanceBenchmarkRunner --mode smoke --output artifacts/operator-performance-benchmark.json --report artifacts/operator-performance-benchmark.md`，随后上传 artifact；workflow 层没有检查 JSON/Markdown 是否存在、mtime 是否晚于本次运行开始，也没有校验 smoke case count。
    - 风险：runner 返回成功但输出路径异常、报告未写入或 CI 上传旧文件时，operator performance benchmark artifact 会给出过期证据；benchmark case 清单缩水也可能只从日志中被动发现。
    - 建议：与 measurement/detection performance gate 一致，在 CI 步骤内做 post-run artifact freshness 与基础 schema/count 校验。
    - 修复：`Run Operator Performance Benchmark Smoke` 改为 PowerShell block，记录 `$startedAtUtc`，运行 benchmark 后强制检查 JSON/Markdown 存在且 `LastWriteTimeUtc` 不早于本次运行开始；解析 JSON 要求 `summary.mode=smoke`、`caseCount>=4`、`passed>=4`、`failed=0`。本地用 repo dotnet shim 运行同款 smoke，4/4 通过，artifact validation passed；仅出现既有 `System.Collections.Immutable` 版本冲突 warning。

105. **UI unit suite 只校验存在测试文件，不校验发现数量**
    - 证据：`ClearVision.Product/tests/ClearVision.Product.UI.Tests/tests/unit/run-all-unit-tests.mjs` 修复前只检查 `.test.mjs` 文件数不为 0，然后把文件列表交给 `node --test`；CI 的 `npm run test:unit` 因此无法发现 UI unit suite 大幅缩水。
    - 风险：测试文件被误移除、glob 目录变化、Node test 发现异常或大量测试没有注册时，前端 unit gate 仍可能以少量测试通过。
    - 建议：runner 层固定 TAP reporter，解析 summary，并对测试文件数和测试数设置保守下限，同时允许环境变量临时覆盖。
    - 修复：`run-all-unit-tests.mjs` 新增默认门槛 `CV_UI_UNIT_MIN_FILES`（默认 25）和 `CV_UI_UNIT_MIN_TESTS`（默认 650）；使用 `--test-reporter=tap` 收集 summary，要求 `tests/pass >= 650` 且 `fail=0`，并输出 summary validation。已验证 `npm run test:unit` 通过 26 个文件、674/674 条测试；临时设置 `CV_UI_UNIT_MIN_FILES=999` 会在执行前按预期失败。

106. **Coverage summary 在缺少 Cobertura artifact 时仍成功**
    - 证据：`.github/workflows/ci.yml` 的 `Write Coverage Summary` 修复前在 `TestResults` 下找不到 `coverage.cobertura.xml` 时只写入 `No Cobertura coverage files were found.`，不会失败；而 Product 与 Desktop 测试步骤都显式启用了 `-Collect "XPlat Code Coverage"`。
    - 风险：coverlet collector 未加载、结果目录异常或 coverage artifact 生成失败时，CI 仍可能生成一个空 coverage summary 并继续上传，导致覆盖率证据缺失却不红。
    - 建议：coverage summary 应要求 Product 与 Desktop 两个根目录分别至少有一个 Cobertura 文件，并校验 XML 中可解析 `line-rate`/`branch-rate`。
    - 修复：`Write Coverage Summary` 现在先检查 `TestResults/product` 和 `TestResults/desktop` 下各至少一个 `coverage.cobertura.xml`，随后解析所有 Cobertura 文件并要求存在 `line-rate`/`branch-rate`，否则直接 throw。已用本地历史 `TestResults` 正向验证 22 个 Cobertura 文件可生成 summary；在空 `.tmp/coverage-negative` 目录中负向验证会因缺少 `TestResults/product` coverage 按预期失败。

107. **upload-artifact 默认缺文件只 warning**
    - 证据：`.github/workflows/ci.yml` 和 `.github/workflows/vision-agent-quality.yml` 的 `actions/upload-artifact@v4` 步骤修复前均未设置 `if-no-files-found`；GitHub 默认行为是缺少匹配文件时只 warning。
    - 风险：operator catalog、性能报告、TRX/coverage、OperatorLibrary 包、quality suite 报告、UI/industrial/release artifact 等关键证据如果路径漂移或未生成，workflow 可能继续执行，只在日志中留下 warning。
    - 建议：关键上传步骤统一使用 `if-no-files-found: error`，让 artifact 缺失成为明确 CI 失败。
    - 修复：主 CI 和独立 `vision-agent-quality.yml` 的所有 `upload-artifact` 步骤均新增 `if-no-files-found: error`。已用脚本扫描确认两个 workflow 内每个 `upload-artifact` 附近都有该策略，并通过 YAML 解析；`git diff --check` 仅有 LF→CRLF warning。

108. **Agent UI contract gate 缺少测试数量守门**
    - 证据：`vision-agent-safe-ci.yml` 通过 `npm run test:agent-ui-contract` 跑前端 Agent contract；修复前该 npm script 直接执行 `node --test tests/unit/ai-agent-ui-contract.test.mjs`，没有解析 TAP summary，也没有最小测试数量要求。
    - 风险：`ai-agent-ui-contract.test.mjs` 大幅缩水、测试注册失败或脚本路径漂移时，只要残留少量测试通过，Safe CI 仍可能把 Agent UI 合同视为已覆盖。
    - 建议：与 UI unit runner 一致，使用 TAP reporter 收集 summary，并在 runner 层强制 `tests/pass` 数量下限与 `fail=0`。
    - 修复：新增 `run-agent-ui-contract-tests.mjs`，默认 `CV_AGENT_UI_CONTRACT_MIN_TESTS=340`，解析 Node TAP summary 并要求 total/pass 均达到门槛且 fail 为 0；`package.json` 的 `test:agent-ui-contract` 已切换到该 runner，Safe CI 无需改 workflow 即可继承守门。已验证正向 `npm run test:agent-ui-contract` 通过 351/351；负向设置 `CV_AGENT_UI_CONTRACT_MIN_TESTS=999` 时测试本身 351/351 后按预期失败并报告覆盖缩水。

109. **PLC regression 默认数量门槛低于当前真实规模**
    - 证据：`run-tests-plc-regression.ps1` 修复前默认 `MinimumTotalTests` 为 40，`-Virtual` 默认 50；`run-operator-library-industrial-gate.ps1` 的 PLC 子 gate 还显式传入 1。当前非虚拟 PLC 过滤器实际执行 72 条，虚拟 PLC gate 实际执行 80 条。
    - 风险：PLC/通讯协议回归测试如果因过滤器、类名或发现问题缩水到 40-50 条，standalone gate 与工业总 gate 仍可能通过，削弱 PLC 通讯层质量信号。
    - 建议：按当前真实测试规模设置保守默认门槛，并让工业总 gate dry-run/日志暴露同一数量预期。
    - 修复：`run-tests-plc-regression.ps1` 默认最小数量提升为非虚拟 70、虚拟 80；`run-operator-library-industrial-gate.ps1` 的 PLC 子 gate 显式传 `MinimumTotalTests=70`。已验证非虚拟 PLC gate 72/72 通过并按 70 校验；负向 `MinimumTotalTests=999` 按预期失败；虚拟 PLC gate 启动 Modbus 与 MC/FINS smoke 后 80/80 通过并按 80 校验；工业总 gate `-Gate plc -DryRun` 已显示 PLC 子命令携带 `-MinimumTotalTests 70`。

110. **Preview smoke 只看退出码不看断言规模**
    - 证据：`package.json` 的 `test:preview-smoke` 直接执行 `preview-regression.smoke.mjs`；脚本内包含多段 preview coordinator、source wiring 和 result panel 断言，但修复前只输出 `preview regression smoke passed`，没有记录或校验断言数量。
    - 风险：未来误删大块 smoke 覆盖、提前返回或只保留少量断言时，CI 仍可能因为脚本退出码为 0 而认为 preview 回归已覆盖。
    - 建议：在 smoke 脚本内部通过断言代理累计通过断言数，并设置可覆盖的最小断言数量门槛。
    - 修复：`preview-regression.smoke.mjs` 新增 `smokeAssert` 计数代理和 `CV_PREVIEW_SMOKE_MIN_ASSERTIONS`（默认 35）；脚本结束时要求断言数达到门槛，否则以非零退出并报告覆盖缩水。已验证 `npm run test:preview-smoke` 通过并输出 38 个断言；负向设置 `CV_PREVIEW_SMOKE_MIN_ASSERTIONS=999` 时按预期失败。

111. **BaselineBenchmark 写出失败算子报告后仍返回成功**
    - 证据：`scripts/BaselineBenchmark/Program.cs` 会在某个算子没有成功样本时把该算子的 `Status` 写成 `Failed`，但修复前报告写出后统一 `return 0`；同时 `--iterations 0` 会形成空采样配置。
    - 风险：性能基线产物已经包含失败或部分采样失败的算子时，调用脚本/CI 仍可能把 benchmark 当作成功证据；空迭代参数也可能生成无意义结果。
    - 建议：基线报告写出后校验每个算子都达到 `图片数 * iterations` 的成功样本数，且 `Status=OK`、`Error` 为空；无效迭代参数直接失败。
    - 修复：`BaselineBenchmark` 新增 `--iterations > 0` 与 `--warmup >= 0` 参数校验；报告写出后要求所有算子完整采样，否则逐项输出失败摘要并返回 4。已验证正常基线 `--iterations 2 --warmup 0` 生成 10 个算子、2 张图、每算子 4 个样本且退出 0；负向 `--iterations 0` 按预期返回 2。

112. **BaselineBenchmark 参数拼写错误会静默使用默认值**
    - 证据：`BaselineOptions.Parse` 修复前对 `--iterations`/`--warmup` 使用 `int.TryParse(...)? parsed : 原值`，未知参数也不报错；例如 `--iterations abc` 会继续使用默认 8 次迭代，输出看似有效的基线报告。
    - 风险：手工复跑或自动脚本里参数拼错、漏值或传错类型时，benchmark 仍可能返回成功并沉淀错误口径的性能证据。
    - 建议：参数解析改为严格模式：未知参数、缺少值、非整数值都应立即失败，并保留后续范围校验。
    - 修复：`BaselineOptions.Parse` 现在对未知参数、缺值和非整数值抛出明确错误，入口捕获后返回 2；`--iterations`/`--warmup` 的范围校验继续保留。已验证正向 `--iterations 1 --warmup 0` 生成 10 个算子、2 张图、每算子 2 个样本且退出 0；负向 `--iterations abc`、缺少 `--output` 值、未知 `--unknown`、`--warmup -1` 均按预期返回 2。

113. **G16 release benchmark 参数错误和场景缩水不够显性**
    - 证据：`scripts/run-g16-release-benchmark.mjs` 修复前手写参数解析会忽略未知参数，`--out-dir --iterations` 也会把下一个 option 当作目录名；报告写出前未校验必须包含 300/1000 primitive 两个发布场景。
    - 风险：发布收口 benchmark 复跑时如果参数拼错、缺值或场景数组被误改，仍可能写出看似有效的 G16 release artifact，削弱发布证据链。
    - 建议：参数解析改为严格模式，并在写 JSON/Markdown 前校验场景数量、primitive count、iterations 与关键数值指标。
    - 修复：`run-g16-release-benchmark.mjs` 新增 `readRequiredValue`、`parseIntegerArgument` 和 `validateReport`；未知参数、缺值、非整数会失败，报告必须包含 300 与 1000 两个场景且关键指标为有限数值。已验证正向 `--iterations 30 --warmup 0` 生成 2 个场景并通过产物解析；负向缺少 `--out-dir` 值、未知 `--unknown`、`--iterations abc`、`--iterations 29` 均按预期失败。

114. **Quality report size guard 匹配不到报告时仍可通过**
    - 证据：`scripts/check-quality-report-size.ps1` 修复前只在报告目录不存在或发现超限文件时失败；如果目录存在但 `ReportExtensions` 配错、目录为空或没有匹配 `.json/.csv`，脚本会输出通过。
    - 风险：CI 的 `Quality Report Size Scan` 可能在报告路径或扩展名漂移后失去实际扫描对象，却继续给出绿色前置质量信号。
    - 建议：要求至少扫描到一定数量的报告文件，并校验 `MaxReportMB`、`MinimumReportFiles` 与扩展名配置有效。
    - 修复：`check-quality-report-size.ps1` 新增 `MinimumReportFiles`（默认 1），要求匹配报告数量达到门槛；同时校验 `MaxReportMB > 0`、`MinimumReportFiles >= 0`、`ReportExtensions` 非空，并在通过日志输出实际文件数。已验证默认目录扫描 244 个报告并通过；负向空目录、`MaxReportMB=0`、`MinimumReportFiles=-1`、空扩展名均按预期失败。

115. **Diff hygiene 未检查 git diff --check 命令级失败**
    - 证据：`scripts/check-diff-hygiene.ps1` 修复前调用 `git diff --check` 后只从输出中提取 `file:line:` 形式的 whitespace finding；如果 `git diff --check` 因 Git 命令异常返回非零但输出不是该格式，脚本可能继续后续 diff 解析并最终通过。
    - 风险：CI 的早期 diff hygiene gate 在 Git 环境、range 或命令级异常下可能失去 whitespace 检查信号，给出误导性绿灯。
    - 建议：区分“发现 whitespace finding”和“命令级失败”；非零退出且没有可报告 finding 时应 fail-closed。
    - 修复：`check-diff-hygiene.ps1` 现在检查 `git diff --check` 的退出码；若非零且没有解析出具体 finding，会抛出 `Unable to run git diff --check`。已验证 `-BaseRef HEAD` 正向通过；临时 mock `git diff --check` 返回 128 且输出非 finding 时按预期失败；缺失 base ref 也保持 fail-closed。

116. **Text encoding scan 单文件/缺失 root 场景不稳**
    - 证据：`scripts/check-text-encoding.ps1` 修复前 `Get-RootFiles` 返回单个文件时会被 PowerShell 拆成标量，随后在 StrictMode 下访问 `$files.Count` 崩溃；缺失 root 也只是 `continue`，可能导致 root 扫描没有候选文件却通过。
    - 风险：手工或 CI 配置如果只扫描单个文件、路径拼错或 root 被移动，编码扫描信号会变成脚本异常或空扫绿灯，而不是明确的配置失败。
    - 建议：保持候选文件列表始终为数组；root 模式缺失路径应 fail-closed；root 扫描默认至少需要 1 个候选文件，BaseRef 模式保留 0 个文本改动可通过。
    - 修复：`check-text-encoding.ps1` 新增 `MinimumFiles`（默认 `-1` 自动模式），root 扫描默认至少 1 个文件，BaseRef 扫描默认 0；`Get-RootFiles` 对缺失 root 直接抛错，主流程用 `@(...)` 保持数组语义。已验证 `-SelfTest` 通过，`-Roots README.md` 扫描 1 个文件通过，`-BaseRef HEAD` 扫描 0 个文件通过；负向缺失 root、`-MinimumFiles 2`、`-MinimumFiles -2` 均按预期失败。

117. **Text encoding BaseRef 路径未检查 Git 命令退出码**
    - 证据：`check-text-encoding.ps1` 修复前在 `Get-ChangedFiles` 中直接执行 `git merge-base` 并 `.Trim()`，缺失 ref 时会变成 `You cannot call a method on a null-valued expression`；后续 `git diff --name-only` 也没有检查退出码。
    - 风险：CI 的 `Text Encoding Scan` 在 Git ref、range 或 diff 命令异常时可能给出不清晰错误，甚至在命令失败但候选列表为空时误判为通过。
    - 建议：与其他前置脚本一致，引入统一 Git 调用包装，检查 merge-base 与 diff 的退出码，并把 Git stderr 带入错误信息。
    - 修复：`check-text-encoding.ps1` 新增 `Invoke-Git`，`Get-ChangedFiles` 现在对 `git merge-base` 和 `git diff --name-only` 都 fail-closed。已验证 `-BaseRef HEAD` 正向通过；缺失 ref 现在输出清晰 `Unable to resolve merge-base...`；临时 mock `git diff --name-only` 返回 128 时按预期失败并报告 `Unable to read changed files...`。

118. **Secret scan 缺少候选文件数量守门**
    - 证据：`scripts/scan-secrets.ps1` 修复前直接把 `Get-GitPathList` 返回值赋给 `$files`，随后扫描并输出 `$files.Count`；候选列表异常为空时没有最小数量校验，单文件场景也可能受 PowerShell 标量拆箱影响。
    - 风险：Git 文件枚举或路径范围异常时，secret scan 可能在没有实际扫描对象的情况下给出绿色信号；本地未跟踪文件覆盖情况也不够显性。
    - 建议：候选列表始终数组化，并默认要求至少枚举到 1 个文件，同时允许调用者显式提高门槛。
    - 修复：`scan-secrets.ps1` 新增 `MinimumFiles`（默认 1），对候选文件数不足和负数参数 fail-closed，并用 `@(...)` 固定数组语义。已验证 `-SelfTest` 通过；`-BaseRef HEAD` 扫描 3258 个 tracked 文件通过；`-BaseRef HEAD -IncludeUntracked` 扫描 3267 个 tracked/unignored-untracked 文件通过；负向 `MinimumFiles=999999` 与 `MinimumFiles=-1` 均按预期失败。

119. **operator_contract_check xUnit smoke 缺少 TRX 数量守门**
    - 证据：`operator_contract_check.ps1 -XunitSmokeOnly` 是保留给旧 7 类 xUnit operator contract smoke 的兼容路径；修复前它只把 `FullyQualifiedName` 类清单交给串行 runner，没有默认 TRX 输出或最小测试数量。
    - 风险：旧兼容 smoke 如果因类名漂移、过滤器缩水或发现异常只跑少量测试，仍可能以成功退出掩盖覆盖缩水。
    - 建议：兼容 xUnit smoke 也应与固定预设一致，默认写 TRX，并按当前真实规模设置保守最小测试数。
    - 修复：`operator_contract_check.ps1` 新增 `ResultsDirectory`、`LogFileName`、`MinimumTotalTests`（默认 80），`-XunitSmokeOnly` 通过 `run-dotnet-test-serial.ps1` 写 `.tmp/test_results/operator-contract-smoke/operator-contract-smoke.trx` 并启用 TRX 数量校验。已验证 xUnit smoke 86/86 通过并按 80/80 校验；负向 `MinimumTotalTests=999` 按预期失败；默认 quality suite `-SuiteMode validate-only` 仍验证 11 个 entry 并通过。

120. **operator_icon_check 全仓递归找目录导致真实 UI fallback 漂移被掩盖**
    - 证据：`operator_icon_check.ps1` 修复前会从仓库根目录递归扫描所有 `*.json`，返回第一个带 `totalCount/operators` 的文件；本地运行已因 `.tmp/python-temp/pip-build-tracker-*` 目录拒绝访问直接失败。固定到 `docs/operators/catalog.json` 后，又暴露真实 catalog 中 `AI 检测`、`AI Detection`、`Communication`、`Detection`、`Flow Control` 5 个分类缺少 category icon fallback。
    - 风险：临时目录权限、旧归档目录或生成器副本都可能让 gate 不稳定或选错 catalog；即使 gate 误绿，算子库里的英文/中英混排分类也会退回默认图标/颜色，削弱目录视觉识别。
    - 建议：catalog 来源应显式、可覆盖且 fail-closed；同时对 runtime enum、catalog operator/category 数量和 category icon/color fallback 做最小规模守门。
    - 修复：`operator_icon_check.ps1` 新增 `CatalogPath`、`MinimumRuntimeOperators`、`MinimumCatalogOperators`、`MinimumCatalogCategories`，默认读取 `docs/operators/catalog.json`，校验 `totalCount`、`operators` 数量和 `id/category` 完整性，并同步检查 category icon/color fallback；`operatorVisuals.js` 补齐 5 个真实 catalog 分类的 icon/color fallback；`app-infrastructure.test.mjs` 的 category registry 用例覆盖这些中英混排分类。已验证默认 operator icon gate 通过 160 个 runtime enum、156 个 catalog operator、31 个 catalog category；缺失 `CatalogPath`、`MinimumCatalogOperators=999`、`MinimumCatalogCategories=999` 均按预期失败；UI unit runner 通过 674/674。

121. **performance budget wrapper 只看 1/1 xUnit 与报告新鲜度，不看报告内容规模**
    - 证据：`run-tests-measurement-performance.ps1` 和 `run-tests-detection-performance.ps1` 只过滤各自 1 个 xUnit 方法，修复前脚本层只校验 TRX 1/1 与 markdown/json 报告存在且新鲜。真实报告内部分别包含 17 个 measurement entry 与 15 个 detection entry，但如果测试方法内部 case 列表被误删或报告只写少量 PASS entry，wrapper 仍可能给出绿色 artifact。
    - 风险：性能预算 gate 的最关键证据其实在报告条目里；只看 xUnit 方法数量会掩盖 benchmark case 缩水、重复、漏项或非 PASS 状态，CI artifact 可能看似新鲜但覆盖面已经退化。
    - 建议：wrapper 在 freshness 之外解析 JSON 报告，要求最小 entry 数、预期 entry 名称、名称唯一、全部 `Status=PASS`，并校验关键耗时/预算字段为有限数值。
    - 修复：两个 performance wrapper 新增 `MinimumReportEntries` 与 `ValidateReportOnly`；measurement 默认要求 17 个预期 entry，detection 默认要求 15 个预期 entry；报告校验会拒绝缺失/重复名称、非 PASS 状态、缺失或非有限的 `BudgetMs/Scale/AllowedMs/MeanMs/P95Ms/P99Ms`。已用历史报告验证 validate-only 正向通过 17/17、15/15，负向 `MinimumReportEntries=999` 均按预期失败；真实 wrapper 串行验证通过：measurement 1/1 且报告 17 entry，detection 1/1 且报告 15 entry。

122. **固定测试 wrapper 吞掉 run-dotnet-test-serial 的 TRX 守门失败退出码**
    - 证据：`run-tests-recognition-regression.ps1 -MinimumTotalTests 999` 修复前内部 `run-dotnet-test-serial.ps1` 已报告 TRX total/executed/passed 12 低于 999，但外层 wrapper 最终仍返回 exit code 0；同时 recognition 默认门槛仍停在 10，低于当前真实 12/12。
    - 风险：phase42、measurement、calibration、detection、data-processing、service/desktop endpoint 等固定 wrapper 如果只在内部打印 TRX validation failed，却把外层退出码留成 0，CI 或本地批处理会把缩水测试集误判为通过；recognition 也会允许少量测试被删后仍绿。
    - 建议：所有调用 `run-dotnet-test-serial.ps1` 的固定 wrapper 都应强制用 runner 的 `-ReturnExitCode` 模式，然后由 wrapper 显式 `return` 或 `exit` 同一退出码；recognition 默认测试数量应贴合当前真实 12 条。
    - 修复：`run-tests-recognition-regression.ps1` 默认 `MinimumTotalTests` 提升为 12；phase42、recognition、measurement regression/accuracy/stability、calibration、detection、data-processing、services regression、desktop endpoints wrapper 均改为总是让 serial runner 返回码，再由外层设置 `$global:LASTEXITCODE` 并 `exit $exitCode`（带 `-ReturnExitCode` 时 `return`）。已验证 recognition 正向 12/12 通过并按 12 校验；负向 `MinimumTotalTests=999` 现在外层返回 exit code 1；10 个修改脚本均通过 PowerShell Parser 语法检查；静态扫描确认所有 `run-tests-*.ps1` wrapper 都具备强制 runner return 与外层退出码传播，PLC/performance 使用等价的专用 `$testExitCode`/`$exitCode` 模式。

123. **quick_contract_suite 只看产物存在且绕过仓库 dotnet SDK**
    - 证据：`run_quality_suite.py` 修复前执行 active contract entry 时只检查 manifest 与文件存在，没有解析 baseline JSON/Markdown 的内容；同一入口还直接调用系统 `dotnet`，在本机触发 `global.json` 要求 9.0.300、系统 SDK 只有 10.0.101 的失败。DeepLearning contract 复跑又暴露 `label_contract_mismatch_fails` 用例仍期待 metadata/external labels 不一致时失败，但当前 `DeepLearningOperator` 和单元测试已明确 ONNX metadata 是权威来源、外部 labels 应被忽略。补上精确 operator 校验后，`g1_p3_contract_batch2` manifest 还漏列了 baseline 中 16 个真实 operator，声明 CaseCount 只覆盖 462/798。
    - 风险：contract baseline 可能已经写出失败、空报告或覆盖缩水，`--validate-only` 仍给出绿色；CI/本地复跑也可能因为系统 SDK 漂移而失败或使用错误工具链。过期 contract 断言会把正确的 metadata 优先级行为误判为失败，并污染 baseline 报告。
    - 建议：质量套件应在 validate-only 和 fresh run 后校验 contract baseline 内容，dotnet entry 应走仓库 SDK resolver；执行前内容校验不能阻止一次失败 artifact 被健康复跑覆盖；DeepLearning contract 应与 metadata authoritative 规则一致。
    - 修复：`run_quality_suite.py` 新增 contract baseline 内容校验，要求 Markdown 非空、JSON root/Summary/Operators 完整、`Passed == CaseCount`、`Failed == 0`、suite 声明的 operator 与 baseline 精确匹配且无重复；`--run` 后校验 artifact 新鲜度和内容，执行前不再因旧失败 baseline 阻塞修复性复跑；dotnet command 通过 `scripts/dotnet.ps1 -PrintPath` 解析仓库 SDK 后直接运行，并设置稳定 DOTNET env。`DeepLearningContractRunner` 将过期 mismatch 用例改为 `label_contract_metadata_overrides_external`，验证 metadata labels 仍有效且外部 labels 被忽略；`quick_contract_suite.json` 补齐 `g1_p3_contract_batch2` 的 38 个 operator 声明。已验证 DeepLearning contract 26/26 通过并刷新 baseline，`python -m py_compile quality/tools/run_quality_suite.py` 通过，`python quality/tools/run_quality_suite.py --suite quick_contract_suite --validate-only` 验证 11 个 entry 通过。

124. **质量套件不认识 retained summary，导致重型证据干净签出假失败**
    - 证据：全量执行 `run_quality_suite.py --validate-only` 时，`dataset_heavy_suite`、`full155_quality_suite`、`public_benchmark_suite` 因 `SurfaceDefectDetection_kolektorsdd2_baseline.json` 与 `QualityFlywheel_public_benchmark_proof_baseline.json` 缺失失败；但同目录存在 `.summary.json`，且 `docs/engineering/quality-report-retention.md` 明确大报告 raw payload 从 git 移除、以 `*.summary.json` 保留证据。进一步运行 `run_public_benchmark_proof.py --validate-only` 会直接 FileNotFound，`audit_quasi_industrial_proof.py` 也把 retained summary 当成 proof 缺失；同时 audit 仍按旧口径要求 A/B replay `executedCandidateCaseCount>=183`，而 v2 schema 文档当前门槛为 `>=160`，DeepLearning 默认允许 `unchanged-baseline-control`。
    - 风险：干净签出或 CI 只保留 summary 时，质量套件、public proof validate 和 quasi-industrial audit 会假失败；团队可能为了让 gate 绿而重新提交大体积 raw JSON，或错误地削弱 active suite。另一方面，整数校验若继续用 Python `int(value)`，字符串或浮点计数字段可能被截断成合法整数，掩盖损坏的 baseline/summary。
    - 建议：suite runner 应显式校验 retained summary 的 schema、原始路径、sha256、retention decision、accepted/failed/count/operator 边界；validate-only 型 entry 运行后不应要求 artifact 新鲜刷新。public proof 与 quasi-industrial audit 应遵守同一 retention 策略和 A/B replay v2 schema。
    - 修复：`run_quality_suite.py` 新增 retained summary fallback，raw baseline 缺失时校验 `quality-report-summary/v1`、`sourceReport.originalPath/originalSha256/retentionDecision/originalSizeBytes`、`accepted`、summary count 和 operator 列表；`read_nonnegative_int` 改为严格 JSON integer，拒绝 bool/string/float；`--run` 前不再要求旧 baseline 已存在，运行后对 `--validate-only` entry 跳过 freshness 但仍校验证据内容。`run_public_benchmark_proof.py --validate-only` 现在可验证 retained summary 与 replay manifest；`audit_quasi_industrial_proof.py` 接受 public proof retained summary，并按 A/B replay v2 当前口径检查 `executedCandidateCaseCount>=160`、0 regression、DeepLearning candidate/control 合法、CameraCalibration 覆盖至少 3 个 case。已验证 public proof validate 通过、quasi-industrial audit 55/55 通过，且 10 个 `quality/evals/suites/*.json` 全量 validate-only 均通过。

125. **质量套件 manifest 解析缺少输入边界校验**
    - 证据：`run_quality_suite.py` 修复前在构造 `SuiteEntry` 时直接 `int(raw.get("estimatedSeconds") or 0)`，如果 manifest 写成字符串、浮点或布尔值，会在解析阶段抛 Python traceback，而不是给出可读 validation error；`baselineJson`、`reportMarkdown`、`datasetManifest` 与 dotnet `--project` 也只做路径拼接/存在性检查，没有明确禁止绝对路径或 `..` 跳出仓库。`datasetManifest` 字段本身也没有存在性守门。
    - 风险：suite manifest 一旦误填类型或路径，CI 输出会变成调试成本较高的 traceback；更糟的是路径字段可以指向仓库外部文件，导致本机状态参与质量证据，破坏可复现性和证据边界。缺失 dataset manifest 也可能直到 runner 执行时才失败，validate-only 不能提前发现。
    - 建议：manifest 解析阶段不要因为用户数据类型崩溃；校验阶段统一报告 `estimatedSeconds`、路径类型、仓库边界、dotnet project 与 dataset manifest 存在性。
    - 修复：`SuiteEntry` 新增 `dataset_manifest` 字段；`resolve_repo_path` 只接受非空字符串；`validate_entry_paths` 要求 `baselineJson`、`reportMarkdown`、`datasetManifest` 字段是字符串且解析后仍在 repo 内；`validate_dotnet_command` 要求 `--project` 也留在 repo 内；active entry 的 `datasetManifest` 现在会在 validate-only 阶段检查存在；`estimatedSeconds` 改为非负 JSON integer 校验，非法值不再触发 traceback。已用内存构造的负向 entry 验证越界 baseline、越界 dotnet project、非法 `estimatedSeconds`、缺失 dataset manifest 均被报错；`python -m py_compile quality/tools/run_quality_suite.py` 通过，10 个 `quality/evals/suites/*.json` 全量 validate-only 仍全部通过。

126. **质量套件 command 与 suite 文件入口缺少执行边界**
    - 证据：`run_quality_suite.py` 修复前只对 dotnet `--project` 做了有限存在性检查；manifest 的 `command[0]` 可以写成任意可执行文件，`python`/`powershell -File` 的脚本路径没有统一校验，`powershell -Command` 也可以为空。`--suite` 如果传入一个已存在的仓库外路径，`suite_path` 会直接接受并读取。
    - 风险：质量套件 manifest 或调用参数一旦被误填/篡改，runner 可能执行仓库外脚本或任意命令，使质量证据依赖本机外部状态；空 PowerShell command、缺失 Python 脚本等问题也会拖到执行阶段才失败，validate-only 不能提前发现。
    - 建议：suite runner 应把可执行入口收敛到已知 runner，并在 validate-only 阶段检查 Python/PowerShell 脚本存在且位于仓库内；显式 suite 文件路径也应限制在仓库边界内。
    - 修复：`run_quality_suite.py` 新增 `ALLOWED_COMMANDS` 与 `validate_command`，只允许 `dotnet`、`python`、`powershell`、`pwsh`；Python command 必须指向仓库内存在的 `.py` 脚本或合法 `-m` 模块；PowerShell command 必须使用 `-File` 或 `-Command`，其中 `-File` 必须指向仓库内存在的 `.ps1`，`-Command` 必须非空；`suite_path` 现在拒绝已存在但不在 repo 内的路径。已用负向内存校验覆盖非法 executable、越界 Python 脚本、缺失 Python 脚本、越界 PowerShell `-File`、空 `-Command`，并确认 `C:\Users\A\Desktop\ClearFrostV5` 这类仓库外已存在路径会被拒绝；10 个 suite 全量 validate-only 仍通过。

127. **生成型 suite entry 运行后仍可能用旧 retained summary 假绿**
    - 证据：`run_quality_suite.py` 修复前在 `validate_fresh_artifacts` 中，只要 `baselineJson` 缺失且旁边存在 `.summary.json`，就会调用 `validate_baseline_presence` 接受 retained summary；这对 `--validate-only` 型 entry 是合理的，但对 `surface_defect_detection_kolektorsdd2` 这类实际生成 raw baseline 的 entry，运行后如果 raw JSON 没有产出，旧 summary 仍可能替代本次产物。
    - 风险：长耗时数据集 runner 如果返回 0 但没有写出 raw baseline，且 Markdown 刚好刷新，post-run artifact gate 可能把历史 retained summary 当成本次 evidence，掩盖 artifact 写出失败或路径漂移。
    - 建议：retained summary fallback 只能用于 validate-only/非刷新入口；凡是命令声明会刷新 artifact 的 run，缺少 `baselineJson` 必须按“本次未产出 artifact”失败。
    - 修复：`validate_fresh_artifacts` 现在只有在 `require_fresh=False` 时才允许 `baselineJson` 缺失后退到 retained summary；`require_fresh=True` 的生成型 entry 缺 raw baseline 会直接报 `expected artifact was not produced`。已用 `surface_defect_detection_kolektorsdd2` 构造 post-run 负向检查，确认缺 `SurfaceDefectDetection_kolektorsdd2_baseline.json` 会失败；`dataset_heavy_suite` 与 `public_benchmark_suite` 的 validate-only retained summary 路径仍通过，全量 10 个 suite validate-only 通过。

128. **suite command 的路径参数未提前校验证据边界**
    - 证据：`run_quality_suite.py` 修复前只校验 `baselineJson/reportMarkdown/datasetManifest` 等声明字段，以及 dotnet `--project`；但 command 内的 `--output`、`--report`、`--index`、`--manifest`、`--model-manifest`、`--baseline-output`、`--suite` 等参数没有统一边界检查。扫描现有 suite 可见大量 runner 依赖这些参数写报告或读取数据集；若参数误填为仓库外路径、缺值或指向不存在的 active input，validate-only 无法提前发现。
    - 风险：命令实际输入/输出可能脱离声明字段和仓库边界，导致质量证据写到外部位置、读取本机外部状态，或等到长耗时 runner 执行后才失败；`--output --report` 这类漏值还会把下一个 option 当成路径，错误信息更晚、更难读。
    - 建议：对常见命令路径参数做通用 repo-boundary 校验；输入类路径在 active entry 上要求存在；嵌套 `run_quality_suite.py --suite` 目标应能解析到仓库内 suite。
    - 修复：`run_quality_suite.py` 新增 `COMMAND_REPO_PATH_FLAGS`、`COMMAND_EXISTING_PATH_FLAGS` 与 `validate_command_path_arguments`，要求 `--output/--report/--baseline-output/--deploy-output/--deploy-report/--field-output/--field-report/--audit-output/--audit-report/--index/--manifest/--model-manifest` 的值非空、不是另一个 option 且留在 repo 内；active entry 的 `--index/--manifest/--model-manifest` 还必须存在；`--suite` 目标必须能通过 `suite_path` 解析。已用负向内存校验覆盖越界 output、`--output` 缺值、active `--index` 缺失、缺失 nested suite、`--suite --run` 缺值；全量 10 个 suite validate-only、audit suite run 与 Python 编译均通过。

129. **显式 suite 路径不存在时会退回 basename 误选仓库内 suite**
    - 证据：修复前 `suite_path` 先判断 `Path(value).exists()`，不存在时无论 `value` 是否是显式路径都会取 `path.name` 到 `quality/evals/suites` 下查找；实测 `../quick_contract_suite.json` 和 `C:/Users/A/Desktop/not-real/quick_contract_suite.json` 都被解析成仓库内 `quality/evals/suites/quick_contract_suite.json`。同时传入 `quality/evals/suites/quick_contract_suite.json` 这类相对文件路径时会返回相对 path，后续显示路径依赖当前工作目录。
    - 风险：CI 或嵌套 suite 命令如果误传了仓库外/不存在路径，只要 basename 与现有 suite 重名，就会静默运行另一个 suite，形成非常隐蔽的假验证；错误 suite 路径也可能以 Python traceback 形式暴露，而不是稳定的质量门错误。
    - 建议：只有简单 suite 名称才允许 fallback 到 `quality/evals/suites`；凡是包含目录成分或绝对路径的显式 suite path，都必须真实存在、是文件且位于 repo 内；加载/解析阶段错误应打印 `error:` 并返回 2。
    - 修复：`suite_path` 现在区分简单 suite 名和显式 path，显式 path 不存在或越界都会 fail-closed，存在但不是文件也会失败，并统一返回 repo 内绝对路径；`main` 捕获 suite path/load/parse 阶段的 `OSError/ValueError/JSONDecodeError`，输出干净 `error:` 并返回 2。已验证 `quick_contract_suite`、`quick_contract_suite.json`、`quality/evals/suites/quick_contract_suite.json` 均正常；`../quick_contract_suite.json`、`C:/Users/A/Desktop/not-real/quick_contract_suite.json`、`C:/Users/A/Desktop/ClearFrostV5` 均被拒绝；CLI 负向返回 2 且无 traceback；全量 10 个 suite validate-only 仍通过。

130. **retained contract summary 未复用 suite operators 严格校验且缺少固定回归测试**
    - 证据：`run_quality_suite.py` 修复 retained summary 后，raw contract baseline 会要求 suite entry 的 `operators` 为非空字符串列表且无重复，但 raw baseline 被移出 git、只剩 `.summary.json` 时，`validate_summary_operators` 只在 `operators` 字段存在时做集合比较，缺失或重复的 contract operator 声明可能被 summary 内部的 operators 掩盖。此前这些边界主要靠临时内存 harness 验证，没有可重复的 Python 测试入口。
    - 风险：contract suite manifest 如果遗漏或重复 `operators`，validate-only 可能仍接受 retained summary，导致 operator 覆盖声明缩水而质量门绿灯；后续重构 runner 时也容易回退到手工验证遗漏的状态。
    - 建议：raw baseline 和 retained contract summary 应共用同一套 suite operator 读取/校验逻辑；非 contract retained summary 保持兼容，只有显式声明 `operators` 时才做匹配；把关键负例纳入 `unittest`。
    - 修复：`run_quality_suite.py` 新增 `read_expected_operators`，raw contract baseline 与 retained contract summary 统一要求非空、非重复的 operator 声明，并对 retained summary 的实际 operators 做精确匹配；非 contract retained summary 不再强制要求 suite operators，但若声明了仍会校验。新增 `quality/tools/tests/test_run_quality_suite.py`，覆盖 retained summary 缺失/重复/不匹配 operators、非 contract 兼容、生成型 entry 缺 fresh raw artifact、validate-only retained summary fallback、command path 越界、显式 nested suite 缺失、`estimatedSeconds` bool 拒绝等 10 个回归用例。已验证 `python -m unittest discover quality/tools/tests` 10/10 通过，`python -m py_compile quality/tools/run_quality_suite.py quality/tools/tests/test_run_quality_suite.py` 通过，全量 10 个 `quality/evals/suites/*.json` validate-only 通过。

131. **public benchmark replay manifest 未绑定当前 retained proof，旧 replay seed 可假绿**
    - 证据：`QualityFlywheel_public_benchmark_proof_baseline.json` 已从 git 移除，当前权威证据是 `QualityFlywheel_public_benchmark_proof_baseline.summary.json`，其中 `sourceReport.originalSha256` 为 `sha256:0481d7db...`；但 `QualityFlywheel_public_benchmark_replay_manifest.json` 修复前的 `sourceProofSha256` 为 `d481bfb...`。`run_public_benchmark_proof.py --validate-only` 只检查 replay manifest 有 cases、无重复、accepted 为 true，没有校验 replay manifest 绑定的是当前 proof summary，也没有校验 schema、summary 计数、classCounts 或 replay command 白名单。
    - 风险：public proof raw payload 被 retention 移出 git 后，旧 replay manifest 可能继续作为当前 proof 的 replay seeds 被接受；case 数量、operator 数量、classCounts 或 replay command 被篡改时也可能绿灯，导致 quasi-industrial audit 使用的复现实验入口与声明证据脱节。
    - 建议：public proof validate-only 应在 retained summary 模式下用 `sourceReport.originalSha256` 作为 replay manifest 的绑定依据；replay manifest 应 fail-closed 校验 schema、source baseline、source SHA、summary 计数与 cases 一致、classCounts 一致、operator count 与 proof summary 一致、replay command 只能使用受控 replay runner。
    - 修复：`run_public_benchmark_proof.py` 新增 SHA 规范化、严格整数、proof/replay schema 常量和 replay command 白名单；`validate_replay_manifest` 现在校验 `sourceProofBaseline/sourceProofSha256` 与当前 raw proof 或 retained summary 绑定，核对 `summary.replayCaseCount/operatorCount/classCounts`，并限制 replay command 为 `run_algorithm_ab_replay.py --execute-camera-calibration|--execute-matching`。同步把 replay manifest 的 `sourceProofSha256` 更新为 retained summary 的 `0481d7db11f693b5f4b764e969c0b6174a732b7f857f65c31402333d3b1007b3`。新增 `quality/tools/tests/test_run_public_benchmark_proof.py`，覆盖 SHA mismatch、summary count mismatch、非法 replay command 和 bool count 拒绝。已验证 `python -m unittest discover quality/tools/tests` 15/15 通过，`python quality/tools/run_public_benchmark_proof.py --validate-only` 通过，全量 10 个 suite validate-only 通过，`python quality/tools/audit_quasi_industrial_proof.py` 55/55 通过。

132. **quasi-industrial audit 自身未独立校验 public replay 与 retained proof 绑定**
    - 证据：修复 public proof runner 后，`audit_quasi_industrial_proof.py` 仍只检查 public replay manifest 存在、accepted、有 cases、triage/replayCommand 非空和无 raw path；如果单独运行 audit，`sourceProofSha256` 写成旧 SHA、summary 计数漂移、classCounts 被篡改或 replayCommand 改成非受控命令，audit 不会明确失败。此前真实 artifact 已出现 retained proof SHA 为 `0481d7db...`、replay manifest 为旧 `d481bfb...` 的脱钩状态。
    - 风险：quasi-industrial audit 是高层质量证据聚合入口；即使底层 public proof validate-only 已加固，audit 如果不能自证 replay seeds 与当前 retained proof 一致，调用者只跑 audit 时仍可能得到假绿。
    - 建议：audit 消费 public benchmark 证据时，应从 raw proof sha 或 retained summary `sourceReport.originalSha256` 得到权威 SHA，并独立校验 replay manifest 的 schema、source baseline、source SHA、summary 计数、classCounts、allowed replay command 和 CameraCalibration 专用 replay mode。
    - 修复：`audit_quasi_industrial_proof.py` 新增 strict int、SHA 规范化、schema 常量和 replay command 白名单；`inspect_public_benchmark_proof` 现在检查 retained summary 原始 SHA/size、proof summary count、operator replayCaseCount 总和、replay `sourceProofSha256` 与 proof 绑定、replay case/operator/classCounts 与 proof summary/cases 一致，以及 replay command 只能为受控 A/B replay runner。新增 `quality/tools/tests/test_audit_quasi_industrial_proof.py`，覆盖 matching SHA 正向、stale SHA 负向和非法 command 负向。已验证 `python -m unittest discover quality/tools/tests` 18/18 通过，`python quality/tools/audit_quasi_industrial_proof.py` 64/64 通过，public proof validate-only 与全量 10 个 suite validate-only 均通过。

133. **suite runner 只检查主 baseline/report，新生成的辅助证据可能漏写仍绿**
    - 证据：`run_quality_suite.py` 修复前的 post-run artifact gate 只检查 entry 声明的 `baselineJson` 与 `reportMarkdown`；但真实 suite command 中还存在 `--field-output/--field-report`、`--deploy-output/--deploy-report`、`--audit-output/--audit-report`、`--baseline-output` 等辅助输出，例如 `FrameChangeTriggerDatasetRunner` 同时写主 dataset baseline 和 field-substitute baseline。若 runner 返回 0 但辅助 field/deploy/audit 证据没有写出或仍是旧文件，父级 suite 仍可能只因主 baseline/report 新鲜而绿。
    - 风险：多产物质量 runner 的关键辅助 evidence 可能缺失、陈旧或路径漂移，尤其是 field replay、runtime preview、deployment readiness、governance audit 这类辅助报告；CI 绿灯无法证明完整 evidence set 被刷新。
    - 建议：suite runner 应区分输入类路径和输出类路径；生成型 entry 跑完后，除了 manifest 声明的主 baseline/report，也要把 command 内受控输出参数解析成 expected artifacts 并做存在性/新鲜度检查。`--validate-only` 与 `--dry-run` 型命令应保持非刷新语义，避免误要求新 artifact。
    - 修复：`run_quality_suite.py` 新增 `COMMAND_OUTPUT_PATH_FLAGS`、`command_output_artifact_paths` 与 `expected_artifact_paths`，生成型 entry 会把 `--output/--report/--baseline-output/--field-output/--field-report/--deploy-output/--deploy-report/--audit-output/--audit-report` 纳入 post-run freshness gate；`entry_refreshes_artifacts` 同时识别 `--validate-only` 与 `--dry-run` 为非刷新命令。`quality/tools/tests/test_run_quality_suite.py` 新增辅助输出缺失必失败、dry-run 不刷新 artifact 的回归测试。已验证 `python -m unittest discover quality/tools/tests` 20/20 通过，全量 10 个 suite validate-only 通过，并用 `algorithm_improvement_suite --entry improve_matching_family --run` 确认嵌套 `dataset_heavy_suite --dry-run` 只打印待执行命令且不触发 artifact freshness。
