# AI 视觉 Agent 前端优化验收

日期：2026-09-06。实施基线：`codex初稿`，HEAD `b288bd832e509f2c3a31b51bdb2c568b61db653f`。

## 实施范围

- 布局与视觉基础：紧凑任务栏、工作台优先、320px 桌面对话侧栏、独立窄屏抽屉、深浅主题、Lucide 工具图标、四个业务示例、自增高输入框。
- 待办与方案编排：权威缺项总览、构建前与运行前分组、逐项展开、确认并保存成功后推进、失败保留输入、只读恢复、方案摘要折叠与真实算子链。
- 构建、预览与历史：状态行、失败原因及已有修复建议前置、参数与资源折叠、验证证据分类、应用差异及参数对照、会话恢复和未读提示。
- 清理 Agent 页面旧样式覆盖，增加固定实色品牌令牌保证主操作对比度。展示状态与业务状态分离；原有构建、应用、预览失效、撤销及后端合同沿用。

## 验证结果

| 验证 | 首轮结果 |
| --- | --- |
| `npm run test:unit` | 50 个文件，1069/1069 通过 |
| `npm run test:agent-ui-contract` | 393/393 通过 |
| Shell、Clarification、Build、Responsive Playwright | 94 通过，1 跳过，0 失败，0 flaky |
| 视觉基线 | 审阅并更新 22 张，正常运行复验通过 |
| WebView2 宿主 | 100% 完整、100% 重开、125% 布局、150% 布局，共 4 轮通过 |
| Git 差异检查 | `git diff --check` 通过 |

Playwright 跳过项是既有 `test.skip('AI panel uses WebView2 postMessage contract for generation turns')`，本次未改变其跳过状态。真实宿主的 WebMessage 身份往返由 WebView2 smoke 单独验证。

新增或加强的验证包括：1366/1024 首屏有效控件及失败修复入口；双主题主要文字和主操作至少 4.5:1 的对比度；待办切换和草稿保留；保存失败后延迟推进；后台更新保留对话焦点；只读资源控件；多资源独立绑定与长中文、长路径；对话偏好、未读、抽屉 Escape 和焦点约束；输入框高度、稳定工具位置及 IME 提交。

内置浏览器检查覆盖 1920x1080、1366x768、1024x768、390px 及深浅主题。工作台、空白页、待办、失败和应用预览未见横向溢出或文字遮挡。

## 证据与复现

前端测试工作目录：`ClearVision.Product/tests/ClearVision.Product.UI.Tests`。

```powershell
npm run test:unit
npm run test:agent-ui-contract
npx playwright test ai-shell-visual.spec.ts ai-plan-clarification.spec.ts ai-build-workspace.spec.ts ai-agent-responsive.spec.ts --reporter=json
```

WebView2 使用仓库 `scripts/run-ai-webview2-release-smoke.ps1`，构建最终 Desktop 资产，并采用隔离的认证目录、Agent Run 存储和 WebView2 profile。DPI 通过真实 WebView2 宿主的 `--force-device-scale-factor` 设置，不修改系统显示设置。检查结束后未检测到残留 `ClearVision.Product.Desktop` 进程。

本地运行证据保留在 `.tmp/ai-agent-frontend-20260906/`：

- `verification/frontend-unit.log`、`verification/agent-contract.log`、`verification/playwright.json`、`verification/webview2.log`。
- `webview2-evidence/`：四轮结构化结果和宿主截图。
- 受版本控制的最终截图位于各 Playwright 测试相邻的 `*-snapshots/` 目录。

界面预览：`http://127.0.0.1:5082/index.html?scenario=clarify&theme=light`，使用演示数据及生产前端源码。

## 边界

本轮是本地前端与真实 WebView2 宿主验证，未调用真实模型、相机或 PLC，也不代表远程 CI、现场验收或发布资格。未新增图像推理、结果叠图或画布编辑能力。首轮验收时未提交或推送 Git；原有视觉审查报告及证据保持原状。

## 分类提交复核（2026-09-06）

在已有界面优化基础上，补充修复了后端节点 GUID 与 `metadata.agentTempId` 的匹配，以及 `plan_default.resource_policy` 被误当成算子待办的问题。修复及对应测试与界面优化分开提交：

- `376f1d57`：算子身份匹配、旧策略待办过滤、后端停止生成虚假待办及对应回归测试。
- `296abf34`：工作台展示与交互优化、主题样式、Lucide 资源及界面测试和截图基线。

| 提交前复核 | 结果 |
| --- | --- |
| `npm run test:unit`，含 Agent 合同测试 | 50 个文件，1074/1074 通过 |
| `VisionAgentBuildOrchestratorTests` | 55/55 通过，0 跳过；使用仓库串行脚本重新构建并执行 |
| Shell、Clarification、Build、Responsive Playwright | 96 通过，1 项既有跳过，0 失败；22 张截图基线匹配，未重新生成基线 |
| 各代码提交的暂存差异检查 | `git diff --cached --check` 通过；清理图标文件末尾多余空行 |

构建编排测试的原始结果位于 `.tmp/ai-agent-commit-20260906/build-orchestrator.trx`。构建存在既有的可空性、弃用 API 和程序集版本冲突警告，没有编译错误。界面回归命令为：

```powershell
npx playwright test ai-shell-visual.spec.ts ai-plan-clarification.spec.ts ai-build-workspace.spec.ts ai-agent-responsive.spec.ts --workers=4 --reporter=line
```

本次提交整理没有重新运行 WebView2；已核对首轮 4 次宿主运行记录及原有截图证据。新的 [AI 视觉 Agent 体验提升计划](../../进行中/当前计划/AI视觉Agent体验提升计划-2026-09-06.md) 单独保存为“待实施”，本次只归档计划与分类提交已有改动，不开始实施该计划，也不推送远程。
