# Option D Functional Mapping

CURRENT screenshots and current ClearVision code are the functional authority. This mapping changes visual placement only. Generated text and data are never product truth.

| Screen | Page | CURRENT function | Option D location | Explicit exclusion |
| --- | --- | --- | --- | --- |
| `01_login` | Login | Authenticate with username, password, remember-account, password visibility, login, and existing session or validation feedback. | A compact access panel sits inside a quiet workstation shell with the form aligned to a strict utility grid; recovery feedback stays attached to the form and no product navigation appears. | No marketing hero, SSO, registration, recovery workflow, captcha, MFA, social login, or app navigation. |
| `02_overview` | Overview | Continue recent work, inspect runtime environment, and enter the currently available product functions. | A command-oriented overview uses one continuous work surface: recent work is the dominant upper list, runtime context is a narrow technical strip, and real function entries form a compact launcher below without metric cards. | No invented KPI, analytics, station telemetry, PLC dashboard, alert timeline, or decorative dashboard card grid. |
| `03_projects_data` | Projects - Data | Refresh, search, sort, import, create, inspect, open, export, and delete projects through the real project table and pagination. | A file-explorer-like project workspace places commands and search in a compact top bar and lets the dense project table occupy nearly all remaining space; row actions stay inline. | No bulk actions, tag filters, live run state, flow/operator/asset counts, analytics, or preview detail pane. |
| `04_projects_empty` | Projects - Empty | Show the empty project library while preserving refresh, import, create-project, search, and sort actions. | The same D project explorer remains visible with refresh, import, create, search, and sort in the compact command bar; the empty message and duplicate create entry remain inside the otherwise unframed table field. | No sample projects, onboarding steps, import wizard, statistics, new navigation, or decorative illustration. |
| `05_flow_editor` | Flow Editor | Navigate project context; set final decision; save; open results, checks, run details, global variables, runtime package and Flow template entries; discover/filter/recent/favorite and add real operators; undo/redo/copy/paste/duplicate/enable/delete and zoom the canonical FlowCanvas; inspect node identity, bindings, parameters and validation; access Preview/result context; and retain status context. | The workflow canvas occupies roughly seventy percent of the viewport. Product navigation compresses to a slim rail; the real operator search/category library becomes an on-demand floating picker from the existing discovery entry; the selected-node Inspector is a contextual right dock; preview/result collapses to an on-demand edge workspace; save/run/status commands are grouped by frequency instead of spread across stacked bars. | No second canvas, second save path, Fit, Auto Layout, new toolbar command, invented node, new run mode, new inspector field, or Roboflow business capability. |
| `06_flow_validation_error` | Flow Validation Error | Keep the complete verified Flow Editor inventory available while showing the selected node's real invalid parameter and attached validation feedback, including save, formal run, and run-detail context. | Reuse the D canvas-first master; open the contextual Inspector only as wide as the invalid field requires, attach the error and correction guidance directly to that field, and keep the canvas visually dominant. | No auto-fix, new validation rule, new error action, invented node, or additional run control. |
| `07_flow_preview_roi` | Flow Preview and ROI | Keep the complete verified Flow Editor inventory available; manually preview or cancel/collapse Preview; use the existing image zoom, fit, actual-pixel, large-image and pixel-probe tools; inspect result summary/key/structured output; edit the existing ROI shape and X/Y/Width/Height draft; undo/redo, discard, and apply ROI. | Enter an on-demand Canvas/Preview context: the canvas remains visible on the left, a large resizable image workspace opens on the right, and ROI controls attach contextually to the image; the full parameter Inspector yields space instead of remaining permanently open. | No new ROI shape, new image tool, camera control, preview mode, save channel, or duplicated Inspector. |
| `08_run_ng_modal` | Run Details NG Modal | Inspect formal-run NG identity and status, the six real run metrics, admission checks, recent result link, run technical information and diagnostics in a modal over the Flow workspace; recheck, rerun through the existing formal-run action, and close. | A wide but bounded run-details overlay sits over the unchanged D canvas context, with run identity and NG status first, readiness checks next, and result/diagnostics in a dense lower split; the canvas remains legible behind the dim layer. | No modal tabs, rerun variant, export, approval workflow, invented metric, new NG action, or desktop window control. |
| `09_results_investigation` | Results Investigation | Switch between 态势总览 and 调查详情; return to the workspace; refresh/export; filter by source, local project, execution/judgment and additional real filters; paginate; select a run; inspect OK/NG evidence, diagnostics, defects and traceability; compare with baseline/current/previous success; and export evidence when present. | A three-zone investigation workspace uses a narrow result chronology, a dominant evidence/detail field, and a contextual diagnostics rail; comparison actions stay adjacent to the selected evidence rather than in dashboard cards. | No invented column, bulk action, analytics dashboard, new comparison mode, or defect workflow. |
| `10_stations_list` | Stations List | Switch between 全站概览 and 异常调查; in the shown 异常调查 state, refresh and search the real station fleet, filter connection and run status, scan the station table, and follow the existing station-detail link while retaining read-only/realtime recovery context. | A fleet operations workspace gives the existing 异常调查 filters and station table the dominant surface, with the real read-only/recovery state in a compact command strip and the existing 全站概览 entry retained without rendering its content on this viewport. | No create, edit, delete, firmware, uncontracted setting, same-page detail inspector, same-page live-data rail, same-page results band, or analytics beyond the existing 全站概览 summary/statistics. |
| `11_station_detail` | Station Detail | In the shown Engineer/read-only state, inspect station identity badges, realtime recovery warning, status overview, recent results and result count/link, production trace chain and health snapshot; refresh and return to the station list. | Station identity, recovery and refresh/return actions form a compact context bar; overview, recent-result trace, production trace and health snapshot occupy aligned evidence bands without introducing administrator commands or new tabs. | No station edit form, delete/create, firmware control, administrator command panel, Ping, reload, stop-run, package deployment, new tab, or new device capability. |
| `12_inspection` | Inspection | Recover realtime state, start/stop continuous inspection, verify readiness, inspect six real metrics, device/run summary, pre-run checks, and the single recent result with diagnostics. | A compact runtime command strip anchors start, stop, and state reconciliation; the six existing metrics become a single horizontal evidence ribbon, with device summary, 6/7 readiness checks, and the one recent result arranged as a disciplined lower workbench. | No new mode selector, trigger selector, evidence canvas, expanded result table, cycle statistics, camera/PLC configuration, manual upload, desktop controls, or invented values. |
| `13_ai_workspace` | AI Workspace | Review AI candidate readiness, plan/build summary, candidate difference, validation, run rehearsal, handoff gate, local draft notice, session version, and technical identity before workspace review. | Use the same workflow-engineering shell as D Flow but no fake canvas: a compact left context column holds candidate readiness and next step; the central workspace shows plan/build and candidate difference; a right contextual rail holds validation, rehearsal, and handoff evidence. AI reads as an auxiliary engineering layer. | No chatbot composer, prompt gallery, application-preview canvas, session dropdown, bind-project action, model marketplace, token usage, magic action, node canvas, or second write channel. |
| `14_ai_failure_recovery` | AI Failure Recovery | Show blocked or failed AI stage, authoritative server replay/recovery state, warnings, history, diagnostics, and existing recovery actions without duplicating writes. | Reuse the D AI three-zone workbench; the failed stage and server-authority warning take the contextual rail, while preserved history and diagnostic evidence remain visible without replacing the candidate workspace. | No blind retry, auto-fix, new write action, new recovery mode, invented terminal result, or new AI ability. |
| `15_operator_catalog` | Operator Catalog | Search and filter the read-only operator catalog by real category/lifecycle/scope/ports/parameters and inspect details through the existing table and pagination. | A search-first engineering index gives the compact filter command bar immediate priority and lets the read-only operator table occupy the full work surface; category and lifecycle remain filters, not new navigation. | No install, edit, delete, run, marketplace, or catalog statistics. |
| `16_system_settings` | System Settings | Navigate the real Settings groups and edit only software title and product theme while auto-start remains read-only, then discard or save the General group. | A slim grouped settings rail replaces the broad page chrome; one wide configuration sheet occupies the workspace with a persistent group-status/save footer and no duplicate horizontal tabs. | No password field, duplicate settings tabs, new category, telemetry, desktop window control, enabled auto-start, or second save path. |
| `17_camera_settings` | Camera Settings | Refresh and discover all supported vendors or Huaray/Hikvision cameras; inspect the real discovery columns; select and save an existing binding; edit its display/active/enabled, acquisition and trigger fields; learn/test Enter or serial photoelectric input; capture one frame or start/stop continuous debug preview; and inspect preview/resource diagnostics. | A device workbench uses a narrow discovery/binding column, a large selected-camera acquisition and trigger workspace, and a contextual single-frame/continuous-preview diagnostic rail; the existing save-binding boundary remains explicit. | No new vendor, SDK status, firmware, Settings calibration control or wizard, acquisition mode, automatic binding, or hardware-connectivity claim. |
| `18_plc_settings` | PLC Settings | Configure S7/MC/FINS protocol, heartbeat and protocol-specific connection fields; test connection; inspect validation; add/edit/delete variables through the exact mapping columns 变量名/地址/数据类型/说明/可写/操作; preserve protocolMismatch; save protocol settings separately from the current mapping. | Connection identity, heartbeat, validation and test form a compact upper configuration strip, while the address mapping table becomes the dominant lower work surface with separate 保存协议设置 and 保存当前映射 boundaries and visible protocolMismatch dependency. | No new protocol, live hardware claim, PLC program editor, new mapping column, or diagnostics module. |
| `19_tcp_settings` | TCP Settings | Manage existing TCP Client/Server profiles, connect/disconnect or start/stop, send text/HEX, receive traffic, inspect the real log, and clear it. | A three-pane communications workbench uses the real Client/Server profile source list, a central profile/connection editor, and a dominant runtime/send-response traffic console with the exact columns 时间/方向/字节/文本/HEX/端点. | No new transport, packet analyzer, live-network claim, profile mode, log column, or telemetry chart. |
| `20_station_communication` | Station Communication | Refresh the authoritative Station communication projection; configure Disabled/LocalLoopback/LanController mode, Studio port, LAN host and local Station sync; explicitly preserve or replace a masked token or regenerate it under existing guards; discard or save; inspect effective-state/restart feedback, endpoints and diagnostics. | A mode-driven configuration workspace places port/host/local-sync and the preserve/replace/regenerate token boundary in the primary column, with effective state, restart feedback and diagnostics in a contextual secondary rail and one anchored save action. | No snippets, raw token display/copy, Station monitor, generic cancel, new transport, remote control, live connectivity claim, token authority, sync mode, or topology dashboard. |
| `21_ai_model_settings` | AI Model Settings | Refresh the model catalog; create/select/delete model configurations; activate the selected model or assign planner/shadow roles while preserving visible statuses; edit display name, provider, model, enablement, endpoint and protected API-key operation; test connection; expand advanced connection/scheduling details; read inference support; discard or save. | A dense model-catalog source column and provider configuration workbench place selection/activation/planner/shadow status beside identity/endpoint/security fields; test, the advanced details disclosure and inference support form a contextual rail with one save footer. | No quality analytics, token/cost dashboard, marketplace, prompt preset, plaintext secret, or new provider capability. |
| `22_diagnostics` | Diagnostics | Read local service, session, desktop-host, version and environment status, expand technical diagnostics, refresh, and safely copy diagnostic information. | A read-only technical console uses a compact status header, aligned service/session/host facts, and a dominant expandable technical projection; copy and refresh stay as the only commands. | No execution control, service restart, secret display, new telemetry, breadcrumb, title icon tile, desktop window control, or alternate shell action. |
| `23_about` | About | Explain product, host, backend, version, license/support identity, and the distinction between Studio configuration and Runtime/Station execution. | A compact identity and composition sheet uses disciplined technical rows and one explanatory band inside the normal product shell. | No invented version/license state, update action, runtime control, support service, or marketing hero. |
| `24_forbidden` | Forbidden | Explain the authority boundary and return the user to the project library through the single existing action. | A restrained authority panel sits within the product access shell, with the reason, impact, and single return action aligned as a compact operational message. | No retry, request-access flow, role change, login control, permission editor, support chat, or new navigation. |

Coverage: `24/24` frozen screens. Functional target: `0 additions / 0 omissions`.
