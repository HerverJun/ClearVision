# Option E Functional Mapping

CURRENT screenshots and current ClearVision code are the functional authority. This mapping changes visual placement only. Generated text and data are never product truth.

| Screen | Page | CURRENT function | Option E location | Explicit exclusion |
| --- | --- | --- | --- | --- |
| `01_login` | Login | Authenticate with username, password, remember-account, password visibility, login, and existing session or validation feedback. | A calm, centered authentication sheet uses exceptional spacing and typography while keeping the same single form, feedback position, and one primary login action. | No marketing hero, SSO, registration, recovery workflow, captcha, MFA, social login, or app navigation. |
| `02_overview` | Overview | Continue recent work, inspect runtime environment, and enter the currently available product functions. | A refined full-width overview gives recent work the primary visual weight, places environment facts in a restrained secondary band, and presents available functions as a disciplined text-and-icon index. | No invented KPI, analytics, station telemetry, PLC dashboard, alert timeline, or decorative dashboard card grid. |
| `03_projects_data` | Projects - Data | Refresh, search, sort, import, create, inspect, open, export, and delete projects through the real project table and pagination. | A premium project library uses a quiet command bar, precise table typography, generous column alignment, and a stable pagination footer without converting projects into cards. | No bulk actions, tag filters, live run state, flow/operator/asset counts, analytics, or preview detail pane. |
| `04_projects_empty` | Projects - Empty | Show the empty project library while preserving refresh, import, create-project, search, and sort actions. | The same E project library remains structurally stable with the real refresh/import/create/search/sort toolbar; the empty message sits in the table field with the existing create command emphasized and no onboarding illustration. | No sample projects, onboarding steps, import wizard, statistics, new navigation, or decorative illustration. |
| `05_flow_editor` | Flow Editor | Navigate project context; set final decision; save; open results, checks, run details, global variables, runtime package and Flow template entries; discover/filter/recent/favorite and add real operators; undo/redo/copy/paste/duplicate/enable/delete and zoom the canonical FlowCanvas; inspect node identity, bindings, parameters and validation; access Preview/result context; and retain status context. | A unified premium workspace gives the canvas dominant area beneath one quiet command bar. The real operator library appears as a refined contextual palette, the selected-node Inspector becomes an adaptive material panel, and preview/result stays available as a restrained dock without permanently consuming canvas height. | No second canvas, second save path, Fit, Auto Layout, new toolbar command, invented node, new run mode, new inspector field, or Roboflow business capability. |
| `06_flow_validation_error` | Flow Validation Error | Keep the complete verified Flow Editor inventory available while showing the selected node's real invalid parameter and attached validation feedback, including save, formal run, and run-detail context. | Reuse the E unified canvas master; the adaptive Inspector gains a precise inline error treatment and a quiet validation summary while all other workspace regions remain stable. | No auto-fix, new validation rule, new error action, invented node, or additional run control. |
| `07_flow_preview_roi` | Flow Preview and ROI | Keep the complete verified Flow Editor inventory available; manually preview or cancel/collapse Preview; use the existing image zoom, fit, actual-pixel, large-image and pixel-probe tools; inspect result summary/key/structured output; edit the existing ROI shape and X/Y/Width/Height draft; undo/redo, discard, and apply ROI. | A refined media workspace slides into the E canvas area with a large dark image surface, a compact ROI tool cluster, and a slim result/details column; cancel and apply remain a clear paired boundary. | No new ROI shape, new image tool, camera control, preview mode, save channel, or duplicated Inspector. |
| `08_run_ng_modal` | Run Details NG Modal | Inspect formal-run NG identity and status, the six real run metrics, admission checks, recent result link, run technical information and diagnostics in a modal over the Flow workspace; recheck, rerun through the existing formal-run action, and close. | A precisely proportioned elevated sheet overlays the E workspace, using controlled depth and typography to separate run identity, checks, result, and diagnostics without becoming a page or dashboard. | No modal tabs, rerun variant, export, approval workflow, invented metric, new NG action, or desktop window control. |
| `09_results_investigation` | Results Investigation | Switch between 态势总览 and 调查详情; return to the workspace; refresh/export; filter by source, local project, execution/judgment and additional real filters; paginate; select a run; inspect OK/NG evidence, diagnostics, defects and traceability; compare with baseline/current/previous success; and export evidence when present. | A premium split view places the result list in a quiet source column and gives evidence, traceability, diagnostics, and existing comparison commands a spacious but dense detail canvas. | No invented column, bulk action, analytics dashboard, new comparison mode, or defect workflow. |
| `10_stations_list` | Stations List | Switch between 全站概览 and 异常调查; in the shown 异常调查 state, refresh and search the real station fleet, filter connection and run status, scan the station table, and follow the existing station-detail link while retaining read-only/realtime recovery context. | A refined fleet list keeps the shown 异常调查 search, connection/run filters and station rows on one continuous high-scanability surface; 全站概览 remains the existing alternate view, not a same-page dashboard. | No create, edit, delete, firmware, uncontracted setting, same-page detail inspector, same-page live-data rail, same-page results band, or analytics beyond the existing 全站概览 summary/statistics. |
| `11_station_detail` | Station Detail | In the shown Engineer/read-only state, inspect station identity badges, realtime recovery warning, status overview, recent results and result count/link, production trace chain and health snapshot; refresh and return to the station list. | A calm read-only operations detail sheet uses progressive section hierarchy and consistent evidence rows for identity, recovery, overview, recent results, traceability and health, with refresh/return kept explicit. | No station edit form, delete/create, firmware control, administrator command panel, Ping, reload, stop-run, package deployment, new tab, or new device capability. |
| `12_inspection` | Inspection | Recover realtime state, start/stop continuous inspection, verify readiness, inspect six real metrics, device/run summary, pre-run checks, and the single recent result with diagnostics. | A quiet continuous-inspection surface uses controlled hierarchy: runtime context and actions first, the same six values as an integrated status band, then the three real lower evidence groups on a common baseline. | No new mode selector, trigger selector, evidence canvas, expanded result table, cycle statistics, camera/PLC configuration, manual upload, desktop controls, or invented values. |
| `13_ai_workspace` | AI Workspace | Review AI candidate readiness, plan/build summary, candidate difference, validation, run rehearsal, handoff gate, local draft notice, session version, and technical identity before workspace review. | A premium two-column engineering review pairs candidate intent/difference with validation/handoff evidence, using exceptional typography and quiet material transitions; status and the two real actions remain concise at the top. | No chatbot composer, prompt gallery, application-preview canvas, session dropdown, bind-project action, model marketplace, token usage, magic action, node canvas, or second write channel. |
| `14_ai_failure_recovery` | AI Failure Recovery | Show blocked or failed AI stage, authoritative server replay/recovery state, warnings, history, diagnostics, and existing recovery actions without duplicating writes. | Reuse the E AI review composition with a restrained failure band, clear authority explanation, and existing recovery/history actions kept adjacent to their evidence. | No blind retry, auto-fix, new write action, new recovery mode, invented terminal result, or new AI ability. |
| `15_operator_catalog` | Operator Catalog | Search and filter the read-only operator catalog by real category/lifecycle/scope/ports/parameters and inspect details through the existing table and pagination. | A refined catalog uses a quiet filter ribbon, high-legibility dense rows, precise port/parameter metadata, and stable detail actions without cardifying operators. | No install, edit, delete, run, marketplace, or catalog statistics. |
| `16_system_settings` | System Settings | Navigate the real Settings groups and edit only software title and product theme while auto-start remains read-only, then discard or save the General group. | A premium source-list settings shell pairs a quiet grouped navigation column with one beautifully aligned General form and a restrained bottom action boundary. | No password field, duplicate settings tabs, new category, telemetry, desktop window control, enabled auto-start, or second save path. |
| `17_camera_settings` | Camera Settings | Refresh and discover all supported vendors or Huaray/Hikvision cameras; inspect the real discovery columns; select and save an existing binding; edit its display/active/enabled, acquisition and trigger fields; learn/test Enter or serial photoelectric input; capture one frame or start/stop continuous debug preview; and inspect preview/resource diagnostics. | A premium camera surface gives the image preview real visual weight while a refined adaptive inspector holds binding, acquisition, trigger, and diagnostics; discovery actions stay in a compact command bar. | No new vendor, SDK status, firmware, Settings calibration control or wizard, acquisition mode, automatic binding, or hardware-connectivity claim. |
| `18_plc_settings` | PLC Settings | Configure S7/MC/FINS protocol, heartbeat and protocol-specific connection fields; test connection; inspect validation; add/edit/delete variables through the exact mapping columns 变量名/地址/数据类型/说明/可写/操作; preserve protocolMismatch; save protocol settings separately from the current mapping. | A refined single-sheet PLC editor groups protocol, heartbeat, connection essentials, validation and protocol-specific fields above a spacious precise mapping table; the two distinct save actions remain anchored to their real scopes. | No new protocol, live hardware claim, PLC program editor, new mapping column, or diagnostics module. |
| `19_tcp_settings` | TCP Settings | Manage existing TCP Client/Server profiles, connect/disconnect or start/stop, send text/HEX, receive traffic, inspect the real log, and clear it. | A premium split view aligns profile selection and editor on the left with a large, quiet traffic/debugging field on the right; connection and send actions remain contextually grouped. | No new transport, packet analyzer, live-network claim, profile mode, log column, or telemetry chart. |
| `20_station_communication` | Station Communication | Refresh the authoritative Station communication projection; configure Disabled/LocalLoopback/LanController mode, Studio port, LAN host and local Station sync; explicitly preserve or replace a masked token or regenerate it under existing guards; discard or save; inspect effective-state/restart feedback, endpoints and diagnostics. | A refined communications sheet uses controlled section spacing for mode, port/host, local sync, token operation, discard/save, effective-state/restart feedback and diagnostics while keeping security-sensitive actions visibly distinct. | No snippets, raw token display/copy, Station monitor, generic cancel, new transport, remote control, live connectivity claim, token authority, sync mode, or topology dashboard. |
| `21_ai_model_settings` | AI Model Settings | Refresh the model catalog; create/select/delete model configurations; activate the selected model or assign planner/shadow roles while preserving visible statuses; edit display name, provider, model, enablement, endpoint and protected API-key operation; test connection; expand advanced connection/scheduling details; read inference support; discard or save. | A premium catalog-and-model sheet uses progressive disclosure and exceptional field rhythm; model role/status actions, credential boundary, test result, advanced details, inference support, discard and save remain explicit and singular. | No quality analytics, token/cost dashboard, marketplace, prompt preset, plaintext secret, or new provider capability. |
| `22_diagnostics` | Diagnostics | Read local service, session, desktop-host, version and environment status, expand technical diagnostics, refresh, and safely copy diagnostic information. | A quiet premium diagnostics sheet turns status, environment, and technical disclosure into one continuous hierarchy with precise monospace detail where needed and no control-center styling. | No execution control, service restart, secret display, new telemetry, breadcrumb, title icon tile, desktop window control, or alternate shell action. |
| `23_about` | About | Explain product, host, backend, version, license/support identity, and the distinction between Studio configuration and Runtime/Station execution. | A calm, exceptionally typeset product identity surface presents version, host, license/support, and composition without marketing treatment or oversized branding. | No invented version/license state, update action, runtime control, support service, or marketing hero. |
| `24_forbidden` | Forbidden | Explain the authority boundary and return the user to the project library through the single existing action. | A quiet centered permission sheet uses controlled contrast and typography to explain the boundary, ending with the same single return action. | No retry, request-access flow, role change, login control, permission editor, support chat, or new navigation. |

Coverage: `24/24` frozen screens. Functional target: `0 additions / 0 omissions`.
