# Option D Round 9 Reject Audit

Audit date: `2026-08-17`

Result: `2/7 PASS`, `5/7 REJECT`. With the previously frozen passes, Option D now has `14/24` screens through the manual Reject Gate.

`Generated` means the exact `gpt-image-2` workflow produced and validated a file. It does not mean the screen passed functional truth or visual consistency review.

| ID | Decision | Minimum blocking evidence |
| --- | --- | --- |
| `D_06_flow_validation_error` | REJECT | The actual zones remain about 65.7% Canvas / 464px Inspector / 177px Preview edge instead of at least 68% / 320-340px / 190-220px. The output also drops the frozen status row, port/output facts and verified commands, while adding Windows controls, an unverified multi-icon capability rail and a fit/fullscreen-like control. |
| `D_07_flow_preview_roi` | REJECT | The CURRENT-dominant rebuild loses the project/save/formal-run context and ROI draft warning, adds an unverified `取消预览` action, changes the CURRENT image evidence, and leaves the dotted Canvas at roughly 63% rather than the frozen 59-61% target. |
| `D_08_run_ng_modal` | REJECT | One real top-level `流程` entry is restored and the heading is `近期结果`, but forbidden `适应画布` remains. The output changes CURRENT `运行包 / 不适用` to `未通过`, adds Windows/help/notification controls and duplicates modal actions. |
| `D_11_station_detail` | REJECT | Required facts appear, but the immutable Round 7 two-row composition is reflowed into new columns and a new four-column diagnostics table; the `连接恢复中` badge is also removed. |
| `D_14_ai_failure_recovery` | REJECT | A new full-height recovery rail duplicates actions and stage/session content despite the frozen no-sidebar layout. `协调服务端状态`, the exact Shell identity, and the required right-side authority copy also drift. |
| `D_15_operator_catalog` | PASS | Controlled Picture Layer corrections remain entirely inside the four declared boxes; rows 004-013 including 008, `实验性 / 4 项`, `浅色·紧凑`, and `fixture-engineer / 工程师` are exact, with no invented function. |
| `D_20_station_communication` | PASS | Controlled Picture Layer corrections remain entirely inside the two declared boxes; only Windows controls and the left-rail `收起` affordance were removed, with all verified Shell, configuration, data and command pixels preserved. |

## Next Correction Route

- Prefer immutable-source Picture Layer correction for `D_03`, `D_04`, `D_09`, `D_10`, and `D_21`, where the remaining blockers are bounded factual or Shell regions.
- For `D_06`, `D_07`, `D_08`, `D_11`, and `D_14`, use a CURRENT-dominant or immutable-source retry only when the prompt can freeze every non-blocking region. Do not promote visually polished recompositions that alter product facts or page ownership.
