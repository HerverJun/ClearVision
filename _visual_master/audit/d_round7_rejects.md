# Option D Round 7 Reject Audit

Audit date: `2026-08-17`

Result: `1/14 PASS`, `13/14 REJECT`.

`D_02_overview` passed all CURRENT-content, Product Shell, layout, and Option D Reject Gates. It is frozen and must not be regenerated in Round 8.

| ID | Decision | Minimum blocking evidence |
| --- | --- | --- |
| `D_02_overview` | PASS | Exact recent project/time/actions, runtime and launcher copy, user role, normal Shell, and launcher-only entries are present. |
| `D_03_projects_data` | REJECT | Confirmed visible `搜索` action is omitted; exact Shell appearance summary must remain `浅色·紧凑`. |
| `D_04_projects_empty` | REJECT | Confirmed empty-state title `暂无工程` is omitted. |
| `D_06_flow_validation_error` | REJECT | Canvas is about 75% and Inspector about 443px, outside the 68-72% / 300-340px geometry gate. |
| `D_07_flow_preview_roi` | REJECT | Adds a permanent left capability rail and top-level `流程`; the dotted FlowCanvas itself occupies only about 41%. |
| `D_08_run_ng_modal` | REJECT | Adds top-level `流程` and forbidden `自动布局`; several admission-check labels drift from CURRENT. |
| `D_09_results_investigation` | REJECT | Shell changes `算子库` to `助手库` and omits service/appearance/more; defect evidence drifts from `Scratch / 置信度 0.910 / 轻微划痕`. |
| `D_10_stations_list` | REJECT | User is misspelled `fure-engineer`; the confirmed refresh action is omitted. |
| `D_11_station_detail` | REJECT | `线序错误` and `WIRE_SWAP` are split across different result rows; confirmed image-upload and identity-gap evidence is omitted. |
| `D_12_inspection` | REJECT | Project context remains `在线持续检测中` instead of `在线瓶盖检测`; readiness text says `已确认保存修订版本` instead of `已确认保存版本`. |
| `D_14_ai_failure_recovery` | REJECT | Adds a duplicate permanent left navigation rail and moves a main recovery action despite a Shell-only instruction. |
| `D_15_operator_catalog` | REJECT | `颜色分析` lifecycle/parameter facts drift to `稳定 / 1项`; Shell adds `语言 中文`, loses compact density, and changes user. |
| `D_20_station_communication` | REJECT | Replaces the normal Product Shell with Settings groups and removes the confirmed left-rail `设置` title. |
| `D_21_ai_model_settings` | REJECT | Invents a successful connection test, latency, status code, and timestamp where CURRENT is untested. |

Round 8 may change only these blockers. All other Round 7 regions are frozen.
